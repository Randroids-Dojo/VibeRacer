# VibeRacer: Game Design Document

> **Before editing:** read the repo root `AGENTS.md`. The em-dash ban applies to this file.

---

## Status (last updated 2026-04-19)

**Status key.** Each section below carries a `**Status.**` line. Sections that have shipped also include a `### Build log` subsection recording what landed, the key files, and any non-obvious decisions. This GDD is intended as a record of truth: when code lands, the relevant section is updated.

| § | Section | Status |
| - | - | - |
| 2 | Core game loop | partial (countdown, race, HUD, lap auto-submit, pause, restart, fresh-slug prompt, PB fanfare with centered HUD burst all work) |
| 3 | Camera and perspective | partial (trailing third-person rig with lerp; player-tunable sliders for height, distance, look-ahead, follow speed, FOV live in Settings; time-of-day lighting presets shipped) |
| 4 | Controls | partial (keyboard WASD/arrows/space + Esc pause + dual-stick or single-stick touch + remappable keyboard bindings; gamepad live with remappable buttons) |
| 5 | Vehicle | partial (arcade integrator + off-track drag; Kenney model + raycast per wheel pending) |
| 6 | Track system | partial (default track renders in 3D; editor UI ships at `/[slug]/edit` with cycle-on-click placement, live validation, and save to `PUT /api/track/[slug]`) |
| 7 | Routing and user-owned paths | partial (middleware + `/[slug]` page + initials prompt + fresh-slug create-or-load + Settings pane on home and pause menu live, with inline initials editing in the Settings pane) |
| 8 | Race flow | partial (countdown with animated red/amber/green traffic light + synth beeps, per-track configurable checkpoint count, lap detection, invalid-lap reset, wrong-way warning banner, drift score HUD, and the full HUD all live) |
| 9 | Title, menu, pause | partial (pause menu and title screen with Play / Load existing / Settings ship; Settings pane is now live for keyboard remap and dual / single touch mode) |
| 10 | Physics tuning (player Setup panel) | done (per-track sliders, last-loaded carryover, leaderboard-attached setups, Try-this-setup) |
| 11 | Leaderboards | partial (autosubmit, anti-cheat, leaderboard UI with version dropdown + race-this-version, overall record in HUD, PB fanfare and record fanfare with a centered HUD burst all live; admin tooling and pagination beyond top 100 pending) |
| 12 | Feedback FAB | partial (API route + React component ship, pause-only visibility wired; deeper copy testing pending) |
| 13 | Audio | partial (music + countdown beeps + engine drone, tire skid, off-track rumble, lap stinger, PB / record fanfare, UI click variants, and a centered HUD burst on PB / record all ship; deeper SFX polish and SFX volume slider pending) |
| 14 | Data model | done |
| 15 | Tech stack | done (scaffold present) |
| 16 | Architecture | partial (game loop + Three.js scene + PauseMenu + FeedbackFab + track editor + music scheduler + touch controls + SFX layer landed; minor module follow-ups still open) |
| 17 | Deployment (manual setup) | done |
| 18 | Stretch and future | out of scope |

Infrastructure commit: `703f080` (Next.js + KV + anti-cheat + four API routes). Vertical slice commit: `194bf91` (`/[slug]` route drives a default 8-piece oval with countdown, physics, camera, HUD, and auto-submit). 128 Vitest unit tests and 8 Playwright smoke tests passing; production build green.

---

## 1. Vision & Pillars

**Pitch.** A cartoony 3D arcade racer where every URL is a playground. Visit a slug, race the track that lives there, submit a lap time, or fork it into a new version. Tracks are built from toy-like snap pieces. A streaming synth soundtrack plays throughout.

**Design pillars.**

1. **Instant fun.** Open a URL, be driving in seconds. No login walls, no lobbies.
2. **Your track, your URL.** Every path in the domain is someone's creation. Anyone can race or fork any track.
3. **Leaderboards that matter.** Per-version boards. Scores autosubmit. Best-effort anti-cheat keeps times honest.

---

## 2. Core Game Loop

**Status.** Partial. Countdown, race, HUD, per-lap auto-submit, pause (Esc + on-screen button), and restart are live at `/[slug]`. Track editing access, load-existing prompt, and PB celebration are not yet wired.

1. Player lands on `/<slug>`.
2. If a track exists at that slug, the latest version loads. If not, a prompt offers "create new track" or "load existing".
3. Countdown: 3, 2, 1, GO.
4. Player races. Crosses finish line. Lap time shown.
5. Lap auto-submits in the background.
6. Loop continues forever. Best lap stays on the HUD. Any new personal best triggers a small celebration.
7. Player can pause to view leaderboards, edit the track, change settings, or exit.

### Build log

- Orchestrator: `src/components/Game.tsx`. `Game` handles initials lifecycle; `GameSession` owns the Three.js canvas, tick loop, phase state (`countdown` | `racing`), and HUD state.
- Tick: `src/game/tick.ts` is a pure `(state, input, dtMs, nowMs, path, params?) => { state, lapComplete | null }`. Physics is frozen until `startRace()` sets `raceStartMs`. Lap complete event fires when all checkpoints are hit in order and the car re-enters piece 0.
- Render loop: `requestAnimationFrame` drives tick + Three.js render every frame. HUD `setState` is throttled to ~20 Hz with a reference-equality bail-out so the HUD tree doesn't re-render when nothing visible changed.
- Tests: `tests/unit/tick.test.ts` covers init state, frozen physics before start, teleporting through all checkpoints to complete a lap, ignoring unexpected cells, and invalid-lap reset when the car re-enters the start piece mid-lap.
- Pause: `GameSession` owns `paused` state plus `pausedRef`, `pauseStartTsRef`, and `resumeShiftRef`. When paused, the RAF loop short-circuits before `tick()` runs. On resume, the accumulated pause duration is added to `state.raceStartMs` so the current lap timer resumes cleanly without a one-frame jump. Pause is only available during the `racing` phase (Esc is ignored during countdown).
- Restart: `restart()` sets `pendingResetRef.current = true`; the loop re-inits game state, camera rig, and renders one frame. Phase flips back to `countdown` so the READY-SET-GO sequence plays again. Tokens and session PB are cleared; all-time PB (persisted in `localStorage`) is preserved.
- Fresh-slug prompt: `src/app/[slug]/page.tsx` now differentiates "KV configured but no saved track for this slug" from "KV unreachable". In the former case it renders `src/components/SlugLanding.tsx` instead of silently serving the default oval. The landing shows the slug as an eyebrow, a `Create new track` CTA linking to `/<slug>/edit`, and a `Load existing` list of recently-updated slugs read from `track:index` via `src/lib/recentTracks.ts` (`zrange` with `{ rev: true, withScores: true }`, top 10). When KV is empty the list shows a friendly "nothing built yet" message. When KV env vars are missing (local dev without KV), the page keeps falling back to the default track so `npm run dev` remains playable with no setup.
- **Not yet landed.** PB fanfare, "Edit Track" entry point from the pause menu.

---

## 3. Camera & Perspective

**Status.** Partial. Trailing third-person rig with linear position + target lerp is live. The four player-facing camera tunables (height, distance, look-ahead, follow speed) are now exposed in Settings and persisted in the existing `viberacer.controls` localStorage key. Time-of-day lighting presets (Noon, Morning, Sunset, Night) skin the sky, ambient light, and sun direction live from Settings. Weather presets (Clear, Cloudy, Foggy, Rainy, Snowy) layer fog, sky tint, ambient / sun multipliers, and the appropriate falling particle layer on top of the time-of-day skin. Track authors can bake a preferred mood (timeOfDay and / or weather) into the saved track version, and every player who races that version sees the author's intended look unless they turn off "Respect track mood" in Settings. Quaternion slerp on orientation is the remaining open item.

Trailing third-person camera, Forza Horizon style.

- Position: behind the car, slightly above, car fully in frame.
- Motion: smooth lerp toward car's heading. Position uses linear interpolation, orientation uses quaternion slerp.
- Tunable parameters: offset height, trailing distance, lerp speed, look-ahead bias.
- Implementation: raw Three.js. Camera position and quaternion updated each tick.

### Build log

- `src/game/sceneBuilder.ts` exports `initCameraRig(carX, carZ, heading)`, `updateCameraRig(rig, carX, carZ, heading, params?)` (mutates `rig` in place to avoid per-frame allocations), and `DEFAULT_CAMERA_RIG`.
- Defaults: `height=6`, `distance=14`, `lookAhead=6`, `positionLerp=0.12`, `targetLerp=0.2`.
- The look-at target is projected `lookAhead` units ahead of the car along its heading so the camera anticipates turns rather than trailing dead behind.
- Player-facing camera tuning: live. `ControlSettings` gained a `camera: CameraRigSettings` field with `height`, `distance`, `lookAhead`, `followSpeed`, and `fov`. Defaults are tuned to reproduce the legacy hardcoded rig exactly so users who never open the panel see the same view they did before (FOV defaults to 70 degrees, matching the original `PerspectiveCamera(70, ...)` constant in `sceneBuilder.ts`). `cameraLerpsFor(followSpeed)` in `src/lib/controlSettings.ts` collapses the two raw lerps onto one slider: `positionLerp = 0.12 * followSpeed`, `targetLerp = 0.2 * followSpeed`, both clamped to `[0, 1]`. `Game.tsx` and `TuningSession.tsx` rebuild a `CameraRigParams` ref every render and pass it to `RaceCanvas` via the new `cameraRigRef` prop. The rAF loop reads the ref each frame so a slider tweak in the pause-menu Settings pane takes effect on resume without re-mounting the renderer; FOV specifically is applied through a dedicated `syncFov` helper that compares against the last applied value and only calls `camera.updateProjectionMatrix()` when the value actually changes (cheap no-op on every frame the slider is idle). Settings UI: a new Camera section under the Controls block in `SettingsPane.tsx` exposes five sliders (Height, Distance, Look ahead, Follow speed, Field of view from 50 to 110 degrees) plus a Reset Camera button (disabled when already at defaults). Storage round-trips through the existing `viberacer.controls` key with a backfill default for legacy payloads (FOV backfills to 70 when reading a pre-FOV camera object so the player keeps the rest of their tweaks). Tests: `tests/unit/controlSettings.test.ts` covers default-in-range (now including FOV bounds), schema rejection of out-of-range stored values (including FOV out-of-range), legacy backfill (including the FOV-omitted camera payload case), round-trips at the FOV slider extremes (50 / 70 / 90 / 110), the `cameraLerpsFor` mapping (legacy match at 1.0, linear scaling, clamping, range guarantee), and round-trip persistence.
- Camera presets: live. `src/lib/cameraPresets.ts` defines four named snapshots (`chase`, `hood`, `cinematic`, `low`) plus `CAMERA_PRESET_LABELS`, `CAMERA_PRESET_DESCRIPTIONS`, `getCameraPreset(name)` (defensive copy + clamp to the slider bounds), `matchCameraPreset(camera)` (returns the preset name whose values exactly match the current settings, else `null` for "Custom"), and `isCameraPresetName(value)` for type guards. The `chase` preset reuses `DEFAULT_CAMERA_SETTINGS` verbatim so picking it is a no-op for users on the legacy view; the other three (`hood` = low close bumper cam with wide FOV, `cinematic` = high far slow follow with narrow FOV, `low` = low tight aggressive chase) cover the obvious arcade-racer moods without exploding into a tunable color picker. Settings UI: a swatch grid sits at the top of the Camera section in `SettingsPane.tsx` (above the existing five sliders); each swatch renders a tiny SVG side-on diagram with a road line, an orange car block, a blue camera dot at the preset's `(distance, height)`, and a dashed sight cone whose spread tracks the preset's FOV so the preset's shape reads at a glance. Selection highlight reuses the same orange ring as the time-of-day and paint swatches. Below the swatches a small status row reads "Preset: <name>" or "Preset: Custom" so the player knows whether the sliders match a preset or have drifted off. Picking a preset writes its full snapshot into `settings.camera` through the existing `setCamera` flow, so the live game reskins on the next frame without rebuilding the renderer; the existing Reset Camera button is unaffected. Tests: `tests/unit/cameraPresets.test.ts` (15 tests covering name uniqueness, label + description presence, default-preset membership, chase-matches-default, every preset value sitting inside the slider bounds, fresh-object isolation, defensive fallback for unknown names, hood / cinematic shape invariants, `matchCameraPreset` round-trip on every preset, single-slider drift returning null, and the `isCameraPresetName` guard).
- Time-of-day lighting presets: live. `src/lib/lighting.ts` defines four presets (`noon`, `morning`, `sunset`, `night`) plus `getLightingPreset(name)`, `TimeOfDaySchema`, friendly labels, descriptions, and `SUN_DISTANCE`. The `noon` preset matches the legacy hardcoded scene exactly (sky `0x9ad8ff`, ground `0x6fb26f`, ambient `0xffffff @ 0.55`, sun `0xffffff @ 0.9` from `(80, 160, 60)`) so users who never open Settings see no change. `ControlSettings` gained a `timeOfDay: TimeOfDay` field persisted in the existing `viberacer.controls` localStorage key with backfill default for legacy payloads. `SceneBundle.setTimeOfDay(name)` is exposed by `buildScene` in `src/game/sceneBuilder.ts`: it captures references to the sky `Color`, the `groundMat`, the ambient and directional lights, then mutates them in place when called. The unit `sunDirection` is multiplied by `SUN_DISTANCE = 200` so the `DirectionalLight` sits well outside the scene at the right angle. `RaceCanvas.tsx` gained a `timeOfDayRef` prop polled each frame through a `syncTimeOfDay` helper that short-circuits on no-op (string equality on the last applied name) and calls the setter only when the value changes. `Game.tsx` and `TuningSession.tsx` mirror `settings.timeOfDay` into the ref each render so a swatch click in Settings reskins the scene on the next frame without rebuilding the renderer. Settings UI: a new Time-of-day section in `SettingsPane.tsx` (right under Car paint) renders a swatch grid where each chip is a half-sky / half-ground gradient with the preset's actual hex colors so the swatch reads as a horizon at a glance; the chip's `title` and `aria-label` carry the description. Tests: `tests/unit/lighting.test.ts` (17 tests covering name uniqueness, label / description presence, schema accept / reject, the `isTimeOfDay` guard, preset bounds, the legacy noon match, unit-length sun directions, morning-east / sunset-west invariants, the night-vs-noon dimness check, defensive fallback for unknown names, and fresh-object isolation). `tests/unit/controlSettings.test.ts` adds default `'noon'`, round-trip of a non-default choice (`'sunset'`), legacy backfill when the key is omitted, and reject-and-default on an unknown stored value.
- Per-track author mood: live. Track authors can bake a baseline scene mood (timeOfDay and / or weather) into a saved track version through the editor's Advanced panel; every player who races that version sees the author's intended look unless they turn off the new "Respect track mood" Settings toggle. Schema additions: `TrackMoodSchema` (`{ timeOfDay?, weather? }`, strict to reject typos) plus optional `mood` fields on `TrackSchema` and `TrackVersionSchema`. The mood is NOT included in the version hash (`hashTrack` continues to read only pieces and checkpointCount), so adding or changing the mood on an existing track keeps every prior leaderboard entry intact and does not invalidate `?v=<hash>` deep links. Pure helpers in `src/game/trackMood.ts` (`resolveActiveMood({trackMood, playerTimeOfDay, playerWeather, respectTrackMood})` returns the active `{timeOfDay, weather}` after composing the author's preference with the player's fallback; `trackHasMood(mood)` for the pause-menu indicator; `sanitizeTrackMood(mood)` strips malformed fields and returns null when the result is empty so the API layer never persists `mood: {}` into KV). `loadTrack` now returns `mood` alongside `pieces` / `checkpointCount` / `versionHash`. `Game.tsx` accepts a new `trackMood` prop, runs it through `resolveActiveMood` at render time, mirrors the result into the existing `timeOfDayRef` / `weatherRef` so the renderer skins the scene on the next frame without rebuilding anything, and surfaces a small cyan "Track mood: <label>" chip in the pause menu when the author baked in at least one field AND the player has the respect toggle on, so the player understands why the scene looks different from their own picks. Editor UI: the Advanced panel gains a "Track mood" row with two dropdowns (Time of day / Weather) each with a "Player pick" sentinel for "no override" plus a "Clear mood" button; the same amber dot the checkpoint override uses now lights up on the closed Advanced button when a mood is set. The PUT route runs `sanitizeTrackMood` on the incoming body and only persists `mood` when at least one field is set. Settings UI: a `Respect track mood` toggle sits under the Weather section (default on, persisted via `viberacer.controls` with a backfill default for legacy payloads). Tuning Lab keeps using the player's own picks (no track mood there). Tests: `tests/unit/trackMood.test.ts` (24 tests covering `resolveActiveMood` fallback / override / both-set / respect-off / malformed-field / fresh-object / non-default-player paths, `trackHasMood` null / undefined / empty / partial / both / malformed cases, and `sanitizeTrackMood` null / empty / malformed / partial / both / fresh-object cases). `tests/unit/schemas.test.ts` adds 12 tests covering `TrackMoodSchema` round-trip and unknown-field rejection plus `TrackSchema` / `TrackVersionSchema` accept / reject paths for the new optional `mood` field. `tests/unit/api.track.test.ts` adds 2 tests covering the full PUT-then-GET mood round-trip (asserting the version hash is unchanged) plus the empty-mood drop. `tests/unit/controlSettings.test.ts` adds default-true, round-trip-disabled, and legacy-backfill coverage for the new `respectTrackMood` field.
- Rainy weather with falling particles: live. `WEATHER_NAMES` extended with `'rainy'`, joining the existing `clear` / `cloudy` / `foggy` swatches in the Settings Weather section and in the per-track-author mood picker. The preset (`fogDensity 0.018`, `fogColor 0x8a909a`, `skyTintMix 0.6`, `ambientMultiplier 1.1`, `sunMultiplier 0.55`) lands between cloudy and foggy on density so distant track features still read but the sky reads as heavy overcast. The visible payoff is the new rain particle layer: pure helpers in `src/game/rain.ts` (`makeRainRng` Mulberry32-style 32-bit RNG so spawn positions are deterministic per seed, `initRainParticles(count, rng, config)` uniformly distributes particles inside the rain box on first paint so the player sees a steady downpour from the first frame, `tickRainParticles(particles, dtSec, rng, config)` advances every particle by `fallSpeed * dt` downward plus `windX/windZ * dt` horizontally and wraps to the top when a particle falls below the floor or to the opposite side when wind drifts past the box, `writeRainGeometry(particles, fx, fy, fz, streakLength, positions)` writes 6 floats per particle into a shared `Float32Array` so the renderer streams the buffer instead of allocating per frame, plus `DEFAULT_RAIN_CONFIG` and the `RAIN_*` tunables for box dimensions, fall speed, wind, color, opacity, and streak length). Renderer side is `buildRainLayer(particleCount?)` in `src/game/sceneBuilder.ts`: one `LineSegments` mesh with two vertices per streak through a single shared `BufferGeometry` (marked `DynamicDrawUsage` so Three.js streams via `bufferSubData`), one `LineBasicMaterial` (`RAIN_COLOR_HEX = 0xc8d6e6`, `RAIN_OPACITY = 0.55`, `transparent`, `depthWrite: false`), `tick(dtSec, fx, fy, fz)` short-circuits when the layer is hidden so dry weather costs zero per frame, `setVisible(value)` flips the parent group's `visible` flag, `reset()` re-seeds the spawn distribution so a Restart starts on a fresh box. `applyTimeAndWeather` in `buildScene` flips rain visibility based on `activeWeather === 'rainy'`, so picking the swatch in Settings (or baking `mood: { weather: 'rainy' }` into a track) lights up the layer on the next frame without rebuilding any geometry. `RaceCanvas`'s rAF loop calls `bundle.rain.tick(dtMs / 1000, camera.x, camera.y, camera.z)` right before `renderer.render` so the rain box drifts with the camera and the streaks never feel like they lag behind the road; the existing `dtMs` clamp at 50 ms / frame applies so a tab pause does not teleport every drop on resume. The full reset path also calls `bundle.rain.reset()` so a fresh race never carries forward an unwrapped tail. Tests: `tests/unit/rain.test.ts` (28 tests covering constant bounds, RNG determinism / range / seed-independence / zero-seed defense, `initRainParticles` count + bounds + determinism + zero-or-negative short-circuit, `tickRainParticles` fall step, empty-pool / non-positive-dt / non-finite-dt no-ops, floor wrap, X / Z horizontal wraps, return-same-array invariant, and `writeRainGeometry` 6-floats-per-particle layout, follow point addition, too-small-buffer throw, return-same-buffer invariant, empty-pool no-op). `tests/unit/weather.test.ts` adds 2 tests covering rainy density-vs-cloudy + sun-dim-vs-cloudy and rainy sky-tint-vs-clear so the preset stays distinct from its neighbors going forward.
- Snowy weather with falling snowflake particles: live. `WEATHER_NAMES` extended with `'snowy'`, joining the existing `clear` / `cloudy` / `foggy` / `rainy` swatches in the Settings Weather section and in the per-track-author mood picker. The preset (`fogDensity 0.014`, `fogColor 0xd6dde6`, `skyTintMix 0.55`, `ambientMultiplier 1.2`, `sunMultiplier 0.7`) sits between cloudy and foggy on density, uses a cold pale grey-blue fog tint instead of the rainy slate grey, and lifts ambient extra so the white flakes pop against the road. The visible payoff is the new snow particle layer: pure helpers in `src/game/snow.ts` (`makeSnowRng` Mulberry32-style 32-bit RNG so spawn positions are deterministic per seed, `initSnowParticles(count, rng, config)` uniformly distributes particles inside the snow box on first paint plus seeds a per-particle `phase` (radians) and `freqScale` in `[0.5, 1.5)` so adjacent flakes drift out-of-phase, `tickSnowParticles(particles, dtSec, nowSec, rng, config)` advances every particle by `fallSpeed * dt` downward plus `windX/windZ * dt` horizontally and wraps to the top when a particle falls below the floor (also re-rolling its phase + freqScale so the flurry stays visually noisy) or to the opposite side when wind drifts past the box, `snowSwayOffset(particle, nowSec, config)` is the per-particle horizontal sinusoid that gives the flakes their natural drift, `writeSnowGeometry(particles, fx, fy, fz, nowSec, positions, config)` writes 3 floats per particle into a shared `Float32Array` so the renderer streams the buffer instead of allocating per frame and applies the X sway inline so the sin call stays in one place, `buildSnowflakeSprite(size)` builds an alpha-feathered RGBA byte array used as the Points texture so each flake reads as a soft round dot instead of the default 1px square, plus `DEFAULT_SNOW_CONFIG` and the `SNOW_*` tunables for box dimensions, fall speed, sway, wind, color, opacity, and point size). Renderer side is `buildSnowLayer(particleCount?)` in `src/game/sceneBuilder.ts`: one `Points` mesh with one vertex per particle through a single shared `BufferGeometry` (marked `DynamicDrawUsage` so Three.js streams via `bufferSubData`), one `PointsMaterial` (`SNOW_COLOR_HEX = 0xffffff`, `SNOW_OPACITY = 0.85`, `SNOW_POINT_SIZE = 0.45`, `sizeAttenuation: true` so distant flakes shrink, `transparent`, `depthWrite: false`, `blending: AdditiveBlending` so the flakes glow against asphalt without turning into bright squares against the lighter overcast sky) plus a procedurally-generated 32x32 alpha-feathered `DataTexture` so no binary asset ships. `tick(dtSec, nowSec, fx, fy, fz)` short-circuits when the layer is hidden so non-snow weather costs zero per frame. `setVisible(value)` flips the parent group's `visible` flag, `reset()` re-seeds the spawn distribution + phase + freqScale so a Restart starts on a fresh box. `applyTimeAndWeather` in `buildScene` flips snow visibility based on `activeWeather === 'snowy'`, so picking the swatch in Settings (or baking `mood: { weather: 'snowy' }` into a track) lights up the layer on the next frame without rebuilding any geometry. `RaceCanvas`'s rAF loop calls `bundle.snow.tick(dtMs / 1000, ts / 1000, camera.x, camera.y, camera.z)` right before `renderer.render` so the snow box drifts with the camera and the sway phase advances naturally with wall-clock time; the existing `dtMs` clamp at 50 ms / frame applies so a tab pause does not teleport every flake on resume. The full reset path also calls `bundle.snow.reset()` so a fresh race never carries forward an unwrapped tail. Tests: `tests/unit/snow.test.ts` (40+ tests covering constant bounds, fall-slower-than-rain regression guard, RNG determinism / range / seed-independence / zero-seed defense, `initSnowParticles` count + bounds + phase-and-freqScale ranges + determinism + zero-or-negative short-circuit, `tickSnowParticles` fall step, empty-pool / non-positive-dt / non-finite-dt / non-finite-nowSec no-ops, floor wrap with phase + freqScale refresh, X / Z horizontal wraps, return-same-array invariant, `snowSwayOffset` zero / range / determinism / non-finite defense / 180-degree-phase-mirror, `writeSnowGeometry` 3-floats-per-particle layout, follow point addition, sway application, too-small-buffer throw, return-same-buffer invariant, empty-pool no-op, non-finite nowSec defense, plus `buildSnowflakeSprite` size validation, white RGB, fully-opaque center, transparent corner, monotonic alpha falloff, and alpha byte-range invariants). `tests/unit/weather.test.ts` adds 4 tests covering snowy density-vs-cloudy + sun-dim-vs-cloudy, snowy ambient-lift-vs-cloudy, snowy density-less-than-foggy, and snowy sky-tint vs clear plus paler-fog-color-than-rainy so the preset stays distinct from its neighbors going forward.
- Weather presets: live. `src/lib/weather.ts` defines three presets (`clear`, `cloudy`, `foggy`) plus `getWeatherPreset(name)`, `WeatherSchema`, friendly labels, descriptions, `mixColorHex`, and the `isWeather` guard. The `clear` preset is a true no-op (zero `FogExp2` density, zero sky tint mix, identity ambient and sun multipliers) so users who never open Settings see no change. `ControlSettings` gained a `weather: Weather` field persisted in the existing `viberacer.controls` localStorage key with backfill default for legacy payloads. Composes with the time-of-day skin: time-of-day picks the base sky color, ambient color, sun direction, and base intensities; weather mixes the sky toward the fog color, scales ambient and sun intensities, and sets `FogExp2.density` plus `fog.color`. `SceneBundle.setWeather(name)` is exposed by `buildScene` in `src/game/sceneBuilder.ts`: it stores the active name, then a shared `applyTimeAndWeather()` helper rewrites every output value from the latest pair so the two settings never get out of sync. `RaceCanvas.tsx` gained a `weatherRef` prop polled each frame through a `syncWeather` helper that short-circuits on no-op (string equality on the last applied name). `Game.tsx` and `TuningSession.tsx` mirror `settings.weather` into the ref each render so a swatch click in Settings reskins the scene on the next frame without rebuilding the renderer. Settings UI: a new Weather section in `SettingsPane.tsx` (right under Time-of-day) renders a swatch grid where each chip shows a sky-over-ground horizon with a small road silhouette and a fog overlay whose opacity scales with the preset's density so the chips read in order at a glance; the chip's `title` and `aria-label` carry the description. Tests: `tests/unit/weather.test.ts` (23 tests covering name uniqueness, label / description presence, schema accept / reject, the `isWeather` guard, preset bounds, the `clear`-is-no-op invariant, density / sky-tint ordering across presets, sun-dim and ambient-lift invariants, defensive fallback for unknown names, fresh-object isolation, and the `mixColorHex` helper at endpoints / midpoint / clamp / identity / 24-bit-bounds). `tests/unit/controlSettings.test.ts` adds default `'clear'`, round-trip of a non-default choice (`'foggy'`), legacy backfill when the key is omitted, and reject-and-default on an unknown stored value.
- Time-of-day auto cycle: live. New Settings option that rotates the active scene through noon, morning, sunset, and night while the player races, in the spirit of Forza Horizon's day-night cycle. Pure helpers in `src/lib/timeOfDayCycle.ts` (`TIME_OF_DAY_CYCLE_MODES = ['off','slow','fast']` mirror the existing per-mode picker shape, `DEFAULT_TIME_OF_DAY_CYCLE = 'off'` so legacy stored payloads keep their existing screen exactly as it was, `TimeOfDayCycleModeSchema` zod enum, `TIME_OF_DAY_CYCLE_LABELS` / `TIME_OF_DAY_CYCLE_DESCRIPTIONS`, the `isTimeOfDayCycleMode` guard, `TIME_OF_DAY_CYCLE_PERIOD_MS` table (`off: 0`, `slow: 5 * 60 * 1000` so a long session sees variety without flickering, `fast: 60 * 1000` so a single race sees one or two transitions), `TIME_OF_DAY_CYCLE_ORDER = ['noon','morning','sunset','night']` the rotation sequence kept separate from `TIME_OF_DAY_NAMES` so a future schema reorder cannot accidentally scramble the cycle, `cycleIndexFor(name)` index lookup with a fall back to noon for unknown values, and `activeTimeOfDayAt(startMs, nowMs, mode, base)` the pure resolver that returns the active TimeOfDay at a given wall-clock instant from a given starting preset, defensive against `'off'` short-circuit / non-positive period / non-finite startMs / non-finite nowMs / reversed clock so a pathological input cannot trap the renderer). `ControlSettings` gained a `timeOfDayCycle: TimeOfDayCycleMode` field (default `'off'`), persisted in the existing `viberacer.controls` localStorage key with a backfill default for legacy payloads. `Game.tsx` introduces a `cycleTimeOfDayRef` and an `effectiveTimeOfDay` derived value that reads from the ref when the cycle is on (and is not suppressed by a track-author baked time-of-day) and from `activeMood.timeOfDay` otherwise; the existing `timeOfDayRef` (and the headlights resolution) are now keyed off `effectiveTimeOfDay` so a cycle into night automatically lights the lamps. A new `useEffect` keyed on `[settings.timeOfDayCycle, trackMoodLocksTimeOfDay, baseTimeOfDayForCycle]` sets up a wall-clock `setInterval` at the picked period: each tick computes the next TimeOfDay via `activeTimeOfDayAt` and only mutates the ref + bumps a local `cycleTick` state when the value actually changes, so the headlights effect picks up the new sky on the same render the renderer's `syncTimeOfDay` poll reskins the scene. The cycle is suppressed automatically when the player has "respect track mood" on AND the track author baked a time-of-day, since the author's pick is the entire point of that toggle; a flip back to `'off'` snaps the cycle ref to the base pick on the next render so the scene lands on the player's chosen sky without stranding mid-rotation. Settings UI: the Time of day section in `SettingsPane.tsx` gains a three-button picker (Off / Slow / Fast) right under the existing swatch grid, plus a sub-hint that swaps to whichever mode is active so the player always sees plain-language confirmation of what the cycle will do. Tests: `tests/unit/timeOfDayCycle.test.ts` (31 tests covering mode-enum membership / stable order / no em-dashes / unique labels, period-table positivity / `'off' === 0` / `'fast' < 'slow'` / `'slow' >= 60s` invariants, rotation-order coverage of every preset / starts-at-noon / mirrors-a-real-day, `TimeOfDayCycleModeSchema` accept-all-three / reject `'instant'` / `''` / `'OFF'` / non-string, `isTimeOfDayCycleMode` mirror coverage, `cycleIndexFor` per-preset lookup + null/undefined fallback, plus `activeTimeOfDayAt` `'off'`-passthrough / start-at-base / fast-step-from-noon every-step / slow-step-from-non-noon / period-interior-holds / reversed-clock-clamp / non-finite-defenses / determinism / full-rotation-returns-to-base / every-step-yields-valid-name). `tests/unit/controlSettings.test.ts` adds 5 tests covering the default `'off'`, round-trip of `'slow'` and `'fast'`, legacy backfill (omitted in stored payload), and reject-and-default on an unknown stored value.
- **Not yet landed.** Quaternion slerp for orientation smoothing (the target-lerp approximates this well enough for the vertical slice).

---

## 4. Controls

**Status.** Partial. Keyboard (WASD + arrows + Space, all remappable) plus Esc-to-pause are live. Touch controls support both a dual-stick layout and a single-stick layout, switchable in Settings. Gamepad support (Standard layout: triggers for gas / brake, left stick for steering, RB for handbrake, Start to pause) is live, with discrete buttons (forward / brake / handbrake / pause) now remappable from Settings. The reserved Q/E shift keys are still pending.

### Build log

- `src/hooks/useKeyboard.ts` returns `{ current: KeyInput }` (ref-like) where `KeyInput = { forward, backward, left, right, handbrake }`. `keydown`/`keyup` listeners mutate the ref and `preventDefault` for recognized keys. The hook now takes a `KeyBindings` argument (with `DEFAULT_KEY_BINDINGS` as a fallback) and reads it through a ref so changing bindings at runtime does not require rebinding listeners.
- Default mapping: `W`/`ArrowUp` = forward, `S`/`ArrowDown` = backward (brake/reverse), `A`/`ArrowLeft` = steer left, `D`/`ArrowRight` = steer right, `Space` = handbrake.
- The tick loop reads `keys.current` each frame and synthesizes `{ throttle, steer, handbrake }` for `stepPhysics`.
- Esc pause: handled in `Game.tsx` via a window `keydown` listener that is gated on `phase === 'racing'`. First press calls `pause()`, second press calls `resume()`. See Section 9 for the pause lifecycle.
- Touch: `src/game/virtual-joystick.ts` exports pure stick state helpers (`createJoystick`, `beginJoystick`, `moveJoystick`, `endJoystick`, `readJoystick`). `src/hooks/useTouchControls.ts` wires pointer events to one or two sticks depending on `touchMode` and writes booleans into the same `KeyInput` ref the keyboard hook owns so the game loop stays single-source.
  - Dual mode: left half spawns a steering stick, right half spawns a gas/brake stick. Both release on pointerup and respawn at the next tap.
  - Single mode: any touch anywhere spawns one stick. X axis steers, Y axis is gas (up) / brake (down). Only one ring renders.
- `src/components/TouchControls.tsx` takes the same `mode` and renders one or two visual rings + knobs while active. Game container uses `touch-action: none` so the browser does not steal pan/zoom.
- Settings storage: `src/lib/controlSettings.ts` defines `ControlSettings = { keyBindings, touchMode }`, defaults, schema validation, and pure helpers (`actionForCode`, `rebindKey`, `clearBinding`, `formatKeyCode`). Persisted to `localStorage` under `viberacer.controls`. `src/hooks/useControlSettings.ts` exposes `{ settings, setSettings, resetSettings, hydrated }` and listens to the `storage` event so a tweak in one tab reaches the live game in another.
- Settings UI: `src/components/SettingsPane.tsx` is a modal pane. The keyboard section detects `(any-pointer: fine)`; the touch section detects `(any-pointer: coarse)` (with `navigator.maxTouchPoints` fallback). Each action shows two slot buttons. Click a slot then press a key to bind. Reassigning a code that was already bound elsewhere transfers it. Reset to defaults is one click. Esc cancels capture.
- Wiring: `src/components/SettingsLauncher.tsx` exposes the Settings button on the home page (`src/app/page.tsx`). The pause menu (`src/components/PauseMenu.tsx`) gains a Settings entry that switches `pauseView` to `'settings'` in `Game.tsx`.
- Tests: `tests/unit/controlSettings.test.ts` covers default-binding lookup, rebind transfers across actions, immutability of inputs, slot growth past current length, clear bounds, key-code formatting, and the localStorage round-trip (defaults on missing or malformed payloads).
- Initials editing: live. The Settings pane (`SettingsPane`) renders an "Identity" section with the current initials in an inline 3-letter input (uppercase A-Z, same `InitialsSchema` validation as the first-visit prompt). Saving calls `writeStoredInitials(value)` from `src/lib/initials.ts`, which writes the localStorage key `viberacer.initials` and dispatches a `viberacer:initials-changed` CustomEvent. `Game` subscribes to that event plus the cross-tab `storage` event so the HUD's RACER block reflects the new tag on the next frame without restarting the race. Mid-race edits affect future laps only; historical leaderboard entries keep their old tag (per `§7` and `§11`).
- Gamepad: live. `src/game/gamepadInput.ts` is a pure helper that maps the W3C Standard Gamepad layout to a `{axes:{steer,throttle}, keys:{...}, pausePressed}` payload (right trigger = forward, left trigger = brake / reverse, A / B as analog-trigger fallback, left stick X with deadzone for steering, dpad left / right overrides the stick, RB or X for handbrake, Start emits pause on the rising edge). `src/hooks/useGamepad.ts` polls `navigator.getGamepads()` on rAF, treats the first connected pad as active, and writes both the analog axes and the boolean keys onto the same `KeyInput` ref the keyboard owns. When the pad is idle (zero deflection, no buttons), it blanks `keys.current.axes` so a connected-but-unused controller does not lock out arrow keys. `KeyInput` gained an optional `axes` field; `RaceCanvas` reads it each frame and prefers it over the boolean derivation when set, which feeds analog values straight into `stepPhysics`. SFX (`steerAbs`, `throttle`) read from the same combined inputs so engine drone, skid, and rumble cues react to trigger pressure rather than just on / off. Last-input-wins now flips `inputModeRef` to `'gamepad'` whenever the analog axes are populated, so the leaderboard's input-mode badge surfaces a small controller icon (alongside keyboard / touch). `InputModeSchema` extended to `z.enum(['keyboard','touch','gamepad'])`. The Settings pane gained a Gamepad section under Controls that shows whether a controller is detected (live `gamepadconnected` listener plus a 1.5s poll) and explains the bindings. Tuning Lab also calls `useGamepad` so the practice loop benefits.
- Gamepad-binding remap UI: live. `ControlSettings` gained a `gamepadBindings: Record<'forward'|'backward'|'handbrake'|'pause', number[]>` field with `DEFAULT_GAMEPAD_BINDINGS = { forward: [7, 0], backward: [6, 1], handbrake: [5, 2], pause: [9] }` (W3C Standard Gamepad indices). Helpers `rebindGamepadButton`, `clearGamepadBinding`, `gamepadActionForIndex`, `formatGamepadButton`, and `cloneDefaultGamepadBindings` mirror the keyboard pattern in `src/lib/controlSettings.ts`. `gamepadToInput(snapshot, prevPause, bindings)` now reads its action -> button-index lists from the bindings argument; for each action it takes the maximum analog `value` across all bound buttons so a fully-pulled trigger and a digital face button feel identical at the physics layer. New `pauseHeld(snapshot, bindings)` exports a "any pause-bound button is currently down" boolean that `useGamepad` uses for rising-edge detection (so rebinding pause to Select still works). The Settings pane's Gamepad section adds a slot table per action (Accelerate / Brake / Handbrake / Pause, two slots each) styled to match the keyboard remap rows. Click a slot, then press a controller button: a small rAF poll watches `navigator.getGamepads()` and accepts the first fresh press (anything held at capture-start is parked until released so a still-pressed RT does not auto-bind the slot you just clicked). Esc cancels capture. Steering stays fixed (left stick + dpad 14/15) and is not user-rebindable today. Bindings round-trip through the existing `viberacer.controls` localStorage key with backfill defaults for legacy payloads. Tests: `tests/unit/controlSettings.test.ts` covers default values, deep clone immutability, rebind transfer + slot extension, clear logic, button-label formatting, storage round-trip, legacy backfill, and out-of-range rejection. `tests/unit/gamepadInput.test.ts` covers throttle / handbrake / pause routing through custom bindings, multi-bound max-analog logic, and the `pauseHeld` helper.
- Restart-lap action: live. New `'restartLap'` entry in `CONTROL_ACTIONS` (default binding `KeyR`, remappable in Settings) lets the player abandon the in-flight lap and restart at the spawn line without sitting through the READY-SET-GO countdown. `CONTINUOUS_CONTROL_ACTIONS` (the five driving actions) plus `isContinuousAction(action)` split held-down actions from one-shots; `useKeyboard` skips one-shots so they never pollute `KeyInput`. `Game.tsx` runs its own keydown listener that fires `restartLap()` on the rising edge (`e.repeat` ignored, editable targets ignored, listener bypassed during countdown / pause). `RaceCanvas.tsx` exposes a new optional `pendingLapResetRef` ref that the rAF loop drains by teleporting the car back to spawn, zeroing `nextCpId` and `hits`, dropping the recording buffer, clearing skid marks, and reseeding `state.raceStartMs = ts` so the lap timer immediately restarts. `lapCount`, `lastLapTimeMs`, the on-disk PB, lap history, the live overall record, and the toast lane are preserved. The Pause menu gains a Restart Lap entry above Restart that calls `resume()` first then pulses the same ref so the loop processes it on the next frame. The pause-menu path also drops the pending replay buffer so an abandoned partial lap never gets posted to the leaderboard. Tests: `tests/unit/controlSettings.test.ts` covers the default `KeyR` binding, the `isContinuousAction` classifier (forward / backward / left / right / handbrake = continuous, restartLap = one-shot), legacy-payload backfill (pre-restartLap stored bindings get the default R), round-trip of a remapped binding, and clearing the slot.
- **Not yet landed.** Q/E shifter keys.

### Keyboard (defaults, remappable in Settings)

| Action          | Keys                  |
| --------------- | --------------------- |
| Accelerate      | `W` or `Up`           |
| Brake / reverse | `S` or `Down`         |
| Steer left      | `A` or `Left`         |
| Steer right     | `D` or `Right`        |
| Handbrake       | `Space`               |
| Restart lap     | `R`                   |
| Shift up        | `E` (reserved)        |
| Shift down      | `Q` (reserved)        |
| Pause           | `Esc` (or gamepad Start) |

Manual gearing is a stretch feature. Default car is automatic.

### Mobile touch: floats where you tap

Virtual joysticks with no fixed positions. Two layouts, switchable in Settings.

**Single stick (default):**
- Any touch spawns one stick anywhere. Horizontal axis steers, vertical axis is gas (up) / brake (down). Releases on touchend and respawns at the next touch point.

**Dual stick:**
- **Left half of screen.** First touch spawns a steering stick at that point. Horizontal axis steers.
- **Right half of screen.** First touch spawns a gas/brake stick. Up = accelerate, down = brake.
- Both sticks release on touchend and respawn at the next touch point.

Pause button floats in a corner and is always tappable during a race.

---

## 5. Vehicle

**Status.** Partial. Arcade integrator with off-track drag ships in `src/game/physics.ts`. Car renders using the Kenney Car Kit `race.glb` loaded via `GLTFLoader`. Per-wheel raycast and angular velocity are not yet landed.

### Build log

- `src/game/physics.ts` exports `stepPhysics(state, input, dtSec, onTrack, params?)`, `DEFAULT_CAR_PARAMS`, and `PhysicsState` / `PhysicsInput` / `CarParams` types.
- Simplified arcade model: scalar speed + heading, no lateral velocity. Throttle adds `accel * dt`; reverse throttle brakes forward motion first, then accelerates backward at `reverseAccel`. Coasting decays at `rollingFriction`. Handbrake adds a drag proportional to `brake * 1.5`. Off-track applies `offTrackDrag` and caps at `offTrackMaxSpeed`. Steering multiplies by `sign(speed)` so reverse steers naturally.
- Defaults: `maxSpeed=26`, `maxReverseSpeed=8`, `accel=18`, `brake=36`, `reverseAccel=12`, `rollingFriction=4`, `steerRateLow=2.2`, `steerRateHigh=2.2`, `minSpeedForSteering=0.8`, `offTrackMaxSpeed=10`, `offTrackDrag=16`. Steering rate lerps between the low and high values across `[minSpeedForSteering, maxSpeed]`, so a low high-speed value tames twitchiness on straights while keeping low-speed agility.
- On-track detection: `distanceToCenterline(op, x, z) <= TRACK_WIDTH/2` in `tick.ts`. Centerlines: straights use segment distance; corners use `|hypot(x - cx, z - cz) - CELL_SIZE/2|` with `arcCenter` cached on each `OrderedPiece` at build time.
- Car visual: `src/game/sceneBuilder.ts::buildCar` returns an outer Group with an inner Group containing the Kenney `race.glb` scene. The inner Group applies `rotation.y = PI/2` (model's local +Z forward remapped to world +X, matching physics heading 0) and a uniform `scale = 1.65` so the ~2.56-unit model matches the prior 4.2-length footprint. Asset served from `public/models/car.glb` (CC0, Kenney Car Kit v3.1). License note in `public/models/KENNEY-LICENSE.txt`. Orientation via `car.rotation.y = state.heading` on the outer Group.
- Car paint customization: live. `src/lib/carPaint.ts` defines an 8-swatch palette (Crimson, Tangerine, Mustard, Lime, Teal, Cobalt, Orchid, Midnight) plus the stock colormap. `ControlSettings` gained a `carPaint: string | null` field (`null` = stock) persisted in the existing `viberacer.controls` localStorage key with the legacy backfill default. `buildCar` finds the GLB's "body" mesh node, clones a fresh `MeshStandardMaterial` for it, and toggles between the original colormap material and a per-paint replacement so wheels stay dark while the body recolors. The setter is exposed through `SceneBundle.setCarPaint` and is buffered until the GLB resolves so calls before load complete do not lose the chosen color. `RaceCanvas` reads a new optional `carPaintRef` each frame and reapplies on change, so a swatch click in the pause menu's Settings pane repaints the car on the next frame without rebuilding the renderer. Settings UI: a new Car paint section in `SettingsPane` renders a swatch grid with a Stock chip (rendered as a checker pattern) plus one button per palette entry. Tests: `tests/unit/carPaint.test.ts` covers palette uniqueness, hex schema validation (case-insensitive lowercase coercion, malformed rejection), null-vs-hex setting union, and `hexToColorInt` parsing. `tests/unit/controlSettings.test.ts` covers the default null value, lowercase round-trip, legacy backfill, and full-payload rejection on a malformed hex.
- Racing number plate decal: live. Pure helpers in `src/lib/racingNumber.ts` (`RACING_NUMBER_*` constants, two curated swatch palettes (`RACING_NUMBER_PLATE_COLORS` 6 entries, `RACING_NUMBER_TEXT_COLORS` 4 entries), `DEFAULT_RACING_NUMBER` (`enabled: false`, `value: '7'`, `plateHex: '#ffffff'`, `textHex: '#1a1a1a'`), `RacingNumberSettingSchema` zod validation (1-2 digit value, lowercase 7-char hex strings, strict object shape), the `findPlateColor` / `findTextColor` palette lookups (case-insensitive, null on miss), `sanitizeRacingNumber(raw)` which strips non-digits / caps at the documented max length / falls back to the default on empty input or non-string, `racingNumberHexToColorInt(hex)` for Three.js color ints, `racingNumberFontSizePx(value, canvasSizePx)` which scales single-digit numbers larger so '7' and '77' read at the same visual weight, and `drawRacingNumberToCanvas(ctx, size, value, plateHex, textHex)` which fills the plate, lays four black border bars, and draws the centered number through the standard 2D Canvas API). Renderer side is `buildRacingNumberPlate()` in `src/game/sceneBuilder.ts`: a `Group` attached to the OUTER car group (so it inherits heading rotation but not the inner-group's yaw / scale offsets), holding a single `PlaneGeometry` (`RACING_NUMBER_PLATE_SIZE = 1.6`) lifted to `RACING_NUMBER_PLATE_HEIGHT_Y = 1.55` above the chassis pivot and laid flat (`rotation.x = -PI/2`, `rotation.z = -PI/2` so the texture's +X aligns with the car's forward and the number reads upright from the trailing chase camera). The plate uses a `MeshBasicMaterial` (no lighting math, the texture already encodes the look) backed by a `CanvasTexture` over a 256-px square 2D canvas. The setter (`SceneBundle.setRacingNumber`) caches the last applied (value, plateHex, textHex) tuple as a single string key so a no-op call (the rAF loop's poll-and-set on every frame) is a single string compare instead of a canvas redraw + GPU upload; only when the tuple changes does it call `drawRacingNumberToCanvas` and flip `texture.needsUpdate = true`. `enabled: false` flips `group.visible = false` and is otherwise a no-op so the disabled path costs zero per frame. SSR / no-canvas environments (Vitest's jsdom) detect the missing `document.createElement` and short-circuit the mesh creation entirely so the helper stays import-safe in unit tests. `ControlSettings` gained a `racingNumber: RacingNumberSetting` field with the documented default, persisted in the existing `viberacer.controls` localStorage key with backfill defaults for legacy payloads. `RaceCanvas.tsx` reads a new optional `racingNumberRef` each frame through a `syncRacingNumber` helper that short-circuits on reference equality on the cached settings object; `Game.tsx` and `TuningSession.tsx` mirror `settings.racingNumber` into the ref each render so a swatch click or number-input keystroke in Settings updates the plate on the next frame without rebuilding the renderer. Settings UI: a new Racing number section in `SettingsPane.tsx` (right under Car paint) renders a single Show plate `MenuToggle`; when enabled, a 2-character text input (`inputMode="numeric"`, `pattern="[0-9]*"`, `maxLength=2`) sits next to a 56x56 DOM live-preview chip showing the current plate / text colors and value, followed by two swatch grids (Plate color, Number color) reusing the existing `PaintSwatch` component, plus a "Reset plate" ghost button that restores the default. Tests: `tests/unit/racingNumber.test.ts` (38 tests covering palette uniqueness / lowercase hex invariants / non-empty / default-membership in both palettes, `DEFAULT_RACING_NUMBER` disabled-by-default + schema-roundtrip + value-matches-constant, `RacingNumberSettingSchema` accept-well-formed / accept-single-digit / lowercase-hex coercion / whitespace-trim / reject-3-digit / reject-non-digit / reject-empty / reject-malformed-plate-hex / reject-malformed-text-hex, `sanitizeRacingNumber` clean-passthrough / strip-non-digits / cap-at-max-length / empty-default-fallback / non-string-default-fallback, `findPlateColor` / `findTextColor` known-hex-match / case-insensitive / unknown-null / null-input-null, `racingNumberHexToColorInt` lowercase / uppercase / malformed-null, `racingNumberFontSizePx` single-vs-double / linear-with-canvas-size / sanitized-fallback / positive-integer, and `drawRacingNumberToCanvas` plate-then-borders-then-number / 4-border-bar-count / malformed-input-falls-back-to-default through a CanvasRenderingContext2D stub). `tests/unit/controlSettings.test.ts` adds 6 tests covering the default racingNumber object, `cloneDefaultSettings` returning an isolated racingNumber, round-trip of an enabled custom number, legacy-backfill (omitted in stored payload), reject-and-default on a malformed value, and reject-and-default on a malformed plate hex.
- Cosmetic headlights: live. Pure helpers in `src/lib/headlights.ts` (`HEADLIGHT_MODES = ['off','auto','on']`, `DEFAULT_HEADLIGHT_MODE = 'auto'`, `HeadlightModeSchema` zod enum, `HEADLIGHT_MODE_LABELS` / `HEADLIGHT_MODE_DESCRIPTIONS`, the `isHeadlightMode` guard, `shouldHeadlightsBeOn(mode, timeOfDay, weather)` resolver, plus the `HEADLIGHT_*` visual tunables for lamp / beam offsets, sizes, opacity, and the warm-white / warm-yellow color palette). The auto rule covers the cases where real headlights help: sunset and night for low ambient light, foggy / snowy / rainy for low visibility; morning and noon under clear / cloudy stay dark on auto. Renderer side is `buildHeadlights()` in `src/game/sceneBuilder.ts`: a single `Group` attached to the OUTER car group (so it inherits heading rotation but not the inner-group's GLB-orientation yaw / scale) holding two `SphereGeometry` "lamps" at the front of the chassis (offset along +X for forward, +Y for height, +/-Z for the lateral spread) plus two long translucent `ConeGeometry` "beam" cones rotated -PI/2 around Z so the cone axis aligns with +X (forward); each beam mesh is positioned half its length forward of the lamp so the apex sits at the lamp and the wide end sits `HEADLIGHT_BEAM_LENGTH = 6` units further along. Lamps share one `MeshBasicMaterial` (warm-white, no transparency, full alpha so the lamp pops on a bright body too); beams share one `MeshBasicMaterial` with `transparent: true`, `opacity: HEADLIGHT_BEAM_OPACITY = 0.18`, `depthWrite: false`, and `AdditiveBlending` so they glow against a dark night scene without needing tonemapping. Pure cosmetic; the assembly does not actually illuminate the road, never affects physics or anti-cheat. The setter (`SceneBundle.setHeadlights(on)`) just flips the parent group's `visible` flag (O(1)) and is exposed through `bundle.setHeadlights`. `RaceCanvas.tsx` reads a new optional `headlightsOnRef` each frame through a `syncHeadlights` helper that short-circuits on no-op (boolean equality on the cached value); `Game.tsx` and `TuningSession.tsx` mirror `shouldHeadlightsBeOn(settings.headlights, activeMood.timeOfDay, activeMood.weather)` into the ref each render so a swatch click in Settings, a time-of-day swap, or a weather flip lights or extinguishes the lamps on the next frame without rebuilding the renderer. `ControlSettings` gained a `headlights: HeadlightMode` field (default `'auto'`), persisted in the existing `viberacer.controls` localStorage key with a backfill default for legacy payloads. Settings UI: a new Headlights section in `SettingsPane.tsx` (right under Racing number, above Time of day) renders a three-button picker (Off / Auto / On) reusing the same `MenuButton` row pattern as the Ghost-car picker, plus a sub-hint that swaps to whichever option is active so the player always sees plain-language confirmation of what the lamps will do. Tests: `tests/unit/headlights.test.ts` (41 tests covering enum membership / stable order / non-empty labels and descriptions / no em-dashes invariant on every player-facing string, `DEFAULT_HEADLIGHT_MODE` membership, `HeadlightModeSchema` accept all three valid modes plus reject `'flicker'` / `''` / `'AUTO'` / `0` / `null` / `undefined` / `{}`, `isHeadlightMode` mirror coverage, `shouldHeadlightsBeOn` `'off'`-always-dark and `'on'`-always-lit invariants across every (timeOfDay, weather) pair, `'auto'` lighting at sunset / night under any weather, `'auto'` lighting in foggy / snowy / rainy at noon, `'auto'` staying dark at noon / morning under clear / cloudy, plus visual-constant invariants (positive finite offsets, lamp radius under 0.5, beam length at least 6 lamp widths so the cone reads as a beam, beam radius wider than a lamp, beam opacity strictly inside (0..1), and 24-bit-bounds for both color hexes). `tests/unit/controlSettings.test.ts` adds 5 tests covering the default `'auto'`, round-trip of `'on'` and `'off'`, legacy backfill (omitted in stored payload), and reject-and-default on an unknown stored value.
- Cosmetic brake lights: live. Pure helpers in `src/lib/brakeLights.ts` (`BRAKE_LIGHT_MODES = ['off','auto','on']`, `DEFAULT_BRAKE_LIGHT_MODE = 'auto'`, `BrakeLightModeSchema` zod enum, `BRAKE_LIGHT_MODE_LABELS` / `BRAKE_LIGHT_MODE_DESCRIPTIONS`, the `isBrakeLightMode` guard, `isBrakingNow(throttle, speed, handbrake)` per-frame predicate, `shouldBrakeLightsLight(mode, braking)` resolver, `BRAKE_LIGHT_MIN_FORWARD_SPEED = 0.5` floor so a player intentionally reversing onto the throttle does not flicker the lamps, plus the `BRAKE_LIGHT_*` visual tunables for lamp / glow offsets, sizes, opacity, and the bright red color palette). The auto rule fires whenever the player is actively slowing the car down: holding the handbrake at any speed, or pressing brake while moving forward. Coasting (zero throttle) and reverse-acceleration (negative throttle while already reversing) both stay dark, matching what a real car does so the visual cue reads honestly. Renderer side is `buildBrakeLights()` in `src/game/sceneBuilder.ts`: a single `Group` attached to the OUTER car group (so it inherits heading rotation but not the inner-group's GLB-orientation yaw / scale) holding two `SphereGeometry` "lamps" at the rear of the chassis (offset along -X for back, +Y for height, +/-Z for the lateral spread, mirroring the headlight layout but on the opposite face) plus two slightly larger `SphereGeometry` "glow" discs co-located with each lamp. Lamps share one `MeshBasicMaterial` (bright red `BRAKE_LIGHT_COLOR_HEX = 0xff2a2a`, no transparency, full alpha so the lens pops on a bright body too); glow discs share one `MeshBasicMaterial` with `transparent: true`, `opacity: BRAKE_LIGHT_GLOW_OPACITY = 0.55`, `depthWrite: false`, and `AdditiveBlending` so the halo glows against either a bright body or a dark night scene without needing tonemapping. Pure cosmetic; never affects physics or anti-cheat. The setter (`SceneBundle.setBrakeLights(on)`) just flips the parent group's `visible` flag (O(1)) and is exposed through `bundle.setBrakeLights`. `RaceCanvas.tsx` reads a new optional `brakeLightModeRef` (the player's mode pick) and computes the live boolean inside its rAF loop using `isBrakingNow(throttleInput, state.speed, k.handbrake)` against the freshest physics state, then routes the result through `shouldBrakeLightsLight(mode, braking)` and an `applyBrakeLights` helper that short-circuits on a cached boolean so the per-frame cost is one compare. Mirroring the mode (not a resolved boolean) into the ref keeps the source of truth in one place: the renderer reconciles the live driver input against the player's preference each frame, which means a Settings flip OR a brake press OR a handbrake release lands on the very next frame. `Game.tsx` and `TuningSession.tsx` mirror `settings.brakeLights` into the ref each render so the lab car visually matches the race car. Most visible from the rear-view mirror inset, where the player sees their own rear lamps glow each time they tap brake, and from chasers (the cyan ghost car keeps the lamps on the same model for visual continuity, but only the player's instance is wired to the live brake state). `ControlSettings` gained a `brakeLights: BrakeLightMode` field (default `'auto'`), persisted in the existing `viberacer.controls` localStorage key with a backfill default for legacy payloads. Settings UI: a new Brake lights section in `SettingsPane.tsx` (right under Headlights, above Time of day) renders a three-button picker (Off / Auto / On) reusing the same `MenuButton` row pattern as the headlights picker, plus a sub-hint that swaps to whichever option is active so the player always sees plain-language confirmation of what the lamps will do. Tests: `tests/unit/brakeLights.test.ts` (42 tests covering enum membership / stable order / non-empty labels and descriptions / no em-dashes invariant on every player-facing string, `DEFAULT_BRAKE_LIGHT_MODE` membership, `BrakeLightModeSchema` accept all three valid modes plus reject `'flicker'` / `''` / `'AUTO'` / `0` / `null` / `undefined` / `{}`, `isBrakeLightMode` mirror coverage, `isBrakingNow` handbrake-always-on / brake-while-moving-forward / throttle-positive-never / throttle-zero-coasting-never / negative-throttle-while-reversing-never / non-finite-defensive-false / handbrake-overrides-non-finite-guard cases plus the positive `BRAKE_LIGHT_MIN_FORWARD_SPEED` floor invariant, `shouldBrakeLightsLight` `'off'`-always-dark / `'on'`-always-lit / `'auto'`-tracks-braking, plus visual-constant invariants (positive finite Y / Z offsets, negative X offset so the lamps mount on the rear, lamp radius under 0.5, glow disc wider than lamp, glow opacity strictly inside (0..1), 24-bit-bounds for the color hex, and a "red dominates green and blue" channel-mask invariant so a future palette tweak that breaks the brake cue gets caught here rather than in a bug report). `tests/unit/controlSettings.test.ts` adds 5 tests covering the default `'auto'`, round-trip of `'on'` and `'off'`, legacy backfill (omitted in stored payload), and reject-and-default on an unknown stored value.
- Tests: `tests/unit/physics.test.ts` covers throttle, max-speed cap, off-track cap, brake-while-moving, coast-to-zero, low-speed steering lockout, and steering while moving.
- **Not yet landed.** Angular velocity + quaternion heading, per-wheel raycast, dev-panel tuning (Section 10), `mass`/`downforce`/`forwardGrip`/`lateralGrip` fields from the GDD spec.

### Visual style

Cartoony, clean, readable silhouette. Start with free CC0 low-poly glTF models from the Kenney Car Kit. Voxel and custom-mesh paths are open for later iterations.

### Physics (simplified arcade)

Custom tick-based integrator. No Rapier or Cannon. Matches FrackingAsteroids' "custom math" approach in `src/game/collision.ts`.

**Parameters (all tunable via the dev panel, see Section 10).**

- `mass` (kg)
- `engineForce` (N)
- `brakeForce` (N)
- `forwardGrip` (0..1)
- `lateralGrip` (0..1)
- `steeringRate` (radians/sec)
- `steeringReturn` (radians/sec when neutral)
- `handbrakeLockFactor` (0..1 multiplier on lateral grip)
- `downforce` (arbitrary units, scales with speed)
- `maxSpeed` (m/s)

**Track adhesion.** Raycast from each wheel to the ground plane. If any wheel is off-track (fails its raycast against valid track mesh), apply strong lateral drag and a slowdown multiplier. This satisfies the requirement that the car stays on the track, without hard walls that feel cheap.

**State.** Position, velocity, heading (quaternion), angular velocity, throttle, brake, steering angle, handbrake.

---

## 6. Track System

**Status.** Partial. Validation, canonical hashing, default 3D track geometry, path ordering, and cell-based checkpoint generation now ship. Editor UI, save flow wired to `PUT /api/track/[slug]`, and additional piece types are still pending.

### Pieces

- Straight
- Left 90
- Right 90
- S-curve right (single-cell chicane that bumps east off the centerline and returns)
- S-curve left (mirror of S-curve right; bumps west off the centerline and returns)

Each piece occupies one cell on an infinite grid. Pieces have entry and exit connectors on cell edges. Both S-curve variants use the same connector pair as a straight (S-N at rotation 0) so they slot in wherever a straight does. Later additions: wider turn, elevation ramp.

### Editor

- Top-down 2D view of the grid.
- Click an empty cell to cycle through pieces and rotations.
- Pieces snap to grid. No free placement.
- Save button is disabled until the track forms a valid closed loop.
- **Closed-loop validation.** BFS through connectors starting from any piece. Valid only if every connector matches (a piece's exit connects to another piece's entry) and the resulting graph is a single closed cycle.
- **Piece limit.** 64 per track to start. Keeps playable and rendering budgets bounded.

### Versioning

- On save, serialize the piece array into a canonical form (stable ordering: row then column; include piece type, rotation, and position).
- Compute SHA-256 of the canonical JSON. This hash is the version ID.
- Each version has its own leaderboard. Previous versions remain accessible via `?v=<hash>`.

### Access

"Edit track" appears in the pause menu and on the title screen.

### Build log

- Piece schema (`type`, `row`, `col`, `rotation`) and 64-piece cap: `src/lib/schemas.ts`. Rotations restricted to the set `{0, 90, 180, 270}`.
- Connector math and BFS closed-loop validation: `src/game/track.ts`. Direction encoding is `N=0, E=1, S=2, W=3`. A piece has two open edges; each open edge must face a matching open edge on the neighboring cell. The graph must be a single connected component covering every piece. `DIR_OFFSETS`, `opposite`, and `cellKey` helpers are exported here and reused by `trackPath.ts` and `tick.ts`.
- Validation rejects: empty track, duplicate cell, dangling connector, connector mismatch, disjoint loops, piece count over 64.
- Canonical versioning: `src/lib/hashTrack.ts` sorts pieces by `row, col, type, rotation` before serializing and SHA-256. Hash is stable regardless of input order. Output is a 64-char lowercase hex string that matches the `VersionHashSchema` regex.
- Default track: `src/lib/defaultTrack.ts` exports `DEFAULT_TRACK_PIECES`, an 8-piece rectangular loop on a 3x3 grid (piece 0 is a straight so the car spawns cleanly on the centerline heading north). Served by `/[slug]/page.tsx` when the slug has no saved track. Its hash is hoisted to a module-level `DEFAULT_TRACK` constant to avoid recomputing per request.
- Path ordering: `src/game/trackPath.ts` exports `buildTrackPath(pieces): TrackPath`. Walks connectors from piece 0, producing `OrderedPiece[]` each with precomputed `center`, `entry`, `exit`, and (for corners) `arcCenter`. Also builds `cellToOrderIdx` (cellKey -> index) for O(1) lookups during lap detection. `CELL_SIZE = 20`, `TRACK_WIDTH = 8`.
- 3D geometry: `src/game/sceneBuilder.ts` builds flat ribbon meshes at `y = 0.01`. Straights are rectangles (TRACK_WIDTH x CELL_SIZE). Corners are quarter-annulus tessellations with 20 segments, inner radius 6, outer radius 14. All track pieces share one `MeshStandardMaterial`; `dispose()` dedupes materials via a Set to avoid double-disposal.
- Per-track checkpoints: one checkpoint per piece, expected in loop order. `tick.ts` records `{cpId, tMs}` when the car's cell becomes the next expected piece's cell and fires `LapCompleteEvent` when the car re-enters piece 0 after N-1 intermediate CPs.
- Tests: `tests/unit/track.test.ts`, `tests/unit/hashTrack.test.ts`, `tests/unit/trackPath.test.ts` (default-track validity, piece ordering, spawn, cell map, one-cell-apart centers, corner `arcCenter` placement, null `arcCenter` on straights).
- Editor UI: `src/components/TrackEditor.tsx` renders a top-down SVG grid around the current bounds (plus two cells of padding). Clicking a cell calls `withCellCycled`, which advances that cell through empty, `straight`/0/90/180/270, `left90`/0/90/180/270, `right90`/0/90/180/270, back to empty. Each piece is drawn as a dark road band plus dashed centerline, with a small dot at the south edge so the rotation reads at a glance without implying travel direction. Piece 0 gets a green border and START label since the car spawns on its entry edge. The footer shows piece count versus the 64-piece cap, a live validation line sourced from `validateClosedLoop`, and Cancel, Clear, and Save buttons. Save is disabled until the loop is valid and the editor is not already saving.
- Editor route: `src/app/[slug]/edit/page.tsx` is a server component. It validates the slug, loads the latest saved pieces through the shared `loadTrack(slug)` helper in `src/lib/loadTrack.ts` (falls back to `DEFAULT_TRACK_PIECES` when KV is unset or the slug has no saved track), and hands them to `<TrackEditor />`.
- Save flow: `TrackEditor` posts `{ pieces }` to `PUT /api/track/[slug]`. The four KV writes (`trackVersion`, `trackLatest`, `trackVersions`, `trackIndex`) run in parallel via `Promise.all`. On 200, the editor navigates to `/[slug]?v=<newHash>` so the driver lands on the freshly hashed version. On 4xx, the server's error message is shown in-line. On KV failure the route returns `503 { error: 'storage unavailable' }` instead of a raw 500. The route rejects without a `viberacer.racerId` cookie (middleware guarantees one is set on any page visit).
- Spawn math: `buildTrackPath` spawns the car at piece 0's entry edge stepped `SPAWN_INSET` (2) units inward along the travel direction. This lands on the centerline for straights and on the arc for corners, and avoids `worldToCell` rounding into the neighbor cell.
- Historical version deep-link: `/[slug]?v=<hash>` routes through `src/app/[slug]/page.tsx`. The page reads `searchParams.v`, validates via `VersionHashSchema`, and calls `loadTrack(slug, hash)`. Unknown or malformed hashes trigger `notFound()` rather than silently falling back to latest. The leaderboard UI can hand users between versions (see §11).
- Pause menu: `PauseMenu` gains an Edit Track entry between Restart and Leaderboards. `Game.tsx` wires `onEditTrack` to `router.push('/<slug>/edit')`.
- Helper + tests: `src/game/editor.ts` exports `cycleCell`, `withCellCycled`, and `getBounds`. `tests/unit/editor.test.ts` covers the full cycle order, rotation and type advancement, piece removal on final-state cycle, and bounds across negative and positive coordinates. `tests/unit/trackPath.test.ts` covers corner-start spawn staying on the arc.
- Configurable checkpoint count: `TrackSchema` and `TrackVersionSchema` now carry an optional `checkpointCount` (`MIN_CHECKPOINT_COUNT = 3`, capped at `pieces.length`). `hashTrack(pieces, checkpointCount)` only emits the field in canonical JSON when it differs from `pieces.length`, so legacy tracks without the field keep their existing version hash and leaderboards. `buildTrackPath(pieces, checkpointCount)` precomputes `cpTriggerPieceIdx[k] = round((k+1) * M / K) % M`, and `tick.ts` reads from that array instead of assuming one CP per piece. The editor hides this behind an Advanced toggle in the footer (with an amber dot when an override is active). Opening the panel surfaces a labeled numeric input alongside an explanation of what checkpoints do and when an author would change the count; the default ("one per piece, strictest") is what every legacy track ships with.
- Historical version forking from the editor: `src/app/[slug]/edit/page.tsx` now reads `searchParams.v`, validates with `VersionHashSchema`, and threads the result into `loadTrack(slug, requestedHash)`. An invalid or unknown hash calls `notFound()`. When a hash is supplied, `TrackEditor` renders a small amber "FORKING v<short>" banner explaining that saving creates a new version on the same slug rather than overwriting the original, plus a "Switch to latest" shortcut. The leaderboard pause overlay (`src/components/Leaderboard.tsx`) gained a "Fork this version" / "Edit latest" button next to the existing "Race this version" button so players can jump straight from any version's leaderboard into editing a copy.
- S-curve piece: live. New `'scurve'` value in `PieceTypeSchema` shares the straight's `[S, N]` connectors at rotation 0 so it slots in wherever a straight does. Geometry is built from four 90-degree arcs of radius 3 plus 4-unit straight bridges at entry and exit; the centerline bumps off-axis by `2 * SCURVE_ARC_RADIUS = 6` at the cell midpoint, which lands the road's outer edge exactly at the cell boundary. `OrderedPiece` gained an optional `samples: SampledPoint[] | null` field; for S-curve pieces `buildTrackPath` precomputes 49 evenly-spaced samples (rotated and translated to match the piece's placement, with the order reversed when the loop traverses the piece against its base direction). `samplePieceAt` and `distanceToCenterline` walk the polyline for sampled pieces; analytic straights and arcs are unchanged. The track editor palette (`TrackEditor.tsx`) gained an S-curve tool with its own SVG glyph alongside Straight / Left turn / Right turn. The render path in `sceneBuilder.ts` builds a triangle strip with `TRACK_WIDTH/2` perpendicular offset for any piece with `samples`. Existing tracks keep their hash because the canonical hash is keyed on piece type strings, not enum index.
- Editor pan and zoom: live. `src/game/editorZoom.ts` owns the pure helpers (`clampZoom`, `shiftZoomTowardCursor`, `fitZoom`, `pinchZoom`, `distance`) with a 0.4x to 2.5x range. `TrackEditor` keeps the SVG `viewBox` at the base content size and scales the rendered `width` and `height` by `zoom`, so all interior coordinates and `data-row` / `data-col` hit-testing keep working unchanged. A floating bottom-right toolbar exposes -, +, and Fit buttons plus a percent readout. Ctrl / Meta + scroll wheel zooms toward the cursor (anchoring the world point under the pointer); plain scroll continues to pan. Two-finger pinch on touch tracks pointer pairs in `pinchRef` and pinches around the midpoint, with a click suppression so the gesture-end pointerup does not place a piece. The grid container itself uses `overflow: auto` for click-and-drag scroll bars and centers the content on first mount. Tests: `tests/unit/editorZoom.test.ts` covers clamping, the cursor-anchored shift formula, fit-to-viewport including padding, pinch ratios, and degenerate inputs.
- Mirror S-curve piece (`scurveLeft`): live. New `'scurveLeft'` value in `PieceTypeSchema` shares the same `[S, N]` base connectors as `scurve` so it slots into a straight's spot interchangeably. Geometry is built by mirroring the cached `SCURVE_LOCAL_SAMPLES` across the local x = 0 axis (negate x, reflect heading via `pi - h`); `sampleScurveLeftLocal()` exposes this for tests. `buildScurveSamples` selects the left or right local set by `piece.type`, and `buildTrackPath`'s `isScurve` check now matches both variants so polyline geometry, distance-to-centerline, and per-piece sampling all flow through the existing scurve code paths. The track editor palette adds an "S-curve (left)" tool with its own glyph (the existing scurve glyph mirrored via SVG `scale(-1 1)`); the right scurve gets relabeled "S-curve (right)" for symmetry. Tests: `tests/unit/track.test.ts` covers shared connectors at multiple rotations; `tests/unit/schemas.test.ts` accepts the new piece type; `tests/unit/trackPath.test.ts` adds 7 tests covering local-sample mirror identity, west-bumping amplitude, closed-loop validity in a stadium layout, sample population, entry/exit endpoint anchoring, every sample staying inside the cell, distance-to-centerline at sample points, and apex point reflecting the right scurve across the cell center. `tests/unit/tuningLabTrack.test.ts` upgrades its piece-mix counter to `Record<PieceType, number>` so adding new piece types is type-safe going forward.
- Inside-corner kerbs: live. Pure helpers in `src/game/kerbs.ts` (`buildCornerKerbTiles(op, segmentCount?)`, `buildTrackKerbTiles(path, segmentCount?)`, `countCorners(path)`, plus the `KERB_*` constants for tile depth, length, lift, segment count per 90 degrees, and the red / white palette). Each `KerbTile` carries a world position, a `rotationY` matching the local arc tangent, the per-tile length and depth, and a hex color that alternates red and white starting from the entry side. Tiles tile along the inner-radius arc of every left90 / right90 piece (radius `CELL_SIZE/2 - TRACK_WIDTH/2 = 6`) so the kerb sits exactly on the inner road edge at the apex. Tile length is computed per-corner as `(arcSweep * innerRadius) / segmentCount` so a row of `KERB_SEGMENTS_PER_CORNER = 8` tiles always lands flush to the arc end. S-curves and straights have no kerbs in v1: the affordance only reads as "this is the apex, hug it" at corners, so adding it to straights would be visual noise. The renderer side is `buildKerbLayer(path)` in `src/game/sceneBuilder.ts`: it walks the tile list, caches one `MeshBasicMaterial` per unique color (so the whole track collapses to two materials regardless of corner count) and one `PlaneGeometry` per `(length, depth)` pair, and lays each plane flat (`rotation.set(-PI/2, 0, tile.rotationY)`) at `KERB_Y = 0.04` so the kerb sits above the asphalt without z-fighting. The layer's `setVisible(value)` flag flips `group.visible`, so a Settings toggle is O(1). Toggleable from Settings (`showKerbs: boolean` in `ControlSettings`, default on, persisted via the existing `viberacer.controls` localStorage key); the canvas reads `showKerbsRef` each frame through a `syncKerbs` helper that short-circuits on no-op (boolean equality on the last applied value). Tests: `tests/unit/kerbs.test.ts` (14 tests covering segment count, non-corner / invalid-segment short-circuits, every tile sitting on the inner-radius arc, alternating red / white starting with red, constant tile depth, summed tile lengths matching the inner arc length, tangent-perpendicular-to-radius rotation invariant on every tile across all four sides of a closed square, first-tile-near-entry / last-tile-near-exit ordering, total-tile-count product for both DEFAULT_TRACK_PIECES and a custom segment count, the straights-only zero-tile case, and the `countCorners` helper). `tests/unit/controlSettings.test.ts` adds default-true, round-trip-disabled, and legacy-backfill coverage for the new `showKerbs` field.
- Editor undo / redo: live. Pure helpers in `src/game/editorHistory.ts` (`createHistory(initial)`, `pushHistory(history, next)` records the prior present onto the past stack and clears the redo stack, `undoHistory(history)` pops the latest past entry into the present and pushes the prior present onto the future stack, `redoHistory(history)` is the mirror, plus `replacePresent` for non-recordable swaps and `resetHistory` for clearing both stacks). The cap `EDITOR_HISTORY_MAX_PAST = 100` drops the oldest entry once exceeded so a long editing session cannot grow without bound. Reference-equal pushes are no-ops so an idempotent setter (e.g. tapping erase on an empty cell) does not pollute the past stack with duplicates. Spread (not `concat`) is used internally so an array-typed `T` is not auto-flattened. `TrackEditor.tsx` wraps its `pieces` state in an `EditorHistory<Piece[]>` and routes every existing mutation through a derived `setPieces` callback that calls `pushHistory`. Two new ghost buttons in the footer (Undo / Redo, both disabled when the relevant stack is empty) sit between Cancel and Reverse direction so they read as edit-history controls. Keyboard shortcuts: `Ctrl/Cmd+Z` undoes, `Ctrl/Cmd+Shift+Z` and `Ctrl/Cmd+Y` redo; the listener bails out when the focused element is an input, textarea, select, or contentEditable so the checkpoint number field and mood pickers behave normally. Tests: `tests/unit/editorHistory.test.ts` (23 tests covering create / push / undo / redo / replace / reset across reference-equality no-ops, non-deep value equality, the redo-branch-clears-on-fresh-push invariant, the cap-at-EDITOR_HISTORY_MAX_PAST drop-oldest invariant, input-not-mutated guarantees, reach-the-seed round-trips on stacked pushes, and an integration scenario modelling a typical edit / undo / new-edit / undo-to-empty session).
- Trackside scenery: live. Pure helpers in `src/game/scenery.ts` (`buildScenery`, `buildTreeScenery`, `buildCornerCones`, `buildStartBarriers`, `maybeTreeAt`, `distanceToTrack`, `sceneryBounds`, `seedFromPath`, `makeSceneryRng`, plus the `SCENERY_*` constants for clearance, jitter, density, cone offset, barrier spacing, and the cartoony palette). Placement is deterministic from a stable seed derived from the piece layout (`seedFromPath` folds piece type, row, col, and rotation into a 32-bit integer via Mulberry32-friendly multiplies) so the same track always gets the same scenery and a player can mentally bookmark "the tree before the hairpin" across sessions. Trees are scattered on a regular grid spanning the bounding box (plus a `SCENERY_RING_PADDING = CELL_SIZE * 1.5` apron) with per-cell jitter; each candidate runs a `distanceToTrack(path, x, z) >= SCENERY_TRACK_CLEARANCE` keep-out so a slight off-track excursion never slams the car into a hedge. Cones march along the OUTSIDE of every corner (radius `CELL_SIZE/2 + TRACK_WIDTH/2 + 1.0`) so they read as a "do not run wide here" cue without colliding with the inside-corner kerbs that hug the inner radius. Barriers frame the start gate: 5 alternating red / white blocks per side, lateral-offset by `TRACK_WIDTH/2 + 1.4` from the finish line center and spread along the heading axis. Renderer side is `buildSceneryLayer(path)` in `src/game/sceneBuilder.ts`: one `Group`, shared geometries (`CylinderGeometry` for trunk, `ConeGeometry` for foliage and traffic-cone body, `BoxGeometry` for cone base and barrier block) and a per-color `MeshStandardMaterial` cache keyed on hex so a hundred trees collapse to two foliage materials plus one trunk material plus the cone / barrier materials. Trees use a green cone on a brown cylinder so the prop reads as a stylized pine without needing an external model; cones use a slim orange cone on a small dark base; barriers are short rectangular blocks alternating red / white. Toggleable from Settings (`showScenery: boolean` in `ControlSettings`, default on, persisted via the existing `viberacer.controls` localStorage key); the canvas reads `showSceneryRef` each frame through a `syncScenery` helper that short-circuits on no-op (boolean equality on the last applied value) and just flips the parent group's `visible` flag, so a flip in Settings hides everything in O(1) without rebuilding any geometry. Tests: `tests/unit/scenery.test.ts` (31 tests covering RNG determinism for a fixed seed, sequence divergence across seeds, value-range guarantees, zero-seed defense, seed stability across calls and divergence across layouts, bounds containment, distance-to-track invariants, the maybe-tree gate logic for clearance / density / sane fields, tree-layout determinism, every tree clearing the keep-out radius, every tree staying inside the bounding box plus jitter, zero-density short-circuit, cones-per-corner count, every cone outside the road edge, color invariants, defensive cones-per-corner rejection, no-corners short-circuit, barrier count and color alternation, finish-line proximity bounds, heading match, combined-builder kind coverage, determinism, and the `includeTrees` / `includeCones` / `includeBarriers` opt-out flags). `tests/unit/controlSettings.test.ts` adds default-true, round-trip-disabled, and legacy-backfill coverage for the new `showScenery` field.
- **Not yet landed.** Wider sweep turn, elevation ramp.

---

## 7. Routing & User-Owned Paths

**Status.** Partial. Middleware, `racerId` cookie, `/` home, `/[slug]` race page, and the initials lifecycle all ship. The create-or-load prompt on a fresh slug, the Settings screen, and `?v=<hash>` deep-linking are not yet landed.

Next.js App Router dynamic routes.

### Routes

- `/` Home. Shows "Create new track" and "Load existing" (a list of latest-updated slugs across the site).
- `/[slug]` Main game page. Loads the latest saved version of the track at that slug. If the slug has no track yet, shows a create-or-load prompt scoped to that slug.
- `/[slug]?v=<hash>` Loads a specific historical version of the track at `<slug>`. Used for leaderboard time travel.

### Ownership and editing rules

- There is no account system. Tracks are not owned in the traditional sense.
- Anyone can visit any slug and race the track there.
- Anyone can edit any slug's track. Editing creates a new version (new hash). Previous versions and their leaderboards are preserved.
- Anyone can submit a lap time on any version they actually raced.

### Initials lifecycle

Initials are the player's leaderboard identity. Three uppercase letters, arcade style.

- Stored in `localStorage` under `viberacer.initials`. Set once, reused everywhere.
- **Single rule for every route.** On page load, check `localStorage` for initials. If missing, show the initials prompt immediately, before the game, editor, or any menu appears. Same behavior on `/` and any `/[slug]`.
- Prompt copy is plain and direct: "Enter 3 initials. They will tag your lap times on the leaderboards." No scare text. No legal boilerplate.
- Settings screen exposes initials for editing at any time. Changing them does not rewrite historical submissions.
- Once initials are set, every completed lap auto-submits silently. No additional prompts.

### Build log

- `racerId` cookie set by `src/middleware.ts`. Cookie name is `viberacer.racerId`, value is a UUID v4, flags are `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`, `Max-Age = 1 year`.
- `src/lib/racerId.ts` exposes `newRacerId()`, `isValidRacerId()` (strict UUID v4 regex, not just "any UUID"), and `readRacerId()` for RSC use.
- Middleware matcher excludes `_next/static`, `_next/image`, `favicon.ico`, and any path with a file extension. All other routes (pages and APIs) run middleware.
- Middleware best-effort writes `racer:<racerId>:firstSeen` to KV via dynamic import wrapped in try/catch so a KV outage never blocks page responses.
- Playwright smoke in `tests/e2e/smoke.spec.ts` verifies the cookie lands on first visit and matches the UUID v4 regex.
- `/[slug]` page: `src/app/[slug]/page.tsx` is a server component. Validates slug via `SlugSchema`, loads the track from KV (or falls back to `DEFAULT_TRACK` when KV env vars are missing or the slug has no saved track), then renders `<Game slug versionHash pieces />`.
- Initials prompt: `src/components/InitialsPrompt.tsx` reads/writes `viberacer.initials` via `readStoredInitials` / `writeStoredInitials`. Validated through `InitialsSchema`. `Game.tsx` blocks the canvas render until initials exist: on mount it reads `localStorage`; if missing, it shows `InitialsPrompt`; once set, it renders `GameSession` which runs the countdown and race.
- `/` page: `src/app/page.tsx` shows "Play default track" plus two sample slug links. The planned Create / Load existing / Settings home UI is still pending.
- `?v=<hash>` deep-link handling: live. `src/app/[slug]/page.tsx` reads `searchParams.v`, validates through `VersionHashSchema`, and loads that specific version from KV (`track:<slug>:version:<hash>`). Missing or invalid hashes call `notFound()`. The overall-record seeded into the HUD is also scoped to the requested version so history browsing shows correct top times.
- Per-slug create-or-load prompt: live via `SlugLanding`. `src/lib/recentTracks.ts::readRecentTracks(kv, limit, excludeSlug?)` reads `track:index` newest-first (`zrange` with `rev: true, withScores: true`), rejects members that fail `SlugSchema`, and returns typed `{slug, updatedAt}` entries. Unit coverage in `tests/unit/recentTracks.test.ts`.
- Home page: `src/app/page.tsx` is now an async RSC. Primary CTA renamed from `Play default track` to `Play at /start` so it stays truthful when `/start` has no saved track (it lands on `SlugLanding` in that case, same as any other fresh slug). Below the CTA a `RECENT` section reuses `readRecentTracks` to show up to ten recently-updated slugs with their date; when the KV index is empty (or KV env vars are missing in local dev), the section falls back to the `/oval` and `/sandbox` sample slugs so the page still has somewhere to click.
- Settings-screen initials editing: live. `SettingsPane` renders an Identity section with the current initials in an inline 3-letter input. Saving routes through `writeStoredInitials` in `src/lib/initials.ts`, which broadcasts a `viberacer:initials-changed` CustomEvent so `Game` can refresh the HUD's RACER block in-place.
- Recent track preview thumbnails: live. `src/lib/trackThumbnail.ts::buildTrackThumbnail(pieces, viewSize?, padding?)` is a pure helper that wraps `buildTrackPath` plus `buildMinimapGeometry` and returns one SVG path per piece, the spawn point in view coordinates, and a `roadStrokeWidth` derived from the actual track-width-to-cell-size ratio so the road thickness reads honestly at the chosen scale (clamped to `[1, viewSize/2]` so single-piece tracks do not paint the whole tile and sprawling tracks still draw a visible road). `src/components/TrackThumbnail.tsx` renders the asphalt as a thick dark stroke with a thinner dashed yellow centerline overlay and a green spawn dot. `src/lib/recentTracks.ts::loadRecentTrackPreviewsSafe(excludeSlug?, limit?)` reuses `loadRecentTracksSafe` then issues parallel `loadTrack` calls (capped at `RECENT_TRACKS_PREVIEW_MAX = 12` so SSR cost stays bounded) and returns a `RecentTrackPreview[]` where each row carries the latest pieces or `null` on a per-row KV miss. `RecentTrackList` accepts an optional `pieces` per item: present rows render the thumbnail, absent rows show a hatched placeholder so the list stays vertically stable even on a partial KV outage. Wired into `src/app/page.tsx` (home) and the fresh-slug landing in `src/app/[slug]/page.tsx -> SlugLanding`. Tests: `tests/unit/trackThumbnail.test.ts` covers the empty-input null short-circuit, single-piece survival, one-path-per-piece for the default oval, requested view size with spawn-inside-view, road-stroke clamping, and the sprawl-shrinks-stroke invariant.
- Your-personal-bests section on the home page: live. `src/lib/myPbs.ts` exports pure helpers `parseLocalBestKey`, `parseStoredLapTime`, `buildMyPbs(entries)` plus the thin `readMyPbs()` wrapper. The wrapper enumerates `localStorage` for keys matching `viberacer.best.<slug>.<versionHash>` (defensive try/catches around `length`, `key`, and `getItem` so a hostile or quota-blocked storage never throws), parses each into a typed `MyPbEntry`, groups by slug, and sorts by best lap time ascending (ties broken by slug name) so the fastest track sits first. Each entry carries every (versionHash, lapTimeMs) the player has logged for that slug so a future drill-down can show per-version PBs without a second scan. `src/components/MyPbs.tsx` is a client component that hydrates from `readMyPbs()` on mount, listens to the cross-tab `storage` event so a fresh PB in another tab refreshes the list live, renders nothing pre-hydration (so the SSR shell stays empty), and renders nothing when the player has no PBs yet (so brand-new browsers do not see an empty section). Each row links to `/<slug>?v=<fastestVersionHash>` so clicking takes the player straight to the exact track version where they set the time. The PB badge uses a green accent (mirroring the PB confetti color) so the home page reads visually distinct from the gold "track record holder" badge on the recent-tracks list. Wired into `src/app/page.tsx` between `Load existing track` and the Tuning Lab launch button. Tests: `tests/unit/myPbs.test.ts` (19 tests covering key parsing happy path, prefix rejection, invalid hash / slug / dot position, lap-time rejection of zero / negative / NaN / Infinity / over-an-hour values, fractional rounding, group-by-slug invariants, sort-by-best-time-then-slug, malformed-entries-skipped invariant, Map iterator compatibility, the hydrate scan, the empty-storage short-circuit, and the throwing-localStorage recovery path).
- Top-time badge on recent track rows: live. `src/lib/recentTracks.ts` gained a `TopTime { initials, lapTimeMs }` interface plus two pure helpers (`parseTopTimeFromZrange(raw)` parses the rank-1 entry out of an Upstash `zrange withScores` payload, `fetchTopTimeForSlug(kv, slug)` resolves the slug's latest version hash then reads the rank-1 leaderboard entry for that version). `loadRecentTrackPreviewsSafe` now resolves the kv handle once up-front and issues each row's `loadTrack` and `fetchTopTimeForSlug` calls in parallel, so the home page does N additional cheap reads (one `get` plus one `zrange limit=1` per slug) without serializing them. `RecentTrackPreview` carries a new `topTime: TopTime | null` field; null means "no record on the latest version" (or KV outage) and the row simply omits the badge so an empty leaderboard reads cleanly rather than showing a placeholder. `RecentTrackList` accepts an optional `topTime` per item and, when present, renders a small gold badge on the slug row showing the holder's initials and `mm:ss.mmm` lap time (reuses `formatLapTime` from `src/lib/share.ts` so the format matches the share text and the HUD). The badge uses the same accent color as the PB confetti fanfare so the visual language stays consistent across the app. Wired into both `src/app/page.tsx` (home) and `SlugLanding`. Defensive parsing rejects malformed initials (must be three uppercase A-Z), wrong-segment-count members, non-finite or non-positive scores, and implausibly long lap times (over an hour) so a corrupt or hostile KV row never poisons the home page. Tests: `tests/unit/recentTracks.test.ts` adds 13 tests covering happy parse, score rounding, empty / non-array / too-short input, malformed initials, too-few segments, non-finite / non-positive / overlong scores, no latest version, populated rank-1, empty leaderboard, blank latest hash, and a thrown-kv recovery.
- Lifetime stats section on the home page: live. Pure helpers in `src/lib/lifetimeStats.ts` (`parseTrackStatsKey(key)` parses a `viberacer.stats.<slug>.<versionHash>` key into typed slug + hash parts (mirrors `parseLocalBestKey` in `myPbs.ts`), `parseStoredTrackStats(raw)` validates a JSON blob through a redeclared `StoredTrackStatsSchema` so a hand-edited or quota-corrupt entry never poisons the totals, `buildLifetimeStats(entries)` folds an iterable of `(key, value)` pairs into a single `LifetimeStats` snapshot deduping the slug count via a `Set` while keeping version count and totals additive, plus `emptyLifetimeStats()` for the React seed). The thin `readLifetimeStats()` wrapper enumerates `localStorage` for keys matching `viberacer.stats.*` (defensive try/catches around `length`, `key`, and `getItem` so a hostile or quota-blocked storage never throws and a single bad row never aborts the scan). `src/components/LifetimeStats.tsx` is a client component that hydrates on mount, listens to the cross-tab `storage` event so a session in another tab refreshes the totals live, renders nothing pre-hydration (so the SSR shell stays empty), and renders nothing when both `totalSessions` and `totalLaps` are zero (so brand-new browsers do not see an empty grid of dashes). The pane shows a 2x2 grid of tiles (Total laps, Time on track via the existing `formatDuration` from `trackStats.ts`, Sessions, and "tracks raced" plus a smaller "N versions" sub-line when the player has forked any tracks) above a small "Racing since YYYY-MM-DD, last raced YYYY-MM-DD" footer (the trailing clause is dropped when first and last are the same day so a one-session player sees a clean line). Wired into `src/app/page.tsx` directly under the existing `MyPbs` section so the player sees their lifetime engagement above the Tuning Lab launch button. Tests: `tests/unit/lifetimeStats.test.ts` (24 tests covering empty-snapshot fresh-object isolation, key parsing happy path / invalid hash / invalid slug / wrong dot position / wrong prefix, stored-blob null / malformed-JSON / negative-lap / fractional-lap / negative-drive / non-finite-timestamp rejection, valid-blob round-trip with explicit nulls, build empty / mixed unrelated keys, multi-track sum / drive ms / sessions, slug dedup across versions, malformed-key-and-blob skip, earliest-firstPlayedAt / latest-lastPlayedAt selection, null-timestamp tolerance with mixed entries, all-null-timestamp short-circuit, Map iteration support, defensive-no-double-count-on-Set-keys-but-totals-additive, hydrate scan, empty-storage short-circuit, missing-window short-circuit, throwing-localStorage recovery, and per-key throw recovery).
- Tracks-you-built section on the home page: live. Pure helpers in `src/lib/myTracks.ts` (`sortMyTracks(entries)` returns a fresh newest-first list with stable ties on slug name, `applyMyTrackSave(prev, slug, updatedAt)` is the immutable reducer that appends a brand-new slug or bumps an existing slug's `updatedAt` to a newer timestamp (older timestamps are intentionally ignored so an out-of-order recorder can never regress a row), `parseMyTracks(raw)` validates a JSON blob through a `SlugSchema` plus positive-finite-timestamp shape and dedupes hand-edited duplicates by keeping the newest `updatedAt` per slug, plus the thin `readMyTracks()` and `recordMyTrack(slug, nowMs?)` wrappers that touch `localStorage`). The wrappers store under a single `viberacer.myTracks` key (one JSON blob, not per-slug, because the list is small and a single write keeps the editor's save flow simple) and dispatch a `viberacer:my-tracks-changed` CustomEvent on every successful write so the home-page section can refresh live. `src/components/TrackEditor.tsx` calls `recordMyTrack(slug)` immediately after a successful PUT response and before the post-save navigation to `/<slug>?v=<hash>`, so every author-save (including a fork that adds a new version on an existing slug, since the editor's save flow is the same code path) lands in the log. `src/components/MyTracks.tsx` is a client component that hydrates from `readMyTracks()` on mount, listens to both the cross-tab `storage` event AND the same-tab `viberacer:my-tracks-changed` custom event so a save in another route inside the same tab refreshes the list once Next's client router lands the player back on the home page, renders nothing pre-hydration (so the SSR shell stays empty) and renders nothing when the player has not authored a track yet (so brand-new browsers do not see an empty section). Each row links to `/<slug>` (race the latest) and exposes an amber "EDIT" chip that links to `/<slug>/edit` so the player can pop straight into the editor without a second click; the amber matches the FORKING banner inside `TrackEditor` for visual continuity. Wired into `src/app/page.tsx` between the recent-tracks list and `MyPbs`. Tests: `tests/unit/myTracks.test.ts` (27 tests covering `sortMyTracks` newest-first / tie-break / non-mutating / empty-input invariants, `applyMyTrackSave` append-to-empty / append-to-populated / bump-existing / older-timestamp-keeps-existing / non-mutating, plus defensive rejection of non-string slugs / SlugSchema-failing slugs / NaN / Infinity / -Infinity / zero / negative / non-number timestamps, `parseMyTracks` null / malformed-JSON / non-array / schema-fail / valid-round-trip / hand-edited-duplicate-dedupe, and `readMyTracks` / `recordMyTrack` empty-storage start, persist-and-dispatch, bump-existing, append-new-alongside-existing, malformed-slug-no-op invariant, sorted-newest-first read, and corrupt-blob recovery).
- Daily Challenge featured card on the home page: live. Pure helpers in `src/lib/dailyChallenge.ts` (`dateKeyForUtc(nowMs)` formats epoch ms as `YYYY-MM-DD` in UTC so two players in different time zones see the same daily slot, with a defensive fall-back to today's key on non-finite or non-number input; `dailyChallengeSeed(input)` is a 32-bit FNV-1a hash mirroring the `slugMusicSeed` style for cross-codebase consistency; `pickDailyChallenge<T extends {slug: string}>(tracks, dateKey)` deterministically picks one entry from the candidate list by mixing the date-key seed with the candidate count via a high-bit-fold-then-modulo so adjacent date keys do not always land on adjacent indices; `loadDailyChallengeSafe(nowMs?, limit?)` is the thin wrapper that calls `loadRecentTrackPreviewsSafe` then picks today's). The candidate set is the freshest `RECENT_TRACKS_DEFAULT_LIMIT` slugs from `track:index` so the daily slot rotates through community work rather than pinning to a stale archive. `src/components/DailyChallenge.tsx` is a server component that renders nothing when KV is unconfigured / has no recent tracks (so a fresh local-dev install does not see an empty card), otherwise renders a featured card with the slug, an 88x88 thumbnail, the current top time on the latest version (or a "set the pace" empty-state when the leaderboard is still empty), the track's last-updated date, and a red "Race" CTA that links straight into `/<slug>` (which resolves the latest version itself, so the URL stays clean). Wired into `src/app/page.tsx` between the primary Play button and the "Go to any track" slug input, so the daily slot is the first thing a returning player sees. Tests: `tests/unit/dailyChallenge.test.ts` (19 tests covering UTC formatting at midnight / single-digit padding / just-before-midnight / next-day rollover, fall-back-to-today on non-finite / non-number input, FNV seed empty / non-string short-circuit, deterministic seed, single-character flip sensitivity, unsigned 32-bit range, empty / non-array list short-circuits, picked-candidate-is-in-list invariant across 60 date keys, idempotent pick across calls, single-candidate degenerate case, adjacent-date-key variation invariant (more changes than collisions across a month), input-not-mutated guarantee, and the typing-preserves-extras invariant on a richer candidate type).

---

## 8. Race Flow

**Status.** Partial. Countdown (now animated traffic light with per-step synth beeps), per-track configurable checkpoint count, lap detection, the full HUD (with live split delta vs local PB and a projected final lap time at each checkpoint), top-down minimap card, top-center rear-view mirror inset, pause button, skid mark trail, invalid-lap handling, drift score readouts, and a wrong-way warning banner are all live.

### Build log

- Countdown: `src/components/Countdown.tsx` cycles `READY -> SET -> GO` on an 800 ms interval, then holds GO for 600 ms and fires `onDone`. Renders a vertical three-lamp traffic-light housing (red, amber, green) with the current step label below. Each step lights exactly one lamp: READY = red, SET = amber, GO = green. Inactive lamps are inset-darkened; the lit lamp glows via `boxShadow`. The label text is gold during READY / SET and green on GO. During countdown, `GameSession` keeps the tick loop running but `state.raceStartMs` is null so physics is frozen and the timer shows 00:00.000.
- Countdown beeps: `playCountdownBeep(isGo)` in `src/game/music.ts` fires a one-shot tone through the shared `AudioContext`, routed to the master gain so it is audible over any active music without piping through the step scheduler. Counting steps use a square wave at A4 (midi 69); GO uses a triangle wave at A5 (midi 81) with a slightly longer decay. Envelope is a 10 ms linear attack then exponential decay to 0.001. Countdown's `useEffect([step])` calls the helper once per step change (mount + each increment), so READY and SET play low beeps and GO plays the higher pitch.
- Lap detection: cell-based. `tick.ts` compares `state.lastCellKey` against the current cell each frame. The expected piece for the next CP comes from `path.cpTriggerPieceIdx[nextCpId]` (precomputed by `buildTrackPath` from the per-track `checkpointCount`, defaulting to one per piece). When the car enters that piece, it records `{cpId, tMs}` where `tMs = nowMs - raceStartMs`. After K CPs, the lap completes (the K-th trigger piece is always piece 0) and resets `nextCpId=0`, `hits=[]`, `raceStartMs=nowMs` for the next lap. Lap count increments.
- HUD: `src/components/HUD.tsx` renders CURRENT (big), LAST LAP, BEST (SESSION), BEST (ALL TIME), RECORD (track-wide top time, loaded from KV in the RSC page and passed through), LAP, RACER, plus an OFF TRACK warning and a transient toast for PB / lap-saved messages. Stat blocks share a `StatBlock` subcomponent. `setHud` is throttled to ~20 Hz with a reference-equality bail-out so the tree doesn't re-render unnecessarily.
- Endless loop: no lap cap. Every completed lap triggers `handleLapComplete` which updates local PBs and fires `submitLap` (fire-and-forget).
- Invalid-lap reset: in `tick.ts`, if the car transitions into the start-piece cell while `nextCpId > 0` (i.e., the player has partial checkpoint progress but is re-entering the start without a valid lap completion), `hits` is cleared, `nextCpId` is reset to 0, and `raceStartMs` is set to `nowMs`. The lap counter is not incremented and no `LapCompleteEvent` fires. Covers driving backward through the start line or taking a shortcut that re-enters piece 0 early. Test coverage in `tests/unit/tick.test.ts`.
- Pause button: always-visible circular button at `bottom: 20, left: 16` (rendered only during the `racing` phase and hidden once the pause menu opens). Clicking calls `pause()` which freezes the tick and shows the pause menu.
- Configurable checkpoint count: `Track.checkpointCount` is optional in the schema, range `[3, pieces.length]`. Distribution formula is `cpTriggerPieceIdx[k] = round((k+1) * M / K) % M`, which always lands the K-th trigger on piece 0 so the lap-complete branch in `tick.ts` is unchanged. Hash compatibility: `hashTrack(pieces, checkpointCount)` only emits the field when it differs from `pieces.length`, preserving every legacy hash. Editor exposes a numeric input next to the piece counter; default reads as `default` and switching back to `pieces.length` clears the override on save. Test coverage: `tests/unit/schemas.test.ts`, `tests/unit/hashTrack.test.ts`, `tests/unit/trackPath.test.ts`, `tests/unit/tick.test.ts`, `tests/unit/api.track.test.ts`.
- Checkered start / finish gate: live. Pure helpers in `src/game/finishLine.ts` (`buildCheckerTexturePixels`, `computeGatePolePositions`, `gatePoleSeparation`, plus the `FINISH_*` constants for stripe depth, square count, pole height / thickness, banner dimensions, and pole inset). The flat white finish stripe in `sceneBuilder.ts` is now a checkered ground stripe drawn from a procedurally-generated 8x2 RGBA `DataTexture` (`NearestFilter` for crisp squares; black at the (0, 0) corner so the parity matches the classic checkered-flag convention) sized via `FINISH_TEXTURE_PIXELS_PER_SQUARE = 16`. The same texture is cloned for an overhead banner suspended `FINISH_GATE_POLE_HEIGHT = 5.5` units up between two `FINISH_GATE_POLE_THICKNESS = 0.35` side poles placed `FINISH_GATE_POLE_INSET = 0.6` units beyond each road edge via `computeGatePolePositions`. The banner spans pole-to-pole plus a small `FINISH_GATE_BANNER_OVERHANG = 0.4` overhang and turns by `heading + PI / 2` so its long axis spans perpendicular to travel direction. Both procedural textures are explicitly `dispose()`-d in the bundle's existing dispose path so a Restart does not leak GPU memory. Tests: `tests/unit/finishLine.test.ts` (12 tests covering buffer size, top-left parity, alternating-square parity row + column, opaque alpha for every pixel, integer / positive dimension validation, perpendicular pole placement at heading 0 + heading PI/2, custom inset respect, midpoint preservation across a non-axis-aligned heading, and the pole-separation formula).
- Drift score HUD: live. Pure helpers in `src/game/drift.ts` (`driftIntensity`, `driftMultiplier`, `initDriftSession`, `stepDriftSession`, `formatDriftScore`, plus the `DRIFT_*`, `SCORE_PER_MS`, `MAX_MULTIPLIER`, `MULTIPLIER_GROWTH_MS`, and `CHAIN_BONUS` tunables). The session machine starts a drift when the combined intensity (`steerAbs * speedRatio * 1.6`) crosses `DRIFT_ENTER_INTENSITY = 0.32` with the car above `DRIFT_MIN_SPEED = 4` and steering above `DRIFT_MIN_STEER = 0.18`; it accrues score every active frame as `intensity * SCORE_PER_MS * dt * multiplier` where the multiplier ramps linearly from 1.0 toward `MAX_MULTIPLIER = 4` across `MULTIPLIER_GROWTH_MS = 4000`. A direction flip during an active session adds a flat `CHAIN_BONUS = 25` (and increments a chain counter) so weaving through an S-curve scores better than a single hard slide. The session ends when intensity drops below `DRIFT_EXIT_INTENSITY = 0.18` for `DRIFT_GRACE_MS = 350` ms (so a brief feathered moment in the middle of a chain does not reset progress) or when the car stays off-track for `DRIFT_OFFTRACK_GRACE_MS = 600` ms (kissing the kerb is forgiven; sliding through grass for half a second is not). `RaceCanvas.tsx` runs the session in its existing rAF loop alongside the skid-mark gating (sharing the same `steerAbs` / `speedAbs` inputs so the SFX cue and the score stay in sync), tracks a `driftLapBest` across each lap, and emits `onLapDriftBest(score)` on lap completion. `Game.tsx` mirrors the live drift state into `HudState`, persists a per-`(slug, hash)` PB through `readLocalBestDrift` / `writeLocalBestDrift` in `src/lib/localBest.ts`, and clears live state on Restart / Restart Lap. The HUD renders a left-edge `DriftPanel` (live score with multiplier label when active, plus `BEST (LAP)` and `BEST (ALL)` mini-blocks) that pulses gold while a session is scoring; the panel is opt-out via `showDrift` in `ControlSettings` (default on, persisted via the existing `viberacer.controls` localStorage key, with the Settings pane gaining a `Drift score` toggle next to Kerbs). Tests: `tests/unit/drift.test.ts` covers intensity floors (speed / steer / maxSpeed bounds), multiplier ramp + cap, session activation + reset, grace-window hold + recovery, off-track grace expiry, chain bonus on direction flip, defensive non-finite handling, and the formatter; `tests/unit/localBest.test.ts` covers drift PB null-on-empty, namespace isolation, refusal-to-write-non-positive, malformed-payload defense, and quota-exception silence; `tests/unit/controlSettings.test.ts` covers default-on, round-trip-disabled, and legacy backfill for the new `showDrift` field.
- Per-sector PB celebration badge: live. New pure helper `compareSectorToBest(currentHit, prevHitTMs, bestSectors)` in `src/game/optimalLap.ts` returns `{cpId, durationMs, priorBestMs, isPb} | null`; `isPb` is true when the just-completed sector beats the player's prior best for that cpId OR when no prior best for that cpId exists (so the very first lap of a brand-new track flashes the badge each sector). Defensive against non-finite or non-positive durations / tMs / prevHitTMs and against a hand-edited stored row with a non-positive duration. `Game.tsx` runs the comparison from `handleCheckpointHit` ahead of the existing split / projection branch so a sector PB still flashes when there is no PB lap on file yet, tracks the running `prevHitTMsRef` (resets to 0 on lap completion, Restart, and Restart Lap), and pushes the result into `HudState.sectorPb` with a `SECTOR_PB_DISPLAY_MS = 2200` ms auto-clear timer (cleared on every fresh sector PB, on lap completion, and on Restart / Restart Lap so the badge never sticks when the player abandons the lap). The HUD renders a small "★ SECTOR PB S<n> 0.421" pill below the live split tile in the same gold palette as the OPTIMAL block and the lap-history PB chip; pop-in animation is keyed on `cpId + generatedAtMs` so back-to-back PBs through a fast section retrigger the pop instead of looking glued to the screen. Tests: `tests/unit/optimalLap.test.ts` adds 10 tests covering first-time-PB (with empty / null bestSectors), faster-than-prior PB, slower-than-prior non-PB, tied-time non-PB, non-positive duration rejection (zero / negative / equal-to-prev), non-finite tMs / prevHitTMs rejection, ignoring a stored best with a non-positive duration, cpId matching not array order, and rounding the duration to whole ms.
- Theoretical-best (OPTIMAL) lap HUD block: live. Pure helpers in `src/game/optimalLap.ts` (`computeSectorDurations(hits)` consecutive-difference math from a lap's `CheckpointHit[]`, `mergeBestSectors(prev, next)` cpId-keyed minimum merge that preserves prev order and appends only the new cpIds in next order, `optimalLapTime(sectors)` rounded sum, `hasCompleteOptimalLap(sectors, expectedSectorCount)` distinct-cpId coverage check). Storage in `src/lib/localBest.ts` (`viberacer.bestSectors.<slug>.<versionHash>` localStorage key with a zod-validated `SectorDurationsArraySchema` so a hand-edited or corrupt payload can never feed the HUD a negative or non-finite duration). The merge runs on every completed lap (not just PBs) so a single fast sector counts toward the optimal even when the rest of the lap was slow, and the optimal can be stitched together across multiple imperfect laps. `Game.tsx` mirrors the running map into `bestSectorsRef`, recomputes on lap completion, and pushes `optimalLapMs` plus `optimalLapComplete` into HudState in the same setHud call as the PB updates. The HUD's `OptimalBlock` slots into the top stat row between BEST (ALL TIME) and RECORD; a complete optimal renders gold with a soft glow, an in-progress one renders dim gold with a `--` placeholder so the player knows the slot exists but is still being built. Tests: `tests/unit/optimalLap.test.ts` (31 tests covering sector-difference math, glitched non-positive / non-finite skip, cpId-order preservation, merge null / empty cases, per-cpId minimum, append-new-only-in-order behavior, immutability of inputs, dropping-invalid-from-prev backfill from next, dropping-invalid-from-prev-and-next entirely, multi-lap stitching, sum + rounding + non-finite-null, completeness against expected count incl. exceeded / missing / invalid / zero-or-negative / duplicate-cpId / single-sector cases). `tests/unit/localBest.test.ts` adds 8 tests covering null on empty, round-trip, namespace isolation, malformed JSON / non-positive / negative-cpId / non-array rejection, and quota-failure silence.
- Wrong-way warning: live. Pure helpers in `src/game/wrongWay.ts` (`expectedTangent`, `headingToVector`, `angleBetween`, `isWrongWayInstant`, plus a debounced `updateWrongWayDetector` with `WRONG_WAY_ANGLE_THRESHOLD = 2*PI/3` (120 degrees), `WRONG_WAY_MIN_SPEED = 1.5`, `WRONG_WAY_ENTER_TICKS = 12`, `WRONG_WAY_EXIT_TICKS = 6`). `expectedTangent` walks the same per-piece geometry as `samplePieceAt` (entry-to-exit direction for straights, sign-of-sweep tangent for arcs, nearest-segment tangent for sampled S-curves) and returns the unit travel direction at the closest point on the centerline. `RaceCanvas` runs the detector once per frame: it projects the car onto its current piece, compares the velocity direction (heading times sign(speed) so reversing into the right way reads correctly) to the expected tangent, and runs the boolean through the debouncer so a sideways drift through a corner does not flicker the warning. Detector resets on full reset and on lap-restart pulses. The HUD adds a `wrongWay` flag to its update payload (mirrored into the existing throttled HUD path); when active, `HUD.tsx` renders a red, gold-bordered "<< WRONG WAY <<" banner with a soft pulse animation at the same vertical band as the OFF TRACK label (the JSX picks one or the other so the two never overlap). Tuning Lab gets a smaller `WRONG WAY` badge in `DriveHud` for the same reason. Tests: `tests/unit/wrongWay.test.ts` covers heading-to-vector for the four cardinals, angle math (parallel / opposite / perpendicular / NaN-safe / acos-clamped), straight tangent direction, corner tangent invariants (unit length, positive dot with chord at entry and exit), polyline tangent at a synthetic S-curve, instant-detection behavior (forward / reverse / below speed floor / non-finite / custom threshold), and the debouncer (enter / exit streak math, brief blip ignored, sane default threshold).
- Racing-line overlay: live. Pure helpers in `src/game/racingLine.ts` (`samplesToPolyline(samples, liftY?)` packs a Replay's `[x,z,heading]` triples into a flat `Float32Array` of XYZ vertices for a `BufferAttribute` and clamps y to a constant lift, `racingLineNeedsRebuild(prev, next)` is a reference-equality gate for the renderer rebuild, plus `RACING_LINE_LIFT_Y = 0.06` (sits above the kerb plane at y = 0.04 so the line never z-fights), `RACING_LINE_COLOR_HEX = 0x55e0ff` (cyan to match the ghost car's emissive tint so the visual language reads as "where the ghost will drive"), and `RACING_LINE_WIDTH_PX` for a future `Line2` upgrade). Renderer side is `buildRacingLineLayer()` in `src/game/sceneBuilder.ts`: a single `LineBasicMaterial` plus a per-replay `BufferGeometry` swapped through `setReplay(replay | null)`, wrapped in a `Group` that flips visibility through `setVisible` for an O(1) Settings toggle. Hidden by default so a fresh load with the toggle off costs zero draw calls; rebuilds the geometry exactly when the active replay reference changes (race load, post-PB swap, leaderboard top fetch resolve), which happens at most a handful of times per session. The layer is sourced from the same `activeGhostRef` the ghost car reads, so `ghostSource` (Auto / Top / Your PB) drives the racing line too. `RaceCanvas.tsx` gained a new `showRacingLineRef` prop polled each frame through a `syncRacingLine` helper that short-circuits on no-op (boolean equality on the cached visibility plus pointer equality on the cached replay) and only sends the active replay through `setReplay` when it actually changes; the layer stays hidden whenever no replay is loaded so an empty group never flashes on. `Game.tsx` mirrors `settings.showRacingLine` into the ref each render so a Settings flip lands on the next frame without rebuilding the renderer. Settings UI: a new Racing line section in `SettingsPane.tsx` (right under Ghost car) renders a single MenuToggle plus a hint explaining the line uses the same source as the ghost. Storage: `ControlSettings` gained a `showRacingLine: boolean` field with **default off** (opt-in coaching aid so legacy stored payloads keep their existing screen exactly as it was on upgrade), persisted through the existing `viberacer.controls` localStorage key with a `false` backfill default for legacy payloads. Tests: `tests/unit/racingLine.test.ts` (21 tests covering constant value bounds and types, the kerb-clearance invariant on `RACING_LINE_LIFT_Y`, `samplesToPolyline` null / undefined / empty / single-sample short-circuits, x and z passed through with the lift applied, custom `liftY` honored, non-finite `liftY` / x / z rejection, and the `racingLineNeedsRebuild` reference-equality gate covering same-ref, fresh-object-same-data, null-to-replay, replay-to-null, null-null, undefined-undefined, and the null vs undefined distinction). `tests/unit/controlSettings.test.ts` adds default-false, round-trip-enabled, and legacy-backfill (omitted in stored payload) coverage for the new `showRacingLine` field.
- Medal badge on BEST (ALL TIME): live. Pure helpers in `src/game/medals.ts` (`MEDAL_TIERS` ascending, `MEDAL_TIERS_DESCENDING`, `MEDAL_THRESHOLDS` ratios, `MEDAL_LABELS`, `MEDAL_COLORS`, `MEDAL_GLYPH`, `medalRank`, `isMedalUpgrade`, `medalThresholdsFor`, `medalForTime`). The player's all-time PB on a (slug, version) is compared against the route's leaderboard #1 lap time (`overallRecord.lapTimeMs`); the highest qualifying tier is platinum (at or below the record), gold (within 102%), silver (within 110%), bronze (within 125%), or null (slower than 125% or no record on file). `HUD.tsx` derives the tier inline from props it already receives (no new state, no new wiring) and renders a small pill chip flush with the BEST (ALL TIME) tile so the medal reads as a property of that lap time. The chip color matches the metal (platinum cool gray, gold warm yellow, silver cool gray-blue, bronze copper) and the label reads as plain text so emoji-free fonts and screen readers both surface the right meaning. Tests: `tests/unit/medals.test.ts` (28 tests covering tier ordering, threshold monotonicity, label / color / glyph presence, rank ordering, upgrade detection across all transitions including null and downgrades, threshold null-on-bad-target, scaling math at a 60 s target, null-on-bad-lap, platinum at and below target, gold / silver / bronze inside and at their boundaries, null when slower than 125%, and inclusive-boundary tie behavior).
- PB streak chip + per-track best: live. Pure helpers in `src/game/pbStreak.ts` (`STREAK_HUD_MIN = 2`, `incrementStreak`, `resetStreak`, `isStreakBest`, `formatStreakLabel`, `sanitizeStreak`). The streak counts CONSECUTIVE all-time PB laps in a single race session on the current (slug, versionHash). `Game.tsx::handleLapComplete` increments the live counter inside the same `setHud` closure that computes `isAllTimePb` so the new streak ships in the same render as the new PB toast; a non-PB lap calls `resetStreak()` so the counter zeroes the moment the chain breaks. The all-time best (per slug + version) lives in localStorage under `viberacer.pbStreakBest.<slug>.<versionHash>` via `readLocalBestPbStreak` / `writeLocalBestPbStreak` in `src/lib/localBest.ts`; the writer floors fractional input, refuses non-finite or non-positive values, and silently swallows quota errors so a lap completion never blows up the flow. Restart and Restart Lap zero the live counter (the all-time best is preserved on disk and stays in HudState); a slug change reloads the best from disk so a navigation between two versions surfaces the right target. The HUD renders a small gold "PB STREAK x{n}" pill flush with the BEST (ALL TIME) tile, alongside the existing medal badge, only when the live count reaches `STREAK_HUD_MIN = 2` so a single first-PB lap stays celebrated by the existing toast / fanfare / confetti without doubling up the chip. The pause-menu Stats pane gains a "Best PB streak" tile (in the same 2x3 grid as Total laps / Sessions / Time on track / Avg lap / Best lap / Last played) showing the all-time best as `x{n}` plus a small secondary line "now: x{m}" so the player can see how close they are to beating their record while paused. Tests: `tests/unit/pbStreak.test.ts` (31 tests covering `STREAK_HUD_MIN >= 2`, `incrementStreak` zero / positive / chained / non-mutating / fractional-floor / non-finite-collapse-to-1 / negative-collapse-to-1 cases, `resetStreak` always zero, `isStreakBest` fresh / beats-prior / tie / below-prior / current-zero / non-finite / negative / non-finite-prior, `formatStreakLabel` below / at / above threshold, fractional floor, non-finite, negative, no em-dash invariant, `sanitizeStreak` non-number / non-finite / non-positive / fractional / passthrough, plus a chained-lifecycle test that verifies the PB-then-non-PB reset, the rebuild-after-reset path, and the best-only-advances-on-exceed invariant). `tests/unit/localBest.test.ts` adds 9 tests covering the new streak storage helpers (null on empty, round-trip, namespace isolation per slug + hash, fractional floor, non-finite refusal, non-positive refusal, hand-edited non-finite / non-positive defense, and quota-exception silence).

### Start signal

- Three-step traffic light: READY (red), SET (amber), GO (green). One lamp lit per step.
- Synth beep per step. Final beep is a higher pitch on GO.
- Input ignored until GO.

### Lap detection

- Invisible finish-line plane spans the start piece.
- `N` checkpoint planes distributed around the loop. Configurable per track (default: one per piece edge that lies on the shortest path).
- A lap counts when all checkpoints are hit in order AND the finish line is crossed.
- Out-of-order or missed checkpoints invalidate the lap. The timer resets at the next finish-line crossing.

### HUD

- Current lap time (big, top center).
- Best lap this session (smaller, beside current).
- Best-ever for this track version (smaller, persistent; from KV). A small medal chip (Bronze, Silver, Gold, Platinum) sits beside it when the player's PB lands within 125% / 110% / 102% / 100% of the route's leaderboard #1 lap time. A small gold "PB STREAK x{n}" pill joins them when the player has chained at least two consecutive PB laps in the current session.
- Lap counter.
- Live split delta vs local PB (color-coded, pops in at each checkpoint, see below).
- Projected final lap time (color-coded, refreshed at each checkpoint, see below).
- Theoretical-best lap (`OPTIMAL`): sum of the player's best ever per-sector durations on this slug + version. Tints gold when every sector has been covered, dims while still building coverage.
- Pause button (always visible on touch devices).
- Minimap (bottom-right, top-down, toggleable in Settings).
- Speedometer (bottom-center, swept needle plus numeric readout, with a session top-speed marker tick on the dial and a PEAK sub-readout, toggleable in Settings).
- Rear-view mirror (top-center inset, second WebGL pass into a small canvas, toggleable in Settings).
- Drift score panel (left-edge, live score with multiplier when active plus per-lap and all-time bests, toggleable in Settings).
- Racing-line overlay (a thin cyan polyline floating just above the asphalt that traces the same recorded lap as the ghost car, opt-in via Settings as a coaching aid).
- Tire smoke puffs (soft white puffs that pop off the rear wheels during hard slides and braking and rise off the asphalt, toggleable in Settings).

### Minimap

A small top-down minimap card sits in the bottom-right corner of the screen during the race. It shows the full track outline plus a yellow arrow for the player car (rotated to match heading) and, when the ghost is enabled, a teal dot for the ghost. The card is opt-out and toggleable from the Settings pane (`showMinimap: boolean` in `ControlSettings`, default on, persisted via the existing localStorage key `viberacer.controls`).

Geometry helpers live in `src/game/minimap.ts` (`buildMinimapGeometry(path, viewSize?, padding?)`, `computeBounds`). The helper walks every `OrderedPiece`, extracts the centerline (entry/exit segment for straights, a 12-segment polyline for corners, the precomputed S-curve samples for sampled pieces), pads the bounds by half the track width, and returns one SVG path string per piece plus a `worldToView(x, z)` projector that preserves aspect ratio. SVG +Y goes downward and world +Z is south, so the projector maps Z directly onto Y without a flip and north (smaller Z) renders above south (larger Z).

`src/components/Minimap.tsx` renders the static SVG once per `pieces` change. The car and ghost markers update via direct DOM mutation in a small rAF loop driven by two pose refs (`carPoseRef`, `ghostPoseRef`) so a 60 Hz position update never triggers a React re-render. `RaceCanvas.tsx` writes the live pose into `carPoseOutRef` and `ghostPoseOutRef` every frame; `Game.tsx` owns the refs and only mounts `<Minimap />` when `settings.showMinimap` is true. Tests live in `tests/unit/minimap.test.ts` (per-piece path strings, in-bounds projection, north-vs-south orientation, view-size respect, aspect ratio centering, empty-path rejection) and `tests/unit/controlSettings.test.ts` (default value, round-trip, legacy backfill).

### Skid marks

Dark tire stripes appear on the road behind the rear wheels whenever the player slides hard or drives off-track. Marks fade to nothing over a few seconds so a long race never accumulates an unbounded streak. Toggleable from Settings (`showSkidMarks: boolean` in `ControlSettings`, default on, persisted via the existing `viberacer.controls` localStorage key); turning the toggle off lets any marks already on the ground finish their fade rather than snapping them away mid-corner.

Pure helpers live in `src/game/skidMarks.ts`. `shouldSpawnSkidMark(intensity, speedAbs, msSinceLastSpawn)` gates spawns on a minimum slide intensity (`SKID_SPAWN_MIN_INTENSITY = 0.35`), a minimum speed (`SKID_SPAWN_MIN_SPEED = 4`), and a fixed cadence (`SKID_SPAWN_INTERVAL_MS = 100`). `skidMarkAlpha(ageMs, peak)` linearly fades each mark from its peak alpha at age 0 to 0 at `SKID_MARK_FADE_MS = 4500`. `skidMarkPeakAlpha(intensity)` scales the peak by the slide's intensity up to `SKID_MARK_BASE_ALPHA = 0.85`, so weak slides leave fainter marks. `nextSkidMarkIndex` advances the ring-buffer write pointer.

`buildSkidMarkLayer(poolSize)` in `src/game/sceneBuilder.ts` owns the renderer side: a `Group` of `SKID_MARK_POOL_SIZE = 220` pre-allocated `MeshBasicMaterial` quads sharing one `PlaneGeometry`. Each spawn places two stripes (one per rear wheel) offset perpendicular to the heading by `SKID_MARK_REAR_OFFSET = 1.05` and back along `SKID_MARK_REAR_BACK = 1.4`. `tick(nowMs)` updates per-mark opacity each frame; `clear()` resets the pool on a Restart so a new lap starts on a clean track. The layer's materials and geometry are released through the bundle's existing scene-traversal `dispose` so there is no double-dispose path.

`RaceCanvas.tsx` reads the existing `skidIntensity` heuristic from `audio.ts` to feed the spawn decision and polls `showSkidMarksRef` each frame so a Settings flip lands without rebuilding the renderer. Tests: `tests/unit/skidMarks.test.ts` covers spawn gating (intensity floor, speed floor, interval debounce, custom interval), alpha ramp (peak at age 0, zero at fade end, midpoint linearity, peak clamping, custom fade), peak alpha scaling, and ring-buffer wrap including a custom pool size and the defensive zero-pool case. Toggle coverage in `tests/unit/controlSettings.test.ts` (default, round-trip, legacy backfill).

### Tire smoke

Soft white puffs pop off the rear wheels during hard slides and braking, then rise off the asphalt and fade in under a second. Visually distinct from the dark skid mark trail (which sits flat on the road and persists for seconds): smoke puffs are lighter, volumetric, and gone quickly so a long slide reads as a steady cloud rather than an opaque smear. The two systems share the same per-frame inputs and the same paired rear-wheel placement, so a hard cornering moment naturally lays both stripes and clouds at the same beat. Toggleable from Settings (`showTireSmoke: boolean` in `ControlSettings`, default on, persisted via the existing `viberacer.controls` localStorage key); turning the toggle off lets any puffs already in the air finish their fade rather than snapping them away mid-corner.

Pure helpers live in `src/game/tireSmoke.ts`. `puffIntensity(speedAbs, maxSpeed, steerAbs, brakeInput, onTrack)` combines the slide cue (steering deflection scaled by speed ratio, same shape as `skidIntensity`) with a separate brake cue and a small off-track boost so a hard straight-line stop puffs even with no steering input. `shouldSpawnTireSmoke(intensity, speedAbs, msSinceLastSpawn)` gates spawns on a minimum puff intensity (`TIRE_SMOKE_MIN_INTENSITY = 0.32`), a minimum speed (`TIRE_SMOKE_MIN_SPEED = 3`, slightly lower than the skid floor so a hard low-speed brake puffs without leaving permanent black streaks), and a fixed cadence (`TIRE_SMOKE_SPAWN_INTERVAL_MS = 130`, slightly slower than skids so the puffs read as discrete blobs). `puffAlpha(ageMs, peak)` linearly fades each puff from its peak alpha at age 0 to 0 at `TIRE_SMOKE_FADE_MS = 900`. `puffScale(ageMs)` linearly grows each puff from `TIRE_SMOKE_START_SCALE = 0.7` to `TIRE_SMOKE_END_SCALE = 2.6` so the cloud blooms outward as it dissipates. `puffRise(ageMs)` advances the puff upward at `TIRE_SMOKE_RISE_SPEED = 1.6` u/s. `puffPeakAlpha(intensity)` scales the peak by intensity up to `TIRE_SMOKE_BASE_ALPHA = 0.7`. `nextTireSmokeIndex` advances the ring-buffer write pointer.

`buildTireSmokeLayer(poolSize)` in `src/game/sceneBuilder.ts` owns the renderer side: a `Group` of `TIRE_SMOKE_POOL_SIZE = 60` pre-allocated `Sprite` instances, each with its own `SpriteMaterial` (so per-puff opacity animates independently) backed by a single procedurally-generated 32x32 alpha-feathered RGBA `DataTexture` (a soft cosine falloff so each sprite reads as a fluffy round cloud rather than a hard square; no binary asset ships) shared across every sprite in the pool. Sprites are camera-facing by construction so the puff reads as volumetric from any angle without per-frame billboard math. Each spawn places two puffs (one per rear wheel) offset perpendicular to the heading and back along the chassis, identical math to the skid-mark layer so the stripes and the puffs land in the same world position. `tick(nowMs)` updates each active puff's opacity, scale, and Y-position every frame; `clear()` resets the pool on a Restart so a fresh race starts on a clean scene. The shared sprite texture is freed through the layer's own `dispose()` (called from the bundle's dispose path) since the scene-traversal materials sweep does not free GPU texture memory.

`RaceCanvas.tsx` runs the tire smoke decision alongside the skid mark decision (separate gates, since the brake-only case should puff even when the skid layer says no), uses the same `brakingNow` predicate that drives the brake light cue so the visual systems stay in sync, and polls `showTireSmokeRef` each frame so a Settings flip lands on the next frame without rebuilding the renderer. Tests: `tests/unit/tireSmoke.test.ts` (54 tests covering constant bounds (positive cadence, fade window strictly shorter than the skid trail, start scale less than end scale, positive rise speed, base alpha and brake weight strictly inside (0, 1], even pool size), `puffIntensity` zero-stationary / scales-with-speed / brake-contribution / off-track-boost / clamping / non-finite defenses / non-positive maxSpeed defense / negative speed defense / negative-input clamping, `shouldSpawnTireSmoke` intensity-gate / speed-gate / interval-gate / pass-when-all-three-clear / custom-interval honored, `puffAlpha` peak-at-zero / zero-at-fade / zero-past-fade / midpoint linearity / peak clamping above 1 and below 0 / non-finite age returns peak / non-finite or zero fadeMs returns zero / custom fadeMs honored, `puffScale` start-at-zero-age / end-at-fade-end / linear-midpoint / clamps-before-zero-and-past-fade / custom-start-and-end honored / non-finite age, `puffRise` zero-at-zero / linear with age / custom rise speed / non-finite age / non-positive rise speed defenses, `puffPeakAlpha` zero-at-zero-intensity / BASE_ALPHA-at-full / clamping above and below / linear scaling, and `nextTireSmokeIndex` advance / wrap / default pool size / degenerate-pool-size defenses). Toggle coverage in `tests/unit/controlSettings.test.ts` (default-true, round-trip-disabled, and legacy backfill for the new `showTireSmoke` field).

### Speedometer

A bottom-center HUD card shows the player's live speed as a swept needle plus a large numeric readout. The dial sweeps from zero on the left to the player's current tuning `maxSpeed` on the right, with a hot-zone tint near the top end. A small REV badge fades in when the car is moving in reverse so the readout (which always shows the magnitude) stays unambiguous. The card is opt-out and toggleable from the Settings pane (`showSpeedometer: boolean` in `ControlSettings`, default on, persisted via the existing `viberacer.controls` localStorage key). The chosen unit (`speedUnit: 'mph' | 'kmh' | 'us'`, default `mph`) sits next to the toggle and applies live without restarting the race.

Pure helpers live in `src/lib/speedometer.ts` (`SPEED_UNITS`, `SpeedUnitSchema`, `convertSpeed`, `formatSpeed`, `unitLabel`, `speedFraction`, plus `MPS_TO_MPH` and `MPS_TO_KMH` constants). The integrator's world units are treated as meters for display so the stock `maxSpeed = 26` reads as ~58 mph or ~94 km/h. Negative speeds (reverse) collapse to their absolute value for the numeric readout and clamp to zero for the gauge needle. `formatSpeed` rounds mph and km/h to whole numbers and prints raw u/s with one decimal so the tuning unit stays useful.

`src/components/Speedometer.tsx` owns its own rAF loop and writes the SVG needle's `transform` attribute and the readout's `textContent` directly so the 60 Hz update never sends React re-renders into the rest of the HUD tree. `RaceCanvas.tsx` writes the live signed speed into `speedOutRef` every frame; `Game.tsx` owns the ref alongside a `maxSpeedRef` mirroring `tuning.maxSpeed` so a slider tweak in TuningPanel reshapes the dial range immediately. The Speedometer is only mounted during the `racing` phase and unmounts on pause so it does not collide with the pause overlay. Tests: `tests/unit/speedometer.test.ts` covers unit enumeration, schema validation, conversion constants, format rounding, reverse magnitude, defensive non-finite handling, and `speedFraction` clamping with bad max-speed inputs. Toggle and unit coverage in `tests/unit/controlSettings.test.ts` (default, round-trip, legacy backfill, malformed-unit rejection).

Top-speed marker on the speedometer: live. The dial draws a small green tick on the arc at the player's session-best speed, and a `PEAK <value> <unit>` sub-readout sits below the live numeric readout in the same green palette. Both are hidden until the tracker has recorded a positive peak so a brand-new race never shows a 0 marker glued to the left edge of the dial. The tick reuses the same rotation math as the needle so the spoke sits exactly on the arc at any maxSpeed, and the readout reuses `formatSpeed` so the units track the live readout (a flip from MPH to KM/H in Settings updates the PEAK label on the next frame). Pure helpers in `src/lib/speedometer.ts` (`updateTopSpeed(prevTopUs, currentRawUs)` advances strictly, treats reverse as its absolute speed, and is defensive against non-finite inputs and a corrupt prior peak; `topSpeedFraction(topUs, maxSpeed)` mirrors `speedFraction` for the tick position). `Game.tsx` owns a `topSpeedRef` so the peak survives the Speedometer's mount / unmount cycle on pause and resume; the Speedometer's existing rAF loop calls `updateTopSpeed(topUs, raw)` each frame and writes back through the ref when `becameTop` fires. A full Restart zeroes the ref so the new run starts on a clean dial; Restart Lap intentionally keeps the running peak so the player chasing a hot lap does not lose their best straight. `ControlSettings` gained a `showTopSpeedMarker: boolean` field (default on, persisted via the existing `viberacer.controls` localStorage key with a backfill default for legacy payloads); the underlying tracker keeps running when the toggle is off so a flip back mid-session immediately surfaces the current peak instead of silently zeroing it. Settings UI: a `Top-speed marker` MenuToggle row sits inside the existing Speedometer subsection (right under the unit picker), disabled when the speedometer itself is hidden. Tests: `tests/unit/speedometer.test.ts` adds 11 tests covering `updateTopSpeed` first-frame seed, strict-advance vs ties / slowdowns, reverse-magnitude collapse, non-finite live-frame defense, prior-peak repair (NaN / Infinity / negative / zero), and a multi-frame chained-lifecycle test; plus `topSpeedFraction` zero-on-no-peak, dial-range mirror, and bad-input defenses (NaN / Infinity / negative peak / zero / negative / NaN maxSpeed). `tests/unit/controlSettings.test.ts` adds 3 tests covering the default-true `showTopSpeedMarker`, round-trip-disabled, and legacy backfill (omitted in stored payload).

### Rear-view mirror

A small panoramic inset at the top-center of the viewport renders the same 3D scene from a backward-facing camera so the player can see the ghost car (or anything else) behind them while racing. Toggleable from the Settings pane (`showRearview: boolean` in `ControlSettings`, default on, persisted via the existing `viberacer.controls` localStorage key); the canvas stays mounted with `display: none` when off so a flip back resumes the pass without rebuilding the second WebGL context.

`Game.tsx` owns a stable `rearviewCanvasRef` that survives pause / resume and a `showRearviewRef` that mirrors the Settings toggle into the rAF loop. `RaceCanvas.tsx` lazily creates a second `WebGLRenderer` bound to the parent's canvas the first frame the ref resolves; the rear pass renders the same `Scene` (track, car, ghost, skid marks, lighting) through a separate `PerspectiveCamera` placed `1.2` units in front of the car at height `2.4`, looking back along the negative heading axis. A per-frame `syncRearSize` short-circuits when the canvas size has not changed so the renderer's `setSize` is a no-op when the inset CSS is stable. The pass itself short-circuits whenever `showRearviewRef.current` is false so a hidden mirror costs nothing per frame. The renderer is disposed on unmount alongside the main renderer.

Layout: the inset is positioned `top: 8`, centered horizontally, sized `min(280px, 38vw) by min(70px, 9.5vw)` so it scales cleanly from phones to desktop. Higher `zIndex: 12` than the HUD (10) so any HUD blocks that wrap into the same band sit underneath the mirror. The canvas is only `display: block` during the `racing` phase and outside of pause so it never collides with the pause overlay or the countdown banner. Coverage in `tests/unit/controlSettings.test.ts` (default value, round-trip, legacy backfill).

### Live split delta vs PB

Each time the player crosses an in-lap checkpoint, a small color-coded tile pops in just below the top stat row showing how the current lap compares to the player's local PB at that exact checkpoint. Negative deltas (ahead of PB) render in green; positive deltas (behind) render in red. The tile reads `vs PB` plus a signed `+/-S.mmm` value (e.g. `-0.421`, `+1.034`), pops in with a brief scale animation per cpId, and fades after 3.5 seconds. It also resets between laps so a new lap starts on a clean slate.

Pure helpers live in `src/game/splits.ts` (`computeSplitDeltaForLastHit`, `formatSplitDelta`, `isSplitExpired`, `SPLIT_DISPLAY_MS`). The PB reference splits are stored alongside the local PB lap time in `localStorage` under `viberacer.splits.<slug>.<hash>` and round-trip through `readLocalBestSplits` / `writeLocalBestSplits` in `src/lib/localBest.ts`. They are written every time the lap-complete branch of `Game.tsx` records a fresh all-time PB, so the next lap immediately compares against the freshest reference.

`RaceCanvas` exposes a new optional `onCheckpointHit(hit)` prop that fires once per in-lap checkpoint (the lap-complete checkpoint stays on the existing `onLapComplete` channel because it carries the full lap info). `Game.tsx` consumes the callback, computes the delta, and pushes it through React state into the HUD. Display state (`splitDelta` on `HudState`) is cleared on `restart()` and on every lap completion.

Tests: `tests/unit/splits.test.ts` covers null-PB and empty-current short-circuits, ahead / behind / exact deltas, the multi-hit "use the last entry" case, missing-cpId fallback, formatter sign and zero-pad, NaN / Infinity defensiveness, the 999.5 ms millis-rollover case, and the display window in `isSplitExpired`. `tests/unit/localBest.test.ts` covers null-on-empty, namespacing per slug + hash, malformed-JSON rejection, schema rejection, and quota-exception silence on writes.

### Projected lap time

A small `PROJECTED` block sits in the top stat row, immediately to the right of `CURRENT`, and shows where the in-flight lap is heading based on the player's pace at the most recent checkpoint. The block renders a formatted `mm:ss.mmm` lap time with a signed `+/-S.mmm` delta line beneath it. The numerals tint green when the projection is ahead of PB, red when behind, and gold when exactly matching. The block is hidden until the player has both a recorded PB lap and at least one matching checkpoint of the current lap, so a fresh page load (no PB) and the first sector after restart never show stale numbers.

The projection is intentionally checkpoint-paced (not per-frame): it is recomputed only when the player crosses an in-lap checkpoint, then persists in `HudState.prediction` until the next checkpoint, lap completion, or restart. This keeps the value calm and readable instead of jittering with every meter of road.

Pure helper `predictLapTimeFromHits(currentHits, pbHits, pbLapMs, recordLapMs?)` lives in `src/game/splits.ts` and returns `{ predictedMs, deltaMs, deltaVsRecordMs, cpId } | null`. The formula is `predictedMs = max(0, round(pbLapMs + lastDelta))` where `lastDelta = currentHit.tMs - matchingPbHit.tMs`. The clamp guards against an implausible negative projection if a hit's `tMs` ever drifts wildly. `Game.tsx` calls the helper from inside `handleCheckpointHit`, mirrors the all-time PB lap time into a `pbLapMsRef` (kept fresh on mount + after every PB-class lap completion) so the closure never reads a stale value, and clears `HudState.prediction` from `restart()`, `armLapReset()`, and `handleLapComplete()`.

The HUD's `PredictionBlock` reuses `formatSplitDelta` for the delta line so the sign + 3-decimal-second formatting stays consistent with the existing live split tile. The block is keyed on `cpId` so a fresh checkpoint cross retriggers React's mount path (the existing top-row stat blocks are unaffected). The block now renders two captioned delta lines: a primary "vs PB" delta (green ahead, red behind, gold tied), and a smaller secondary "vs REC" delta when the track-wide RECORD lap time is on file. The "vs REC" delta is computed as `predictedMs - round(recordLapMs)`, mirrored from `hud.overallRecord.lapTimeMs` into a `recordLapMsRef` that gets the same optimistic post-record swap as `pbLapMsRef` so the next checkpoint of a fresh lap projects against the freshest baseline.

Tests: `tests/unit/splits.test.ts` covers null-PB / null-PB-lap / empty-hits short-circuits, defensive bogus PB lap rejection (zero, negative, NaN, Infinity), missing cpId in PB, ahead / behind / exact projections, multi-hit latest-entry selection, the negative-projection clamp, the rounding case for fractional PB lap inputs, the omit-record-delta-when-no-record case, ahead / behind / tied record deltas, defensive bogus record lap rejection (zero, negative, NaN, Infinity), and the rounding case for fractional record lap inputs.

### Endless loop

No lap cap. The player keeps racing until they pause and exit. Every completed lap tries to submit.

### Ghost car

A translucent cyan ghost car races alongside the player, replaying the path of the leaderboard's fastest known lap (or the player's own personal best, once they have one). The ghost is purely visual: there is no collision with the player, no input from it, and it never affects physics or anti-cheat. It is toggleable on or off in Settings (`showGhost: boolean` in `ControlSettings`, default on, persisted via the existing localStorage key `viberacer.controls`).

Each lap, `RaceCanvas` samples the player's `(x, z, heading)` at a fixed 30 Hz cadence into an in-memory buffer. On `LapCompleteEvent` the buffer is flushed as a `Replay` (`{lapTimeMs, samples: [x, z, heading][]}`) and bundled into the next `/api/race/submit` POST. The server stores it under `lap:replay:<nonce>` and updates `track:<slug>:<hash>:topReplay` to point at the new nonce when the lap takes rank 1. As a one-time bootstrap, if `topReplayPointer` is empty (the existing #1 predates this feature and has no recorded replay), the next submission with a replay is promoted regardless of rank so a ghost appears immediately rather than waiting for someone to beat the legacy time.

On race load `GameSession` resolves the active replay in priority order: local PB replay (`viberacer.replay.<slug>.<hash>` in localStorage) first, then `GET /api/replay/top` for the leaderboard top. The active replay is stored in `activeGhostRef` and consulted each frame; ghost pose is computed by interpolating the samples at `t = nowMs - state.raceStartMs`. Because `tick.ts` resets `raceStartMs` on every finish-line crossing, the ghost automatically restarts from `t=0` in lock-step with the player's lap timer.

When the player completes a lap that beats their previous local PB, the buffered replay becomes the new local PB replay and immediately replaces `activeGhostRef.current`, so the next lap is run against the player's own freshly-recorded path.

Ghost rendering reuses the player car's GLB through `buildGhostCar()` in `sceneBuilder.ts`, which clones the model and overrides every material with a translucent emissive cyan (`opacity 0.45, depthWrite: false`). The ghost mesh is added to the same scene as the player, rendered after the track and the player car so it draws on top in case of overlap.

Ghost-source picker: live. `src/lib/ghostSource.ts` defines `GHOST_SOURCES = ['auto', 'top', 'pb']` plus `GhostSourceSchema`, friendly labels and descriptions, the `isGhostSource` guard, and three pure helpers (`pickGhostReplay(source, localPb, top)` for race-load resolution, `pickGhostAfterPb(source, newLocalPb, prevActive)` for the post-PB swap, `ghostSourceNeedsTopFetch(source)` to skip the leaderboard-top network call when the player chose `pb`). `ControlSettings` gained a `ghostSource: GhostSource` field with default `'auto'` (matches the legacy resolution exactly so users who never open Settings see no change), persisted in the existing `viberacer.controls` localStorage key with a backfill default for legacy payloads. `Game.tsx` reads `settings.ghostSource` at race-load to pick the active replay (auto = local PB if present, else leaderboard top; top = always the leaderboard top, even when the player has a faster PB; pb = local PB only, no fallback) and mirrors the value into `ghostSourceRef` so `handleLapComplete` can honor the player's choice on the post-PB swap (top keeps chasing the leaderboard #1 even after a personal best, auto and pb swap to the freshly recorded PB so the next lap chases the player's own best path). The Settings pane's Ghost-car section now renders a four-way picker (Off / Auto / Top time / Your PB); Off sets `showGhost: false`, the other three set `showGhost: true` plus the chosen source. The descriptive sub-hint under the picker swaps to whichever option is active so the player always sees plain-language confirmation of what is about to race them. Tests: `tests/unit/ghostSource.test.ts` (21 tests covering enum membership, label / description uniqueness, schema accept / reject, the `isGhostSource` guard, `pickGhostReplay` across all three sources at every (PB present?, top present?) combination, `pickGhostAfterPb` keeping vs swapping per source, and the top-fetch skip for `pb`). `tests/unit/controlSettings.test.ts` adds default `'auto'`, round-trip of `'top'` and `'pb'`, legacy backfill when the key is omitted, and reject-and-default on an unknown stored source.

---

## 9. Title Screen, Menu, and Pause

**Status.** Partial. Pause menu is live with Resume, Restart, Leaderboards (button present, no-op wired intentionally until §11 ships), and Exit to title. Title screen at `/` is live: a Fredoka-wordmark logo over a Three.js background loop (a car auto-driving the default oval track with a slow orbiting camera), with Play / Load existing track / Settings (disabled stub). Settings pane itself is not yet wired.

### Build log

- `src/components/PauseMenu.tsx` is a dark overlay card with six buttons (Resume highlighted as primary, Restart, Edit Track, Leaderboards, Settings, Exit to title) plus a small "Esc to resume" hint. Pure presentational, no state of its own.
- `src/components/Game.tsx` owns the pause lifecycle: Esc key while `phase === 'racing'` calls `pause()`; clicking the bottom-left pause button does the same. Both `pause()` and `resume()` set `pausedRef.current` synchronously so the RAF loop picks up the state change on the very next frame.
- Pause freezes simulation without drift: on pause, `pauseStartTsRef.current = performance.now()`; on resume, `resumeShiftRef.current += performance.now() - pauseStartTsRef.current`. The loop drains the shift by adding it to `state.raceStartMs` so the lap timer resumes where it left off.
- Restart replays the countdown: `restart()` sets `pendingResetRef.current = true`, clears the token, resets session HUD state, and flips `phase` back to `'countdown'`. The Countdown component remounts and the READY-SET-GO sequence plays again. All-time PB in `localStorage` is preserved.
- Exit to title uses Next.js's `useRouter().push('/')`.
- Leaderboards button: toggles a sub-view inside the paused overlay. `pauseView: 'menu' | 'leaderboard'` in `Game.tsx` drives which component renders. Leaderboard has a Back button that returns to the menu. Reopening pause always starts on `'menu'`.
- Edit Track button: wired to `router.push('/<slug>/edit')`. See Section 6 for the editor UI.
- Title screen at `/`: server component in `src/app/page.tsx`. `next/font/google` loads Fredoka as the cartoony wordmark font (CSS var `--font-cartoony`). `src/components/TitleBackground.tsx` mounts a full-viewport canvas behind the menu that reuses `buildTrackPath` + `buildScene` to render the default oval track, then drives a car along the centerline (straights interpolate entry→exit, corners sample the arc at radius `CELL_SIZE/2`) with a slow camera orbit. Menu is Play (links to `/start`), Load existing track (the `RecentTrackList` of the latest-updated slugs, falling back to sample slugs), and a Settings button that opens the Settings pane in a modal via `src/components/SettingsLauncher.tsx`.
- Settings pane: `src/components/SettingsPane.tsx` ships keyboard remap (with a click-then-press capture flow per slot, modifier keys ignored, conflict transfer to the new action) plus a dual / single touch mode toggle. Reachable from both the title screen and the pause menu. See Section 4 build log for storage and detection details.
- Share button: pause menu entry between Settings and Exit. `Game.tsx` builds a `SharePayload` from the current slug, version hash, local PB, and overall record (helpers in `src/lib/share.ts`), then calls `shareOrCopy` which prefers `navigator.share` (mobile share sheet) and falls back to `navigator.clipboard.writeText` on desktop. The shared URL is `${origin}/${slug}?v=${versionHash}` so recipients always race the exact version the sharer was on. The button label flips to "Link copied!" / "Shared!" / "Could not share" for 1.6s, then back to "Share track". Pure helpers (`buildShareUrl`, `buildShareText`, `buildSharePayload`, `shareOrCopy`) are unit-tested in `tests/unit/share.test.ts`.
- Touch pause button sizing: live. `Game.tsx` now ships the always-visible pause button with a `<style>` block (`PAUSE_BUTTON_CSS`) instead of inline styles so a `(any-pointer: coarse)` media query can grow the hit target from 48x48 to 64x64, bump the SVG glyph from 22 to 30, thicken the ring, and add `env(safe-area-inset-bottom)` to the bottom offset so it clears the iOS home indicator. `touch-action: manipulation` removes the legacy 300ms tap delay and `-webkit-tap-highlight-color: transparent` hides the gray flash on tap. Fine pointers keep the original compact look.
- Lap history pane: live. New pause-menu entry "Laps" (with a parenthesized count when at least one lap has been logged) opens an in-overlay panel listing every completed lap of the current session newest-first. Each row shows the lap number, the formatted time, an orange PB chip when the lap matched or beat the prior local PB at completion, and a signed delta vs the prior PB (green for ahead, red for behind, dim placeholder when no PB existed yet). The current local PB row gets a subtle accent border so it stays easy to spot in a long list. A summary header above the list shows session count, best, and average. The list resets on Restart; the local PB on disk is unaffected. Pure helpers (`appendLap`, `makeLapEntry`, `formatLapDelta`, `summarizeHistory`) live in `src/game/lapHistory.ts`; the panel itself is `src/components/LapHistory.tsx`. Unit coverage in `tests/unit/lapHistory.test.ts` (15 tests covering null-PB short-circuits, PB tie + faster + slower paths, append immutability, multi-lap chaining, signed-delta formatting incl. the 999.5 ms millis rollover, summary across empty / single / multi-entry inputs).
- Per-lap sector breakdown in the Laps pane: live. `LapHistoryEntry` gained an optional `sectors: SectorDuration[]` field; `appendLap` / `makeLapEntry` accept the just-completed lap's per-sector durations (computed from `event.hits` via `computeSectorDurations` in `Game.tsx::handleLapComplete`, which already runs the same calculation for the OPTIMAL block). The helper makes a defensive copy so a caller's mutable buffer never leaks into the history array, and filters out non-finite or non-positive durations so the breakdown UI never has to render garbage rows. `LapHistory.tsx` makes each row a click-to-expand button (the disclosure caret rotates on toggle, ARIA `aria-expanded` mirrors the state) that reveals a per-sector chip strip beneath the row. Each chip shows `S<n>`, the sector duration formatted as `S.mmm`, and a signed delta (`formatLapDelta`) vs the player's all-time best for that cpId; the delta tints gold when the lap matched or beat the all-time best (delta <= 0) and red otherwise. A small star marker sits next to the `S<n>` label when the chip's duration matches the all-time best for that cpId; otherwise a green dot marks the session-best for that cpId. Multi-select expansion by design so the player can compare two laps' sector tables side by side. The `bestSectors` snapshot is sourced from `bestSectorsRef.current` (the same map that feeds the OPTIMAL block); the session-best lookup is built once per render via `useMemo` over the entries themselves so the `S` chips stay coherent without an extra round-trip to disk. Tests: `tests/unit/lapHistory.test.ts` adds 3 tests covering sector pass-through, the non-finite / non-positive filter, and the defensive-copy invariant; existing tests updated for the new required `sectors` field on `LapHistoryEntry`.
- Lap-time trend chart: live. The Laps pane now renders a small SVG line chart above the lap list once the session has logged 3 or more laps (sub-3 reads as a stray dot or a lone slope, both low signal). Pure helpers in `src/game/lapChart.ts` (`buildLapChartGeometry(history, {width, height, padY?})` returns per-point `(x, y)` SVG-space coords plus `bestY`, `averageY`, `slowestMs`, `fastestMs`; `pointsToPolyline(geom)` formats the polyline `points` attribute string). Coordinate convention is SVG-style (origin top-left, +y down) but inverted in domain so the FASTEST lap sits at the top of the inner band and the SLOWEST at the bottom: racers read "higher is better" at a glance. The chart layers four references in z-order: a dashed muted average line, a dashed amber PB line (only drawn when the all-time PB sits inside the session's visible y-range so it never floats unanchored at the edge), a dashed green session-best line, and the orange lap-time polyline on top. Per-lap markers are small accent circles, with PB ticks rendered as larger gold dots with a dark stroke so they pop against the polyline; each circle carries a `<title>` for native tooltip-on-hover (`"Lap N: mm:ss.mmm"`). `LapHistory.tsx` builds the geometry once per history change via `useMemo`. Defensive: non-finite or non-positive lap times are filtered before any domain math so a corrupt entry can never poison the geometry. Tests: `tests/unit/lapChart.test.ts` (17 tests covering empty / non-positive / non-finite dimension short-circuits, invalid-entry filtering before stride math, single-entry centered placement, even horizontal spread, fastest-at-top / slowest-at-bottom mapping, average-between-bounds for asymmetric spreads, mid-band collapse on every-lap-the-same, entry-reference passthrough, default `padY` of 6, and polyline formatting incl. two-decimal rounding).
- How to play overlay: live. New pause-menu entry "How to play" (between Settings and Share track) and a matching button on the title screen (under Settings) open an overlay reference for new players. Pure helpers live in `src/lib/howToPlay.ts` (`buildKeyboardHelpRows(bindings)`, `buildGamepadHelpRows(bindings)`, `buildTouchHelp(mode)`, plus the `HOW_TO_PLAY_KEYBOARD_ORDER` / `HOW_TO_PLAY_GAMEPAD_ORDER` display orders, the `HOW_TO_PLAY_GOAL_TITLE` / `HOW_TO_PLAY_GOAL_BODY` copy, and the static `HOW_TO_PLAY_TIPS` bullet list). Keyboard and gamepad rows are derived live from the player's current `KeyBindings` / `GamepadBindings` so a remap in Settings is immediately reflected; codes route through `formatKeyCode` / `formatGamepadButton` so a row reads like a keycap chip ("W", "Up arrow", "RT", "A / Cross") rather than a raw `KeyW` code. Empty per-action arrays surface "not bound" rather than a blank cell. Touch help swaps between single-stick and dual-stick blurbs based on `settings.touchMode`. Pro tips bullet the non-obvious mechanics (handbrake drift, restart-lap key, ghost car, racing-line overlay, time-of-day / weather customization, Setup tuning carrying across leaderboard entries) so a returning player who skipped Settings still discovers them. The component (`src/components/HowToPlay.tsx`) reuses the shared MenuUI primitives (overlay, panel, sections, header, button) so the visual language matches every other paused overlay; `src/components/HowToPlayLauncher.tsx` is the title-screen entry point that mirrors `SettingsLauncher`. Game.tsx wires it through a new `'howToPlay'` `PauseView` and hands the live `keyBindings`, `gamepadBindings`, and `touchMode` from `useControlSettings` straight into the overlay so race-time edits land without remount. Tests: `tests/unit/howToPlay.test.ts` (21 tests covering display-order coverage of every action, default key formatting (W / Up arrow / Space / R), remap reflection, empty-binding handling, blank-string filter, missing-action-array fallback, gamepad index-to-glyph translation including default RT / A / Start, defensive negative / NaN filter, single vs dual touch blurb distinctness, presence-of-pause-button bullet in both modes, non-empty static copy, three-or-more pro tips, the no-em-dash invariant across every static string, and the `continuousActionsHaveLabels` sanity check).
- Achievements pane: live. New pause-menu entry "Achievements (x/y)" (between Stats and Setup) opens a cross-track lifetime achievement list. Pure helpers in `src/game/achievements.ts` define the catalog (14 milestones across `speed`, `progression`, `style`, `mastery`, `discovery` categories: First Lap, Local Hero, Sub-30 / Sub-20 / Sub-15, Drift King, Hat Trick / On Fire PB streaks, Track Veteran, Marathoner, Perfectionist, Variety Pack, Lost Tourist, Platinum) plus the `evaluateAchievements(snapshot)` evaluator (single-pass, returns every id whose criterion the snapshot satisfies so a single fast first lap unlocks every speed tier in one go), `unlockAchievements(prev, earned, meta)` (adds only ids not already unlocked, preserves prior `unlockedAt` so a repeat trigger never retoasts), `achievementProgress(map)` for the badge readout, plus typed `AchievementMap` and `AchievementSnapshot` shapes. Storage in `src/lib/achievements.ts`: a single `viberacer.achievements` localStorage key for the lifetime unlock map (zod-validated; unknown ids filtered on read so a renamed achievement does not poison the renderer) plus a `viberacer.slugsVisited` key tracking the distinct slugs the player has touched (sourced when `beginRace()` flips into the racing phase, idempotent on re-visits, surfaced as `distinctSlugCount` to the evaluator so Variety Pack credits at the fifth distinct slug). `Game.tsx` mirrors the unlock map into a state value plus a ref, evaluates after the trackStats writeback in `handleLapComplete` (so `nextStats.lapCount` and `nextStats.totalDriveMs` are already updated), feeds the just-finished lap's drift peak through a `lastLapDriftScoreRef` set by `handleLapDriftBest` (which fires immediately before `onLapComplete` in the rAF loop) so the achievement evaluator sees the right drift score for the SAME lap, latches a `wrongWayTriggeredRef` from `onCanvasHud` so a fleeting wrong-way blip credits the badge on the next lap completion (reset on full Restart, kept on Restart Lap since the player did go the wrong way at some point in this session), and computes the medal tier inline via `medalForTime(lapBestAllTimeMs, recordTimeForMedal)` so the Platinum achievement unlocks on the same lap that earns the platinum medal. Newly-unlocked ids surface as a yellow "Achievement: <name>" toast lane (separate from the lap-saved toast lane so a true PB lap that also unlocks an achievement shows both messages); multiple unlocks in one lap collapse to a single combined toast so the lane never queues a stack. The pane component is `src/components/AchievementsPane.tsx`; it sorts unlocked-first (by `unlockedAt` desc) then locked in canonical catalog order, renders a header progress bar plus per-row card with a colored badge / category chip / description / "Earned YYYY-MM-DD on /<slug>" footer for unlocked entries (locked rows render dimmed with a muted "Locked" label so the player can browse what is on offer before earning anything). Cross-tab updates land via the same-tab `viberacer:achievements-changed` CustomEvent plus the browser's `storage` event so unlocking in another tab refreshes the pane immediately. Tests: `tests/unit/achievements.test.ts` (48 tests covering catalog uniqueness, no em-dash invariant on every player-facing string, valid category enum, every documented threshold's at-and-just-below boundary case (sub-30 / sub-20 / sub-15, drift king, triple / five PB streak, track veteran, marathoner, variety pack), null / non-finite / non-positive defenses on lap time / drift score / pb streak, the medal-tier-only-platinum invariant, `unlockAchievements` ignoring already-unlocked ids while preserving their original `unlockedAt`, fresh-object isolation, mixed new + already-unlocked merge, `achievementProgress` empty / partial / cap-at-total / unknown-id-filter, and combined first-lap-on-fresh-slug + non-PB scenarios). `tests/unit/achievementsStorage.test.ts` (12 tests covering empty-storage / malformed-JSON / schema-fail short-circuits, unknown-id filter on read, write-then-read round-trip, the `ACHIEVEMENTS_EVENT` dispatch, and the slug-visit counter incl. idempotent re-visits, multi-slug ordering, and dedupe of hand-edited duplicates).
- Photo Mode pause pane: live. New pause-menu entry "Photo mode" (between How to play and Share track) opens an in-overlay capture panel that saves a screenshot of just the 3D scene. Pure helpers in `src/lib/photoMode.ts` (`buildPhotoFilename(slug, format, date)` produces `viberacer-<safeSlug>-<YYYYMMDD-HHmmss>.<ext>`, `sanitizeSlugForFilename(raw)` strips path separators / unsafe punctuation and caps length at 48 with a `'track'` fallback for empty input, `formatPhotoTimestamp(date)` emits a chronologically-sortable 15-character stamp, `mimeForFormat` / `extensionForFormat` map the friendly `'png'` / `'jpg'` enum to `'image/png'` / `'image/jpeg'` and the matching extension, `downloadDataUrl(dataUrl, filename)` triggers a browser download via a transient anchor element, plus the `PHOTO_JPG_QUALITY = 0.92` and `PHOTO_FILENAME_PREFIX = 'viberacer'` constants). `RaceCanvas.tsx` exposes a new `captureScreenshotRef: MutableRefObject<((mime?, quality?) => string | null) | null>` prop the parent reads via the same poll-and-set pattern as the other refs; the installed function force-renders the latest scene + camera (no `preserveDrawingBuffer` opt-in so the perf cost stays at zero outside Photo Mode) and immediately calls `canvas.toDataURL` inside the same JS task so the WebGL drawing buffer is still valid. Returns null on a tainted GPU read so the UI can surface a friendly error. The hook is detached on unmount so a stale closure cannot fire after the renderer is disposed. `Game.tsx` owns the ref and a new `'photo'` `PauseView`; `PauseMenu.tsx` adds the entry and wires it through. The pane component (`src/components/PhotoMode.tsx`) reuses the shared MenuUI primitives (overlay, panel, header, hint, section, button) so the visual language matches every other paused overlay; it offers two CTAs (PNG primary, JPG secondary), shows a confirmation banner that fades after 1.8 s with the saved filename or an error message, and explains that the saved image captures only the 3D scene (no HUD, no menu overlay) since the React DOM does not bleed into a `canvas.toDataURL` read. The Feedback FAB is suppressed while the pane is open so its floating button does not visually compete with the capture controls. Tests: `tests/unit/photoMode.test.ts` (21 tests covering MIME and extension mapping, JPG quality bounds, slug passthrough / lowercase / unsafe-character strip / leading-and-trailing-dash trim / 48-char cap / empty / unsafe-only / non-string fallback, timestamp zero-padding / chronological sort / 15-character invariant, filename composition + sanitization + empty-slug fallback + prefix invariant, and `downloadDataUrl` null / empty / undefined short-circuits, the anchor lifecycle (href, download, click, remove), and the no-document SSR fallback).
- End-of-session summary pane: live. Picking "Exit to title" from the pause menu now opens an in-overlay `SessionSummary` pane when the player has at least one completed lap (a fresh paused-and-bailed session with zero laps still routes straight out so the no-laps case stays instant). The pane reads a single `SessionSummaryStats` snapshot built by `summarizeSession` in `src/game/sessionSummary.ts`: lap count, best / average / total lap time (via the existing `summarizeHistory`), wall-clock duration on the page (via a `sessionStartedAtRef = Date.now()` latched at mount and surviving Restart so the counter measures page presence rather than per-race time), session-best drift score (mirrored from `hud.driftLapBest`, which the HUD callback accumulates across laps via `Math.max(prev, next)` so the value reads "best of session"), and a signed delta between the session's best lap and the player's all-time PB at mount (a `sessionPriorPbRef` snapshot taken before any of this session's PBs were written, so the comparison reads "what bar did I clear today" rather than the post-session bar the player already moved). The Vs PB tile flips its label and accent color to "New PB" when the delta is negative; on a tie it stays muted (an exact match is not an improvement to celebrate). All four CTAs route through the shared `MenuUI` primitives so the pane matches every other paused overlay: a primary "Race again" (closes the pane and calls the existing `restart()` so the player stays on the track with a fresh log), a soft "Share session" reusing the same `handleShare` and transient label flow as the pause menu Share entry, a confirm-tone "Exit to title" that finally calls `router.push('/')`, and a ghost "Back" that drops the player back at the pause menu. Sanitization in the helper drops degenerate inputs (non-finite or non-positive lap times / drift scores / prior PB) so a corrupt storage read can never feed the pane garbage values; the session duration is clamped to 24h so a tab left open for a week never overflows the readout. Tests: `tests/unit/sessionSummary.test.ts` (15 tests covering empty history, multi-lap aggregation, negative delta + new-PB latching, positive delta, tie reading as not-a-PB, null prior PB short-circuit, drift sanitation across valid / zero / negative / non-finite, duration clamping at zero / negative / NaN / Infinity / past-24h, prior-PB sanitation rejecting non-finite / non-positive, history immutability, plus formatter coverage of sub-second / sub-minute / sub-hour / hour-plus / clamped-24h paths).
- Per-track engagement Stats pane: live. New pause-menu entry "Stats" (between Laps and Setup) opens an in-overlay panel listing the player's all-time engagement totals on the current `(slug, versionHash)`: total completed laps, distinct race sessions, cumulative time on track (sum of completed lap times), average lap time, best lap (mirrored from `readLocalBest` for visual continuity with the HUD), and the first / last played dates so a long-running track shows its history. Stats are per-version so a fork to a new layout starts on a clean slate, matching the rest of the local-PB storage scheme. Pure helpers live in `src/game/trackStats.ts` (`emptyStats`, `recordSession`, `recordLap`, `formatDuration`, `formatPlayedAt`, `averageLapMs`); the recorders return new snapshots so React state updates fire on reference inequality. Defensive: non-finite or non-positive lap times are dropped from the totals; non-finite or non-positive timestamps fall back to existing values so a misbehaving clock cannot poison the record. Storage in `src/lib/localBest.ts` (`viberacer.stats.<slug>.<hash>` localStorage key with a zod-validated payload via the new `TrackStatsSchema` so a hand-edited or corrupt blob can never feed the pane garbage values; `freshTrackStats` is a re-exported empty-snapshot factory). `Game.tsx` mirrors the snapshot into a state value plus a `trackStatsRef`, calls `recordTrackStatsSession` exactly once per Game mount on the countdown -> racing transition (a `sessionCountedRef` latch guards the increment so a Restart that re-mounts is counted again, intentionally), calls `recordTrackStatsLap` from inside `handleLapComplete` after the toast / fanfare branch so a degenerate lap never inflates the totals, and writes through to localStorage on every change. The slug-change effect refreshes the snapshot from disk and resets the session latch so navigating between two versions in the same tab loads the right record. The pane component is `src/components/TrackStatsPane.tsx`; it renders a 2x3 grid of stat tiles with the best-lap tile accent-bordered to mirror the HUD's BEST tile, plus a small "First raced on YYYY-MM-DD" footer (collapsed to a friendly empty state when no laps are logged yet). Tests: `tests/unit/trackStats.test.ts` (32 tests covering empty-snapshot defaults + isolation, session-counter monotonicity, first-played seeded once and preserved across sessions, lap-counter + total-drive-ms accumulation, rounding-half rule, defensive drops for non-positive / non-finite lap times, fall-back-to-existing for non-finite / non-positive `nowMs`, immutability of inputs, duration formatting in MM:SS / HH:MM:SS / sub-second-floor / non-finite / negative branches, ISO-style date formatting in UTC with single-digit-month / day padding plus null / non-finite / non-positive guards, and average-lap math including the corrupt-totalDriveMs guard). `tests/unit/localBest.test.ts` adds 9 tests covering null on empty, namespacing per slug + version hash, full round-trip, the fresh-snapshot round-trip, refusal to persist corrupt input, schema rejection of a hand-edited payload, malformed JSON, the quota-exception silent-swallow, and the `freshTrackStats` factory output.

### Title screen (route `/`)

- Logo ("VibeRacer") with the game's cartoony font and color palette.
- Cartoony background loop (simple procedural scene, could be an auto-driving car on a looping sample track).
- Streaming synth title music (see Section 13).
- Menu items:
  - "Play" (starts a race at a default slug or prompts for a name)
  - "Load existing track" (list of latest-updated slugs)
  - "Settings"
  - "How to play"

### Pause menu

Triggered by `Esc` or the on-screen pause button. Freezes simulation and music intensity (music can keep playing, muted or ducked).

Items:

- Resume
- Restart Lap (zeroes the in-flight lap, keeps session PB and lap history)
- Restart
- Edit Track
- Leaderboards (for this track version)
- Laps (this session's completed laps with deltas vs PB)
- Stats (per-track engagement totals across all sessions: lap count, time on track, sessions, average lap, first / last played)
- Achievements (cross-track lifetime milestones: speed tiers, PB streaks, drift, track veteran, marathoner, perfectionist, variety pack, platinum)
- Setup
- Settings
- How to play (controls reference: keyboard, touch, gamepad, plus pro tips)
- Photo mode (saves a screenshot of just the 3D scene as PNG or JPG)
- Share track (Web Share API on mobile, clipboard fallback on desktop)
- Exit to title (routes through a session summary pane when at least one lap was completed; otherwise routes straight to the title screen)

The feedback FAB appears only while paused, and is suppressed inside the Photo mode pane so its floating button does not crowd the capture controls. See Section 12.

---

## 10. Physics and Controls Tuning (player Setup)

**Status.** Done. Tuning is a first-class player feature: a Setup panel in the pause menu exposes live sliders for every `CarParams` field, saves per track, and carries the most recent setup forward to fresh tracks. Each lap submission carries its setup so leaderboard viewers can see and one-tap reuse another player's tuning.

### Setup panel

- Pause menu entry: "Setup".
- Component: `src/components/TuningPanel.tsx`.
- Each parameter has a slider and a numeric input. Per-field "reset" link reverts that field to its default. Footer has "Reset to defaults" for the whole setup.
- The two `steerRate*` fields share a 2D pad: horizontal axis = low-speed steering, vertical axis = top-speed steering. A small grey dot marks the defaults; the live value tracks the orange dot. Numeric inputs sit alongside for precision.
- A status line shows a STOCK chip when the setup matches `DEFAULT_CAR_PARAMS`, otherwise a TUNED chip.

### Storage

- Per-track key: `viberacer.tuning.track:<slug>` in `localStorage`.
- Last-loaded key: `viberacer.tuning.lastLoaded` (mirrors whichever per-track tuning was most recently saved).
- Resolution order on load: per-track save, then last-loaded carryover, then `DEFAULT_CAR_PARAMS`.
- One-shot migration from the old dev-only key `viberacer.dev.tuning` if present.
- Resolver, schema, and helpers live in `src/lib/tuningSettings.ts`. Hook is `src/hooks/useTuning.ts` (slug-scoped, hydrates on mount, listens for cross-tab `storage` changes).

### Slider ranges

Bounds double as anti-cheat sanity caps (server validates with the same numbers via `CarParamsSchema`):

| field | default | min | max | step | unit |
|---|---|---|---|---|---|
| maxSpeed | 26 | 12 | 50 | 0.5 | u/s |
| maxReverseSpeed | 8 | 2 | 20 | 0.5 | u/s |
| accel | 18 | 4 | 48 | 0.5 | u/s² |
| brake | 36 | 8 | 80 | 1 | u/s² |
| reverseAccel | 12 | 2 | 30 | 0.5 | u/s² |
| rollingFriction | 4 | 0 | 20 | 0.25 | u/s² |
| steerRateLow | 2.2 | 0.5 | 5.0 | 0.05 | rad/s |
| steerRateHigh | 2.2 | 0.5 | 5.0 | 0.05 | rad/s |
| minSpeedForSteering | 0.8 | 0 | 5 | 0.1 | u/s |
| offTrackMaxSpeed | 10 | 2 | 30 | 0.5 | u/s |
| offTrackDrag | 16 | 0 | 60 | 1 | u/s² |

### Leaderboard attachment

Each lap submit includes `tuning` (current `CarParams`) and `inputMode` ('keyboard' or 'touch'; last-input-wins detection). Server stores them under `lap:meta:<nonce>` in KV; the leaderboard read batches these via `mget` and serves them on each entry. Older entries with no meta surface as `null` and the UI shows a dim placeholder. Out-of-range tuning is rejected at submit time (silent drop) and replaced with `null` if it ever sneaks through.

### Interactive Tuning Lab

A dedicated mode at `/tune` for players who want guided tuning instead of raw sliders. Reachable from the title-screen menu (button next to Settings) and from the Settings pane (in-game and on the title screen). Mobile-first, no leaderboards, no KV.

- Curated test loop in `src/lib/tuningLabTrack.ts`: a 12-piece closed loop with straights, two left turns, four right turns, an S-curve, and a hairpin. Verified by `tests/unit/tuningLabTrack.test.ts` against `validateClosedLoop` and a simulated centerline drive that fires `lapComplete`.
- Render primitive `src/components/RaceCanvas.tsx` (extracted from `Game.tsx`) drives the canvas, scene, and rAF loop. Both the race flow and the lab use it.
- Session flow in `src/components/TuningSession.tsx`: intro (control-type and tag chips) -> countdown -> drive (one full lap) -> 5-point Likert form -> recommendation diff -> drive again or save.
- Recommendation engine in `src/lib/tuningLab.ts`: pure `recommendNextParams(current, ratings, prevDeltas, damping)`. Each rated aspect maps to one or more `CarParams` keys with a sign and weight; the per-param delta is `unit * sign * weight * (max - min) * baseStep * damping`, averaged across contributing aspects, and clamped via `clampParams`. Damping halves a per-param multiplier (floor 1/16) when the next round flips its delta sign, so oscillation tames itself.
- Aspects rated (5-point Likert, 3 = just right): top speed, pickup, braking, low-speed turn, high-speed turn, coast, off-track penalty.
- Saved tunings in localStorage under `viberacer.tuningLab.saved`. Each row tags `controlType` (`keyboard` / `touch_single` / `touch_dual`) and up to four `trackTags` (`twisty` / `fast` / `mixed` / `technical`). Sortable by recency, mean rating, fastest lap, or name. Filterable by control and tag, plus name search.
- "Apply to next race" writes through to the existing `viberacer.tuning.lastLoaded` key (and a synthetic `viberacer.tuning.track:__lab__` save) so the next slug the user opens picks the lab tuning up automatically.
- The session also auto-persists the live params to `lastLoaded` on every change so an unsaved session still carries its most-recent setup forward when the user leaves the lab. During the drive phase a "Restart" button resets the car to the start line without going to feedback.
- Export and import are clipboard-based. "Copy JSON" on a saved tuning copies a single `SavedTuning`. The home view also offers "Copy all saved tunings". Import view accepts pasted JSON and dispatches via `parseImportedJson`: a single tuning lands directly in the saved list; a session payload saves its final round.

---

## 11. Leaderboards

**Status.** Partial. Server-side: storage model, `/api/race/start`, `/api/race/submit`, the full anti-cheat validation chain, rate limits, and nonce rotation are in. Client-side: auto-submit on lap completion and local PB tracking (session + all-time in `localStorage`) are live. The leaderboard UI (top 25 view, version dropdown, PB highlight on the board, PB celebration fanfare) is not yet started.

### Storage

Per `(slug, versionHash)` sorted set in KV. Score is the lap time in milliseconds. Member is a composite string: `initials:racerId:ts:nonce`.

Lower score wins. Return the top N (default 25) for display.

### Autosave flow (core UX)

- Every completed lap auto-submits. No manual "submit time" button.
- Crossing the finish line triggers a background `POST /api/race/submit`.
- Initials are always already set by the time the game is playable (prompted on page load per Section 7).
- The lap submits silently. The HUD flashes a small confirmation: "saved, NEW PB!" or "saved, #12 on board" or just "saved".
- Changing initials mid-session only affects future laps.

### Concurrency model (important)

Multiple players can race the same `/slug?v=<hash>` simultaneously. The system must handle this cleanly and prevent one player from spoofing another.

- Nonces are cryptographically random (16 bytes from `crypto.randomBytes`). Collisions between concurrent racers are effectively impossible.
- Every race token is bound to a specific racer via a server-issued `racerId` cookie. This prevents one player from capturing another player's in-flight token and submitting times under it.
- `racerId` is an opaque UUID. Not derived from IP (mobile IPs change). Not derived from MAC address (browsers cannot read MAC). It is set by the server on first visit to any route as a cookie: `HttpOnly`, `Secure`, `SameSite=Lax`, TTL one year.
- A lightweight Next.js `middleware.ts` runs on every request and ensures the cookie exists.
- IP is captured in logs for audit and rate limiting only. It is not used as a hard identity check, since a WiFi-to-cell handoff would otherwise invalidate an in-flight race.

### Server flow

1. **First visit (any route).** Middleware checks for the `racerId` cookie. If missing, generate a UUID v4, set the cookie, write `racer:<racerId>:firstSeen` to KV. Transparent to the user.
2. **Race start.** Client calls `POST /api/race/start?slug=<slug>&v=<hash>`. Server reads `racerId` from the cookie. Server returns a signed race token encoding `{slug, versionHash, nonce, issuedAt, racerId}`, HMAC-signed with `RACE_SIGNING_SECRET`.
3. **During the race.** Client records checkpoint hits: `[{cpId, tMs}, ...]` where `tMs` is milliseconds since race start.
4. **Lap completion.** Client calls `POST /api/race/submit` with `{token, checkpoints, lapTimeMs, initials}`. The `racerId` is read server-side from the cookie, never trusted from the request body.
5. **Server validates:**
   - Token signature is valid.
   - Token is not expired (age <= 15 minutes).
   - Token's embedded `racerId` matches the cookie's `racerId` on this request. (Rejects cross-browser token theft.)
   - Nonce is unused (one-shot; stored at `race:token:<nonce>` with TTL, deleted on submit).
   - Slug and versionHash in token match the request's target.
   - All checkpoints are hit in correct order.
   - Each segment time meets or exceeds a configured minimum (catches teleport hacks).
   - `lapTimeMs` equals the sum of segment times within a small tolerance.
   - `initials` match `/^[A-Z]{3}$/` after uppercase normalization and a profanity filter.
6. **On valid submit.** `kv.zadd` into the leaderboard. The leaderboard member encodes `initials:racerId:ts:nonce` so admin tooling can later identify and revoke individual racers without exposing racerId in the public display. The response returns a fresh nonce so the next lap can submit under a new one-shot nonce. The session's signed base token stays the same; only the nonce rotates.
7. **On invalid submit.** Drop silently. No error leaked. The HUD still shows the local time so the player never knows whether a submission failed (deliberate, to avoid teaching cheaters what to fix).
8. **Rate limiting.** Mirrored from Determined's `api/leaderboard.js` pattern:
   - Per-IP: about 1 submit / 3 sec average, burst 5 (TTL 60s).
   - Per-racerId: same.
   - Per-IP daily cap (TTL 24h).
   - When a limit trips, drop silently.

### What this protects against

- **Concurrent same-track racers.** Each has their own `racerId` and nonces. No collision.
- **Stolen token replay.** An attacker without the victim's cookie fails the racerId check.
- **Time-travel submissions.** Old tokens expire after 15 minutes.
- **Nonce reuse.** One-shot nonces are deleted on submit.

### What this does NOT protect against (scoped-out, best-effort)

- A player modifying client physics to produce fast-but-plausible times. Segment minimum times set a floor; anything below is rejected. Times above the floor are trusted.
- Two devices sharing a browser cookie (same family computer). They collectively share a racerId. Acceptable trade-off.
- Network-level replay within the 15-minute window by the legitimate cookie holder. Low impact: they could already submit themselves.

### Leaderboard UI

- Default view: top 25 for the current track version.
- Dropdown to browse older versions of the same slug.
- Sortable columns (rank, initials, time, date).
- Per-row input-mode icon: keyboard / phone glyph (or dim placeholder for entries without meta).
- Per-row STOCK / SETUP chip next to the initials. Tap SETUP to open a popover that lists every `CarParams` value side by side with the defaults, plus "Copy JSON" and "Try this setup" (the latter applies the entry's tuning to your local setup and resumes).
- Personal best is highlighted if the current `racerId` has a score on this board.
- PB celebration (larger flash plus a small synth fanfare) fires only on a new personal best against this track version.

### Build log

- `POST /api/race/start` (`src/app/api/race/start/route.ts`) validates `slug` and `v`, reads `racerId` from cookie, generates a 16-byte random nonce via `crypto.randomBytes`, signs `{slug, versionHash, nonce, issuedAt, racerId}` with HMAC-SHA256, and writes `race:token:<nonce>` to KV with 15-minute TTL.
- Token format: `base64url(JSON).base64url(hmac)`. Signing key is `RACE_SIGNING_SECRET`. Verification uses `timingSafeEqual`. Helper: `src/lib/signToken.ts`.
- `POST /api/race/submit` (`src/app/api/race/submit/route.ts`) runs the full validation chain, then calls `kv.del(race:token:<nonce>)` to enforce one-shot nonces (replay drops silently), `kv.zadd` to the leaderboard with composite member `initials:racerId:ts:nonce`, updates `racer:<racerId>:lastSubmit`, and returns a fresh nonce plus re-signed token.
- Pure validation logic: `src/lib/anticheat.ts` (`validateLap`). Covers bad signature, token expired, racer mismatch, target mismatch, checkpoint order, segment floor, lap-time tolerance, bad initials, profane initials. Defaults: `tokenMaxAgeMs = 15 * 60 * 1000`, `minSegmentMs = 200`, `lapTimeToleranceMs = 50`. Small starter profanity blocklist. All settable via the `opts` arg for testing and tuning.
- Rate limits: `src/lib/rateLimit.ts` using `kv.incr` + `kv.expire`. Defaults: 5 per IP per 60s, 5 per racer per 60s, 500 per IP per 24h. Any trip drops silently.
- **Silent-drop convention.** On any validation or rate-limit failure, submit returns HTTP 202 with `{ok: false}`. Rationale: 202 is semantically "accepted and being processed" which cleanly masks why a submission was rejected without teaching cheaters what to fix. The HUD still shows the local time.
- Typed KV key helpers and TTL constants: `src/lib/kv.ts`. Mirrors the exact key names in Section 14.
- Tests: `tests/unit/signToken.test.ts`, `tests/unit/anticheat.test.ts`, `tests/unit/api.raceStart.test.ts`, `tests/unit/api.raceSubmit.test.ts`. In-memory `FakeKv` for route tests: `tests/unit/_fakeKv.ts`.
- Client auto-submit: `src/components/Game.tsx::startRaceServerSide` fires `POST /api/race/start` when the countdown ends and stores the returned token in `tokenRef`. `submitLap` fires `POST /api/race/submit` on every completed lap, rotating the token on success. If KV or `RACE_SIGNING_SECRET` is not configured the server returns 500 and the client swallows the error; gameplay is unaffected.
- Local PB tracking: `src/lib/localBest.ts` stores `viberacer.best.<slug>.<versionHash>` in `localStorage`. `GameSession` seeds `bestAllTimeMs` from that key on mount and updates it on every new all-time PB. Session PB is held in React state only. `handleLapComplete` runs PB detection independent of the server round-trip; toast reads "NEW RECORD!", "NEW PB!", or "lap N saved" in that priority order.
- Overall track record: `src/lib/leaderboard.ts` exports `readLeaderboard(kv, slug, hash, limit, myRacerId)` which walks `zrange` output (with scores) and parses composite members (`initials:racerId:ts:nonce`) into typed `LeaderboardEntry[]` plus a `meBestRank`. The `[slug]/page.tsx` RSC calls it with `limit=1` on the server so the top entry is available on first paint and passed as `initialRecord` through `<Game>` into the HUD's RECORD block.
- `GET /api/leaderboard?slug=X&v=HASH&limit=25`: `src/app/api/leaderboard/route.ts` runs on the Node.js runtime. Validates `slug` and `v` through the same zod schemas as the other routes (returns 400 on invalid). Reads the cookie's `racerId` so `isMe` is server-marked. Returns `{slug, versionHash, entries, meBestRank}`. Clamps `limit` to `[1, LEADERBOARD_MAX_LIMIT]` (default 25, max 100). On KV error returns 200 with an empty `entries` list so the client degrades gracefully.
- Leaderboard UI: `src/components/Leaderboard.tsx` is a paused-only overlay. Fetches `/api/leaderboard` with `cache: 'no-store'`. Renders rank / racer (with a "you" badge for `isMe`), time, date. The user's rows are highlighted with the accent color. If the user has any entry, a footer reads "Your best on this track: #N".
- Client optimistic record update: `handleLapComplete` compares the new lap to the current `overallRecord.lapTimeMs`. A faster lap becomes the new record immediately in the HUD even before the server acknowledges the submit. If the server rejects the submit silently, the HUD will still show the optimistic record until the next page load refreshes it from KV. Acceptable trade-off vs round-tripping before updating.
- Tests: `tests/unit/api.leaderboard.test.ts` covers 400 on bad params, sorted order with ranks, `isMe` + `meBestRank` via cookie, limit clamping, and the empty-board case. Smoke: `tests/e2e/smoke.spec.ts` hits the route for both rejection and shape.
- Version dropdown: `src/components/Leaderboard.tsx` now fetches `/api/track/<slug>` on mount and renders a dropdown listing the stored versions (newest first, tagged `latest` / `racing`). Selecting a different version re-fetches `/api/leaderboard?slug=X&v=HASH` and re-renders the table. If the user selects a version other than the one currently being raced, a `Race this version` button appears and navigates to `/<slug>?v=<hash>` (or `/<slug>` when the selection is the latest). If KV is empty or unreachable the dropdown gracefully degrades to showing just the current version.
- PB confetti celebration: live. `src/components/ConfettiOverlay.tsx` is a full-viewport, click-through 2D canvas that spawns a tumbling-paper confetti burst when a lap takes the local PB ('pb', green palette, 80 chips) or beats the track-wide record ('record', multicolor gold-led palette, 140 chips). `Game.tsx` flips a `confettiKind` plus a monotonic `confettiKey` from `handleLapComplete` so back-to-back PBs spawn fresh batches and a Restart clears the in-flight chips. The simulation runs in normalized 0..1 viewport coords (resize-safe) with seeded RNG so spawns are deterministic. Particles spray upward in the upper hemisphere, fall under gravity, drag with air, fade from age 1.8s to 3.2s, and the rAF loop self-stops once the batch expires (no idle CPU when not celebrating). Pure helpers in `src/game/confetti.ts` (`spawnConfettiBatch`, `stepConfetti`, `confettiAlpha`, `isBatchExpired`, `makeRng`); 31 unit tests in `tests/unit/confetti.test.ts` cover deterministic seeding, palette + count contracts, gravity / drag / rotation step math, fade ramp boundaries, and batch-expired detection.
- Server-returned leaderboard rank surfaced in the lap-saved toast: live. `POST /api/race/submit` now performs a `kv.zrank(lbKey, member)` (1-indexed) plus a `kv.zcard(lbKey)` after the `zadd` and surfaces both as `submittedRank` / `boardSize` on the JSON response. Both reads run in parallel through `Promise.all` so the round-trip cost is one request, and a thrown KV exception is swallowed so the route still returns a clean 200 with `submittedRank: null` / `boardSize: null` rather than dropping the player's lap. Pure helpers in `src/lib/lapToast.ts` (`isLapRankInfo` defensive validator that rejects null / undefined / non-object / missing-fields / non-finite / fractional / zero-or-negative / rank-greater-than-boardSize, `formatRankSuffix` produces `#R / B` with spaces around the slash so it never reads as a fraction, `buildToastWithRank(baseToast, kind, info)` returns `<base toast> · #R / B` for `'lap'` / `'pb'` toasts and the unchanged base for `'record'` since the new record is always #1 by definition). `Game.tsx::submitLap` reads the new fields off the response, validates them with `isLapRankInfo`, then `setHud`'s through `buildToastWithRank` while the toast lane is still showing (the existing 1800 ms auto-clear timer is untouched, and the update short-circuits when the toast has already been cleared so a slow round-trip never revives a dead lane). The HUD's existing toast block renders the upgraded text without any wiring change. Tests: `tests/unit/lapToast.test.ts` (23 tests covering `isLapRankInfo` happy path / boundary cases / null / undefined / non-object / missing-rank / missing-boardSize / non-finite / fractional / zero-or-negative / rank-greater-than-boardSize, `formatRankSuffix` small-board / rank-1 / large-numbers, and `buildToastWithRank` lap-suffix / pb-suffix / record-omits-suffix / null-info / malformed-info / empty-base-toast / null-kind paths). `tests/unit/api.raceSubmit.test.ts` adds `submittedRank: 1` / `boardSize: 1` assertions to the clean-lap test plus a new test that submits a slower-than-legacy lap (ranks #2 of 2) followed by a faster-than-legacy lap (ranks #1 of 3) so the route's zrank + zcard wiring is verified end-to-end. `tests/unit/_fakeKv.ts` gains `zrank` (returns the 0-indexed position or null when the member is absent) and `zcard` (returns the entry count) so route tests can exercise the new path without a live Redis.
- **Not yet landed.** Admin tooling to revoke racers by composite member, pagination beyond top 100.

---

## 12. Feedback FAB (port from Epoch, with two modifications)

**Status.** Partial. API route ships (targets `Randroids-Dojo/VibeRacer`, em-dashes stripped). `FeedbackFab.tsx` component ships with both modifications: single-click opens the input panel directly, and the component is mounted only while the pause menu is open.

### Source files to port

- `/Users/randroid/Documents/Dev/Epoch/components/shared/FeedbackFab.tsx`
- `/Users/randroid/Documents/Dev/Epoch/lib/consoleCapture.ts`
- `/Users/randroid/Documents/Dev/Epoch/app/api/feedback/route.ts`
- Relevant `.epoch-fab-*` CSS blocks from `Epoch/app/globals.css`.

Keep the screenshot and console log capture behavior. Both are useful for bug reports in a 3D game.

### Modification 1: single click to input panel

Epoch's FAB opens an intermediate menu with a "Feedback" button. VibeRacer skips the menu.

- Change the `toggle()` function so `view` transitions directly: `'closed'` to `'feedback'` (not `'closed'` to `'menu'`).
- Delete the entire `.epoch-fab-menu` DOM block.
- Clicking the FAB once opens the title plus description input immediately.

### Modification 2: pause-only visibility

- Conditionally mount the `<FeedbackFab />` component only when game state is `paused`.
- Not rendered on the title screen. Not rendered during active racing.
- A simple game state context (`{ isPaused: boolean }`) provides this.

### Config updates

- Change the hardcoded repo in the ported `route.ts` from `Randroids-Dojo/epoch` to the VibeRacer repo slug (confirm with user before commit).
- Keep the `GITHUB_PAT` environment variable name (matches Epoch for consistency across projects).
- Screenshot upload path stays at `.github/feedback-screenshots/`.

### Build log

- `src/app/api/feedback/route.ts` ported from Epoch. `REPO` changed to `Randroids-Dojo/VibeRacer`. Em-dashes stripped from formatting strings (the `formatConsoleLogs` separator and the body-limit omission message both use colons now).
- `src/lib/consoleCapture.ts` ported verbatim from Epoch (no em-dashes in the source, ellipsis U+2026 on truncation is fine).
- Route is on the Node.js runtime. Requires `GITHUB_PAT` to post issues; returns 500 if the env var is missing.
- Tests: `tests/unit/api.feedback.test.ts` covers the missing-PAT path, the missing-title path, and a mocked happy path that asserts the GitHub issues URL contains `Randroids-Dojo/VibeRacer`.
- `src/components/FeedbackFab.tsx` ports the Epoch FAB with both modifications. The `View` type dropped to `'closed' | 'feedback'`; `toggle()` flips between them directly so a single click opens the input panel. Pause-only visibility is handled at the call site: `Game.tsx` only mounts `<FeedbackFab />` inside the `paused` branch alongside `<PauseMenu />`. No `isPaused` context needed since only `Game.tsx` renders the FAB.
- Styling: inline `React.CSSProperties` objects instead of a global stylesheet, matching the project convention (HUD, PauseMenu, Countdown). FAB pill at `bottom-right`, panel opens immediately above it. Screenshot capture and console log buffering work unchanged from Epoch.
- **Not yet landed.** Playwright coverage for the FAB open/close/submit flow. Visual polish pass on the panel.

---

## 13. Audio

**Status.** Partial. Music ships; SFX pending.

Pure Web Audio API. No Tone.js. One shared `AudioContext`, procedural synth voices, scheduled via a 50 ms tick with 120 ms lookahead.

### Music (shipped)

Three named tracks live in one engine (`src/game/music.ts`) and share the scheduler. Each track owns a step counter, pattern, and per-track `GainNode` so tracks can overlap during a crossfade.

- **Title:** cartoony pentatonic loop in C major at 128 BPM. Mounts via `<TitleMusic />` on the home page and slug-landing page. Starts on the first pointerdown/keydown to satisfy browser autoplay policies; a one-shot document-level listener inside `music.ts` handles the retry.
- **Game:** driving minor-key loop in G minor at a configured 140 BPM. Crossfades in from title at race start over 3 seconds. Tempo ramps with car speed (70% to 100% of configured BPM, so 98 to 140 BPM). Voice volumes, drums, and a counter-melody fade in above intensity thresholds so low-speed driving sounds sparse and floored-it sounds full. The root key, scale flavor, and tempo are nudged from a stable per-slug hash when the player has the "Per-track flavor" toggle on (default on); turning it off plays the same loop on every track.
- **Pause:** slow, very quiet sine pad in C major at 68 BPM. Crossfades in when the player pauses, crossfades back to game on resume, and crossfades to title on restart. All pause/resume transitions are 0.8 seconds.

### Scheduler notes

- `crossfadeTo(target, fadeSec)` fades all non-target tracks to 0 and fades the target up to its configured gain. Creates the target track on demand.
- `setGameIntensity(0..1)` is called every rAF frame in `Game.tsx` with `|speed| / maxSpeed`. Short-circuits within `INTENSITY_EPSILON` so 60 Hz calls are effectively free. Only affects the game track; title and pause ignore intensity.
- `fadeTrackTo(track, 0, fadeSec)` schedules a per-track prune timer that disconnects the gain node and removes the track from the map once the fade completes. This replaced an earlier central `stopMusic` timer and eliminates silent-work scheduling for faded-out tracks.
- First-gesture unlock is encapsulated in `music.ts`; callers never install their own listeners.

### Build log

- `src/game/music.ts` owns the engine, scheduler, voice helpers (`schedNote`, `schedKick`, `schedNoise` shared by snare and hat with a cached noise buffer per kind), the three pattern functions, and `TRACK_CONFIG` (the single source of truth for per-track BPM, root MIDI, scale, step renderer, and target gain). Pure helpers `midiFreq`, `scaleDeg`, and `SCALES` are exported for testing.
- `src/components/TitleMusic.tsx` is a null-rendering client component that calls `startTitleMusic` on mount and `stopMusic` on unmount. Mounted in `src/app/page.tsx`, `src/components/SlugLanding.tsx`, and inside `Game.tsx` (so the countdown still has title music before the race-start crossfade).
- `src/components/Game.tsx` drives the transitions: `crossfadeTo('pause')` on pause, `crossfadeTo('game')` on resume, `crossfadeTo('title')` on restart, and `crossfadeTo('game', RACE_START_CROSSFADE_SEC)` when countdown ends. The rAF loop calls `setGameIntensity(|speed| / DEFAULT_CAR_PARAMS.maxSpeed)` each frame.
- Tests: `tests/unit/music.test.ts` pins `midiFreq`, `scaleDeg` (wrap, octave shift, negative degrees), and the four canonical scale constants.
- Per-track music personalization: live. Pure helpers in `src/game/musicPersonalization.ts` (`slugMusicSeed` 32-bit FNV-1a hash of the slug, `personalizeForSlug` picks `{rootMidiOffset, scaleFlavor, bpmOffset}` from three menus indexed off the seed via different primes so the choices do not move in lockstep, plus `NEUTRAL_PERSONALIZATION`, `personalizationEquals`, and the published `ROOT_OFFSETS` (-5..+7 inside one octave so the music stays in the same audible register), `SCALE_FLAVORS` (`minor` / `dorian` / `pentatonic`), and `BPM_OFFSETS` (-12..+16 so the slowest possible BPM after the 70%-tempo intensity ramp still sits well above the 40 BPM floor) menus). The empty / non-string slug short-circuits to the neutral tweak so the legacy hardcoded G-minor / 140-BPM game loop is preserved exactly. `music.ts` gained `setMusicPersonalization(p | null)`: idempotent (no-op when the value matches the active one) and, when the game track is already live, mutates `rootMidi` / `scale` / `baseStepDur` in place plus reseeds `stepDur` from the new base so a current intensity does not linger at the old tempo. Title and pause tracks are unaffected. `AudioSettings` gained a `musicPerTrack: boolean` field with default `true` and a backfill default for legacy stored payloads. `Game.tsx` reads the toggle through `useAudioSettings` and runs a `useEffect([slug, audioSettings.musicPerTrack])` that calls `setMusicPersonalization(personalizeForSlug(slug))` when the toggle is on or `setMusicPersonalization(NEUTRAL_PERSONALIZATION)` when off, so the next race (or a slug change without remount) picks up the new flavor without restarting the audio engine. Settings UI: a new "Per-track flavor" sub-section in the Audio block with a single MenuToggle (disabled when music is muted entirely) plus a hint explaining what the toggle does. Tests: `tests/unit/musicPersonalization.test.ts` covers neutral identity, menu-bound invariants, deterministic seed, defensive non-string seed, single-character sensitivity, fresh-object isolation, menu membership, slug-to-tweak determinism, and cross-slug fingerprint variety. `tests/unit/audioSettings.test.ts` adds default-true, round-trip-disabled, and legacy backfill (omitted in stored payload) coverage for the new `musicPerTrack` field.

### SFX

- Countdown beeps: shipped. `playCountdownBeep(isGo)` in `src/game/music.ts` plays a one-shot tone through the shared master gain. A4 square for 3/2/1, A5 triangle for GO.
- Engine drone, pitch-shifted by speed: shipped. `startEngineDrone` / `updateEngine` / `stopEngineDrone` in `src/game/audio.ts`. One persistent sawtooth oscillator into a lowpass biquad and a per-voice GainNode. `RaceCanvas` calls `updateDriveSfx` every rAF frame, which feeds drone frequency, cutoff, and volume from `|speed| / maxSpeed`. `setTargetAtTime` smoothing keeps per-frame writes click-free. Volume ducks to 0 when paused or pre-race; off-track applies an additional small duck so going off the road sounds drier.
- Tire skid: shipped. `startSkid` / `updateSkid` / `stopSkid` in `src/game/audio.ts`. One looping noise buffer through a lowpass biquad and a GainNode. The pure helper `skidIntensity(speed, maxSpeed, steerAbs, onTrack)` proxies a slip-angle-style cue from `steerAbs * speedRatio + (offTrack ? 0.4 : 0)`. Justification: scalar physics has no real slip angle, so we surface the visible cues a player associates with skid (sharp steering at speed, leaving the road).
- Finish-line stinger: shipped. `playLapStinger()` plays a three-note triangle arpeggio (E5, G5, C6) on every completed lap that is not already a PB.
- PB celebration jingle: shipped. `playPbFanfare('pb')` plays a 5-note major arpeggio with a sub-octave triangle layer; `playPbFanfare('record')` adds octave-up doublings on the final two notes plus a kick on beat one. Wired in `Game.tsx::handleLapComplete`: `record` outcome plays the bigger fanfare, `pb` plays the smaller one, otherwise the lap stinger. The visual side lives in `HUD.tsx`: a centered radial-gradient burst plus an inset edge-flash, gold for `record`, green for `pb`. Total animation under 1.2s.
- UI click beeps: shipped. `playUiClick(variant)` schedules a one-shot oscillator (`'soft'` / `'confirm'` / `'back'` variants). The shared hook `useClickSfx(variant)` in `src/hooks/useClickSfx.ts` returns a stable callback. Wired into PauseMenu (every menu button), SettingsPane (close, done, reset, open Tuning Lab), TuningPanel (close, done, reset), Leaderboard (back, race-this-version), and InitialsPrompt (save).
- Off-track rumble: shipped. `playOffTrackRumble()` plays a short low-passed noise burst on the transition from on-track to off-track. Triggered inside `updateDriveSfx` from the `prevOnTrack && !onTrack` edge.
- Pause / restart / exit safety: `silenceAllSfx(0.05)` in `Game.tsx::restart` and the unmount cleanup ramps the continuous voices to 0; one-shots ride out their own sub-second tails. `RaceCanvas` cleanup calls `stopEngineDrone(0.1)` and `stopSkid(0.1)` on unmount.
- Haptic feedback (Vibration API) on lap completion, fresh personal best, and fresh track-wide record: live. Pure helpers in `src/lib/haptics.ts` (`HAPTIC_OUTCOMES = ['lap','pb','record']` mirror the existing `ToastKind` 1:1, `HAPTIC_PATTERNS` table with escalating wall-clock budgets (lap 40 ms, pb 240 ms triple-pulse, record 480 ms quintuple-pulse) capped by `HAPTIC_PATTERN_MAX_MS = 1000` so a future tweak cannot accidentally schedule a half-second buzz on every lap, `patternFor(outcome)` returns a fresh defensive copy so caller mutation cannot poison the constant table, `patternTotalMs(pattern)` for the budget invariant, plus `HAPTIC_MODES = ['off','auto','on']`, `DEFAULT_HAPTIC_MODE = 'auto'`, `HapticModeSchema` zod enum, `HAPTIC_MODE_LABELS` / `HAPTIC_MODE_DESCRIPTIONS`, the `isHapticMode` / `isHapticOutcome` guards, the `shouldHapticFire(mode, isTouch)` resolver (off always suppresses, on always fires, auto fires only on touch), the `isTouchRuntime()` detector (matchMedia `(any-pointer: coarse)` first with a `navigator.maxTouchPoints` fallback so a webview without matchMedia still detects correctly, SSR-safe with a thrown-matchMedia recovery), and `fireHaptic(outcome)` (defensive against missing navigator / missing vibrate / thrown vibrate / unknown outcome / zero-budget pattern, returns false rather than throwing so a misconfigured browser never breaks the lap-saved flow). `ControlSettings` gained a `haptics: HapticMode` field (default `'auto'`), persisted in the existing `viberacer.controls` localStorage key with a backfill default for legacy payloads. `Game.tsx::handleLapComplete` mirrors `settings.haptics` into `hapticsModeRef` each render, and right after the existing audio cue branch routes the outcome through `shouldHapticFire(hapticsModeRef.current, isTouchRuntime())` before calling `fireHaptic(outcome)` so a desktop session with mode 'auto' never buzzes. Settings UI: a new Haptic feedback section in `SettingsPane.tsx` (right under Brake lights, above Time of day) renders a three-button picker (Off / Auto / On) reusing the same `MenuButton` row pattern as the brake lights picker, plus a sub-hint that swaps to whichever option is active so the player always sees plain-language confirmation of what the buzz will do. Tests: `tests/unit/haptics.test.ts` (39 tests covering outcome enum membership and stable order, every pattern non-empty / non-negative / finite, total budget under `HAPTIC_PATTERN_MAX_MS`, escalation invariant (lap < pb < record), `isHapticOutcome` mirror coverage, mode enum membership / stable order / default `'auto'` / non-empty labels / no em-dashes, `HapticModeSchema` accept all three valid modes plus reject `'AUTO'` / `''` / `'buzz'` / `0` / `null` / `undefined` / `{}`, `isHapticMode` mirror coverage, `shouldHapticFire` `'off'`-always-false / `'on'`-always-true / `'auto'`-tracks-isTouch, `patternFor` defensive-copy invariant + per-outcome correctness, `patternTotalMs` empty / non-finite / negative / zero-additive cases, `isTouchRuntime` SSR / matchMedia-true / matchMedia-false-without-touchPoints / maxTouchPoints fallback / desktop-zero-touchPoints / matchMedia-throws-falls-back-to-touchPoints, `fireHaptic` unknown-outcome / missing-navigator / missing-vibrate / passes-pattern-and-returns-true / passes-through-false / thrown-vibrate-swallowed-returns-false / fresh-pattern-per-call invariants). `tests/unit/controlSettings.test.ts` adds 5 tests covering the default `'auto'`, round-trip of `'on'` and `'off'`, legacy backfill (omitted in stored payload), and reject-and-default on an unknown stored value.

All SFX share one `AudioContext` and master `GainNode` with the music scheduler via `src/game/audioEngine.ts`. The shared module owns the singleton, the autoplay first-gesture handler, and the keyed noise-buffer cache (`'snare'`, `'hat'`, `'skid'`, `'rumble'`). Pure helpers (`droneFreqHz`, `droneFilterHz`, `droneVolume`, `skidIntensity`, `uiClickEnvelope`) live in `audio.ts` and are unit-tested in `tests/unit/audio.test.ts` against a minimal AudioContext stub.

### Stretch: personalization

- Slug-driven scale / tempo / root-key tweaks: live (see "Per-track music personalization" in the Build log above).
- Initials-driven perturbation: live as an opt-in on top of per-track flavor. New `initialsMusicSeed(initials)` and `personalizeForRacer(slug, initials)` helpers in `src/game/musicPersonalization.ts` fold an FNV-1a hash of the player's initials into the slug seed via a 13-bit rotate-left plus XOR (with a guard for the rare zero-after-XOR case) so flipping a single letter reshuffles every output dimension instead of just one. `AudioSettings` gained a `musicMixInitials: boolean` field with default `false` (so legacy stored payloads keep playing the existing slug-only flavor unchanged on upgrade) plus a backfill default for legacy stored payloads. `Game.tsx` reads both the slug and the player's initials each render and re-fires the existing music-personalization effect when either changes (or when the player edits their initials in Settings), so the game track picks up the new flavor on the next race without restarting the audio engine. The Settings pane's "Per-track flavor" sub-section gains a "Mix in your initials" toggle right under the existing flavor toggle (disabled when music is muted entirely or when per-track flavor is off, since the initials hash has nothing to fold into in that case). Tests: `tests/unit/musicPersonalization.test.ts` adds 18 tests covering empty / whitespace-only / non-string initials short-circuits, deterministic hashing, case-insensitive folding, sensitivity to single-letter changes, whitespace trim, the slug-only fall-back when initials are missing, menu-bound invariants across many (slug, initials) pairs, deterministic per-pair output, divergence-when-initials-change-on-same-slug, divergence-when-slug-changes-for-same-initials, divergence-from-slug-only personalization on most pairs, and the zero-after-XOR guard. `tests/unit/audioSettings.test.ts` adds default-false, round-trip-true, and legacy backfill (omitted in stored payload) coverage for the new `musicMixInitials` field.
- Per-pattern (lead / bass) variations remain on the wishlist.

---

## 14. Data Model (Vercel KV via `@upstash/redis`)

**Status.** Done.

Match FrackingAsteroids' client pattern in `src/lib/kv.ts`.

```
track:<slug>:latest             : string (current version hash)
track:<slug>:version:<hash>     : JSON { pieces, createdByRacerId, createdAt }
track:<slug>:versions           : list of { hash, createdAt } (newest first)
track:index                     : sorted set (score = updatedAt) for "load existing"
lb:<slug>:<hash>                : sorted set (score = lapTimeMs, member = "initials:racerId:ts:nonce")
lap:meta:<nonce>                : JSON { tuning, inputMode } per-lap metadata for the leaderboard side-panel
lap:replay:<nonce>              : JSON Replay { lapTimeMs, samples: [x, z, heading][] } for the ghost car
track:<slug>:<hash>:topReplay   : string nonce that points at the active ghost replay for this track version
race:token:<nonce>              : JSON { slug, versionHash, racerId, issuedAt } TTL 15min, deleted on submit
racer:<racerId>:firstSeen       : ISO timestamp (set once on first visit)
racer:<racerId>:lastSubmit      : ISO timestamp (updated on each submit, for audit)
ratelimit:submit:ip:<ip>        : incr, TTL 60s
ratelimit:submit:racer:<id>     : incr, TTL 60s
ratelimit:submit:daily:<ip>     : incr, TTL 24h
```

Anti-cheat tunables (`MIN_SEGMENT_MS`, `MAX_DAILY_SUBMITS`, etc.) mirror Determined's `api/leaderboard.js` values. Start conservative, relax if legitimate players hit limits.

### Build log

- Typed key helpers for every key family plus TTL constants: `src/lib/kv.ts`. Every function takes typed `Slug`, `VersionHash`, `RacerId` (zod-validated at the route boundary) and returns the exact key string documented above.
- Anti-cheat tunables live in `src/lib/anticheat.ts` as `ANTICHEAT_DEFAULTS` (overridable per-call via an `opts` arg). Rate-limit tunables live in `src/lib/rateLimit.ts` as `RATE_LIMITS`. Both are starting conservative.
- Tests: `tests/unit/kv.test.ts` pins the exact key shapes and TTL values so future refactors cannot silently change them.

---

## 15. Tech Stack (concrete)

**Status.** Done for the scaffold. Every dependency below is installed and wired into scripts or the test harness.

| Area            | Choice                                             |
| --------------- | -------------------------------------------------- |
| Framework       | Next.js 15 (App Router)                            |
| UI              | React 19                                           |
| Language        | TypeScript 5                                       |
| 3D              | `three` (raw, no react-three-fiber)                |
| Physics         | Custom math-based integrator plus raycast          |
| Touch           | Custom virtual joystick (ported from FrackingAsteroids) |
| Audio           | Native Web Audio API                               |
| Storage         | `@upstash/redis`                                   |
| Validation      | `zod`                                              |
| Unit tests      | Vitest                                             |
| E2E / smoke     | Playwright                                         |
| Hosting         | Vercel                                             |
| Initial assets  | Kenney Car Kit (CC0 low-poly glTF)                 |

Do not add new dependencies in these categories without user approval. See `AGENTS.md`.

### Build log

- `package.json` pins the listed libraries at the versions above. Scripts: `dev`, `build`, `start`, `lint`, `type-check`, `test`, `test:watch`, `test:e2e`.
- Vitest config at `vitest.config.ts` scopes to `tests/unit/**`. `passWithNoTests: true` keeps the command green between commits that temporarily remove tests.
- Playwright config at `playwright.config.ts` uses `next build && next start` as the webServer command (faster and more realistic than `next dev` on first request). Injects dummy KV env vars for smoke runs so the app does not crash on import.
- Three.js is pinned in `package.json`. The Kenney Car Kit race model ships at `public/models/car.glb` (CC0, Kenney Car Kit v3.1); the shared `Textures/colormap.png` it references sits at `public/models/Textures/colormap.png` so the glb's relative URI resolves. License note at `public/models/KENNEY-LICENSE.txt`. Loaded via `GLTFLoader` from `three/examples/jsm/loaders/GLTFLoader.js`.

---

## 16. Architecture

**Status.** Partial. Directory layout matches the target. Infrastructure (`lib/*`, `game/track.ts`, `middleware.ts`, all API routes including `/api/leaderboard`), core game logic (`game/tick.ts`, `game/physics.ts`, `game/trackPath.ts`, `game/sceneBuilder.ts`, `game/editor.ts`, `game/music.ts`, `game/audioEngine.ts`, `game/audio.ts`, `game/virtual-joystick.ts`), and the React components (`Game`, `HUD`, `Countdown`, `InitialsPrompt`, `PauseMenu`, `FeedbackFab`, `Leaderboard`, `TrackEditor`, `TitleMusic`, `TouchControls`) plus the `useClickSfx` hook are all in.

Mirror FrackingAsteroids' clean split: pure TypeScript game engine, React UI layer, serverless API routes, KV for persistence.

```
src/
  app/
    page.tsx              # home (initials prompt + create/load)
    [slug]/page.tsx       # race page
    api/
      race/start/route.ts
      race/submit/route.ts
      track/[slug]/route.ts
      feedback/route.ts   # ported from Epoch
  components/
    GameCanvas.tsx        # owns the Three.js renderer
    HUD.tsx               # lap time, best, countdown, confirmations
    TrackEditor.tsx       # top-down piece editor
    PauseMenu.tsx
    TitleScreen.tsx
    FeedbackFab.tsx       # ported from Epoch, with mods
    Countdown.tsx         # READY-SET-GO traffic light
  game/
    tick.ts               # pure (state, input, dt) returns state (unit-testable)
    physics.ts            # car integrator
    collision.ts          # raycast to track
    track.ts              # piece geometry + graph + loop validation
    virtual-joystick.ts   # ported from FrackingAsteroids
    music.ts              # streaming synth scheduler
    audio.ts              # SFX
  hooks/
    useGameState.ts
    useKeyboard.ts
    useTouchControls.ts
  lib/
    kv.ts                 # @upstash/redis wrapper
    schemas.ts            # zod schemas (track, submission, token)
    hashTrack.ts          # canonical SHA-256 of piece array
    signToken.ts          # HMAC race token sign and verify
    racerId.ts            # read/write racerId cookie server-side
    consoleCapture.ts     # ported from Epoch
  middleware.ts           # ensures racerId cookie on every request
```

**Game loop pattern (from FrackingAsteroids).** `tick(state, input, frameDeltaMs)` is a pure function that returns the next state. It is unit-tested in isolation. `GameCanvas` runs it each `requestAnimationFrame`. The React HUD reflects state via props and hooks.

### Build log

- Files currently under `src/`:
  - `app/layout.tsx`, `app/page.tsx` (home), `app/[slug]/page.tsx` (race page), `app/[slug]/edit/page.tsx` (editor page), `app/api/race/start/route.ts`, `app/api/race/submit/route.ts`, `app/api/track/[slug]/route.ts`, `app/api/feedback/route.ts`, `app/api/leaderboard/route.ts`.
  - `components/Game.tsx`, `components/HUD.tsx`, `components/Countdown.tsx`, `components/InitialsPrompt.tsx`, `components/PauseMenu.tsx`, `components/FeedbackFab.tsx`, `components/TrackEditor.tsx`, `components/Leaderboard.tsx`, `components/SlugLanding.tsx`, `components/TitleMusic.tsx`.
  - `game/track.ts` (direction helpers + validation), `game/trackPath.ts` (ordering + waypoints + on-track math), `game/tick.ts` (pure state update), `game/physics.ts` (arcade integrator), `game/sceneBuilder.ts` (Three.js scene + camera rig), `game/editor.ts` (cycle-piece helper + grid bounds), `game/music.ts` (Web Audio scheduler + title/game/pause tracks), `game/audioEngine.ts` (shared AudioContext + master gain + autoplay-gesture handler + noise-buffer cache), `game/audio.ts` (engine drone, tire skid, lap stinger, PB / record fanfare, UI clicks, off-track rumble + per-frame `updateDriveSfx`).
  - `hooks/useKeyboard.ts`, `hooks/useTouchControls.ts`, `hooks/useClickSfx.ts`.
  - `game/virtual-joystick.ts`.
  - `components/TouchControls.tsx`.
  - `lib/schemas.ts`, `lib/kv.ts`, `lib/hashTrack.ts`, `lib/signToken.ts`, `lib/anticheat.ts`, `lib/rateLimit.ts`, `lib/racerId.ts`, `lib/consoleCapture.ts`, `lib/defaultTrack.ts`, `lib/localBest.ts`, `lib/leaderboard.ts`, `lib/recentTracks.ts`, `middleware.ts`.
- Path alias `@/*` maps to `src/*` in `tsconfig.json` and `vitest.config.ts`. All imports in code and tests use the alias.
- Route handlers declare `export const runtime = 'nodejs'` so `node:crypto` works directly (`randomBytes`, `createHmac`, `timingSafeEqual`). Middleware stays on the default edge runtime but gates its KV write behind a dynamic import + try/catch so edge runtime limits do not matter here.
- Game loop pattern from FrackingAsteroids holds: `tick(state, input, dtMs, nowMs, path, params?)` is a pure function, fully unit-tested in isolation. `GameSession` runs it each `requestAnimationFrame`. React HUD reflects state via props with a throttled (~20 Hz) update + reference-equality bail-out.
- **Not yet landed.** `components/TitleScreen.tsx`, additional `hooks/*` (useGameState).

---

## 17. Deployment and Manual Setup

**Status.** Done. Vercel project is live, KV is linked across all environments, and `GITHUB_PAT` and `RACE_SIGNING_SECRET` are set. The steps below are kept as a record of what was configured.

These are steps the user performs in external dashboards. Every step is mandatory for a production deploy except where marked optional.

### 1. Create the Vercel project

- Vercel dashboard, "Add New", "Project".
- Import the `VibeRacer` repo from GitHub.
- Framework auto-detects Next.js. Accept defaults.

### 2. Preview deploys

- Automatic once the project is imported.
- Every PR gets a unique preview URL.

### 3. Production deploys on merge

- Automatic.
- Merges to `main` deploy to production.

### 4. Create the KV store

- Vercel dashboard, "Storage" tab.
- "Create Database", select **Upstash for Redis** (or the Vercel KV option if presented; they are compatible).
- Name the database `viberacer-kv`.
- Link it to the VibeRacer project across **Development, Preview, and Production** environments.
- Vercel auto-injects `KV_REST_API_URL` and `KV_REST_API_TOKEN` (or the Upstash equivalents). No code change needed.

### 5. Create the GitHub PAT for the feedback FAB (mandatory)

- GitHub, Settings, Developer settings, Personal access tokens, **Fine-grained tokens**, "Generate new token".
- Repo access: select `VibeRacer`.
- Permissions:
  - `Issues`: Read and write
  - `Contents`: Read and write (needed for screenshot upload to `.github/feedback-screenshots/`)
- Copy the token once generated (it is shown only once).
- Add to Vercel project environment variables as `GITHUB_PAT` in all environments (Development, Preview, Production).

### 6. Set `RACE_SIGNING_SECRET` (mandatory)

- Generate locally: `openssl rand -hex 32`.
- Copy the hex string.
- Add to Vercel project environment variables as `RACE_SIGNING_SECRET` in all environments.
- This key signs the anti-cheat race tokens. Rotating it invalidates all in-flight tokens.

### 7. Update the feedback target repo

- In the ported `app/api/feedback/route.ts`, change the hardcoded `Randroids-Dojo/epoch` to the VibeRacer owner and repo slug.

### Optional

- Add a `CODEOWNERS` file so feedback issues can be auto-assigned.
- Enable Vercel Analytics for traffic insight.

---

## 18. Stretch and Future

**Status.** Out of scope for v1. Listed for awareness.

Not v1. Listed so agents know not to scope-creep into them without approval.

- Car customization beyond the v1 paint palette: a racing-number plate decal landed (see Section 5 build log). Full liveries / stripes and multiple body shapes remain stretch.
- Music personalization: slug-driven landed (see Section 13). Initials-driven perturbation and per-pattern lead / bass variations remain stretch.
- More track pieces (ramps, banked turns, jumps, 45-degree turns).
- Friend challenges: live. Pause-menu "Challenge a friend" entry generates a `?challenge=<nonce>&from=<INI>&time=<MS>` URL pinned to the player's most recent submitted PB. Recipient opens the link, the page parses the params on the server (`parseChallengeFromSearchParams` in `src/lib/challenge.ts`), and `Game.tsx` fetches the referenced replay via `GET /api/replay/byNonce` and uses it as the active ghost (overriding the regular `auto` / `top` / `pb` source preference) for the entire session, even after the recipient sets their own PB. A cyan banner pinned just below the rear-view mirror reads "Challenge from XYZ beat 0:42.123" so the target is unambiguous. Implementation: `src/lib/challenge.ts` (URL build / parse), `src/app/api/replay/byNonce/route.ts` (read by-nonce replay), `submit/route.ts` returns the just-submitted nonce, `src/lib/localBest.ts::readLastSubmit` / `writeLastSubmit` track the player's most recent PB nonce per (slug, version) keyed under `viberacer.lastSubmitNonce.<slug>.<hash>`. The HUD banner styling matches the ghost car's cyan accent so the visual link between "the cyan ghost" and "the challenge target" is immediate. `src/components/PauseMenu.tsx` disables the entry until the player has a submitted PB on the current track so the generated link will always resolve to a real ghost. Tests: `tests/unit/challenge.test.ts` (URL build / parse / nonce validation), `tests/unit/api.replayByNonce.test.ts` (route 200 / 400 / 404 paths), `tests/unit/localBest.test.ts` adds 7 tests for the last-submit pointer storage, and the existing `api.raceSubmit.test.ts` happy-path test now also asserts the `submittedNonce` field on the response.
- Per-slug weather or time-of-day overrides: live. Track authors can bake a baseline mood (timeOfDay / weather) into the saved track version through the editor's Advanced panel; players see the author's intended look unless they turn off "Respect track mood" in Settings. See Section 3 build log.
- Swap the custom physics integrator for Rapier if the arcade model proves insufficient for depth.
